# WO-POST-GOLDEN-FOUNDATION — canonical authority and lean active tree

status: QUALIFICATION CHECKPOINT — UNACCEPTED; release and independent verification pending
kind: bounded non-rung Post-Golden documentation/QA normalization
owner: sole Builder for this bounded order
founder-authority: F-1 and F-2 approved verbatim on 2026-09-03
depends: Golden accepted; clean saved checkout codex/wo-post-golden-foundation-final at 80883799d5c845339a08f6430360b471c3d0eb09; approved reference 13cf85ee
build-authority: CLOSED — qualification commands only; FM-0 and Proof A CLOSED

## Plain-English outcome

QuantFlow will retain the same accepted application while a founder or fresh agent sees one current
route, one current rule set, and no closed work presented as active work. Historical orders and design
material remain recoverable under `docs/history/`; Golden evidence remains byte-for-byte immutable.

## LockStep

| Rung | Status | Goal | Acceptance proof | Stop condition |
|---|---|---|---|---|
| Golden | DONE | trusted Windows/product floor | accepted Golden designation | any Golden evidence mutation |
| Foundation Closure | ACTIVE | approve canonical documents; normalize gates and active context | this order's complete matrix + independent Verifier | product/runtime/schema drift or unresolved current authority |
| FM-0 | PARKED | first football feasibility decision | separate future `NEXT.md` rotation | never opened by this order |

## Frozen starting identities

- `main` / `origin/main` / Builder starting floor: `80883799d5c845339a08f6430360b471c3d0eb09`
- approved documentation parent: `1e04e400b2be72e5cd79d21ab54ed1b9bf734962`
- Golden candidate: `7c26141f3ab365630b01e8570b395f8856603453`
- Golden founder product: `a91b5deec9bb49538136c76a031648be2c08e05e`
- preserved pre-normalization `NEXT.md` SHA-256: `C355555AEDBBF15E143E21AC6166BAF6386329043DA741D237A010899A2D68AC`
- preserved ledger path: `docs/history/orders/NEXT-post-golden-ab40524d.md`

## Authorized result

### F-1 canonical documents

1. Record founder approval in the Official Roadmap and Institution Contracts.
2. Move both route documents from `docs/orders/` to `docs/plans/`.
3. Preserve the founder-approved Product Surface companion and Demo Spec byte-for-byte as canonical,
   versioned repository authority at `docs/plans/PRODUCT-SURFACE-AND-WORKFLOW-ARCHITECTURE.md` and
   `docs/plans/DEMO-SPEC.md`. Vault copies remain non-authoritative research/cache material.
4. Preserve the four-point clarification: deterministic metric definitions; Dataset purpose separation;
   CURRENT MISSION as a derived Kernel projection; retrieval discovers while the Kernel verifies.

### F-2 authority normalization

Apply P-1 through P-12 as one package. This order contains the complete binding requirements; private
Vault notes are non-authoritative research and are not required to execute or verify the work.

#### P-1 through P-7 — exact current-authority corrections

- **P-1 — `START_HERE.md`.** Replace the active-track banner so `NEXT.md` names the one open order or
  closes the Builder door; identify `docs/plans/OFFICIAL-ROADMAP.md` as the approved post-Golden route
  and `docs/history/orders/GOLDEN-RUN.md` as the completed historical R-ladder. In Authority Documents
  item 2, replace “active rung” with “open order.” Replace the old Golden/SPRINT/Institutional Build Plan
  route paragraph with the approved roadmap + contracts pointers and the rule that none authorizes work.
  In ADR item 7 state that only accepted ADRs bind; a `DRAFT` does not.
- **P-2 — current reference status.** Immediately below DOCTRINE Part V's heading, mark its six phases
  superseded by the completed Golden route and then the approved post-Golden roadmap; keep the reasoning
  but forbid selecting work from it. Add `status: reference — visual/product register; not build or route
  authority` near the top of both `docs/PRODUCT.md` and `docs/DESIGN.md`.
- **P-3 — `AGENTS.md`.** Replace the stale “no roadmap” paragraph with: `NEXT.md` alone authorizes builds;
  `docs/plans/OFFICIAL-ROADMAP.md` is the route; `docs/history/orders/GOLDEN-RUN.md` is completed history;
  neither is permission. Add `docs/plans/INSTITUTION-CONTRACTS.md` as required reading before admission,
  Dock, tool/capability, or Evaluation seam changes.
- **P-4 — old institutional route.** Move `docs/plans/INSTITUTIONAL-BUILD-PLAN.md` to
  `docs/history/plans/INSTITUTIONAL-BUILD-PLAN.md` and replace only its status line with
  `HISTORICAL — pre-Golden R18–R25 route; superseded by docs/plans/OFFICIAL-ROADMAP.md on 2026-09-03;
  R18 frozen by ADR-0004`.
- **P-5 — current pointer.** At final candidate, `docs/orders/NEXT.md` is a compact pointer with exactly
  one status, exactly one `active-order: none`, a closed Builder door, the four canonical `docs/plans/`
  companions, `g11-status: CLOSED`, `r18-status: FROZEN / SUPERSEDED`, and the preserved ledger path/hash.
  It must not restore the obsolete `BUILDER CANDIDATE AUTHORIZED` phrase.
- **P-6 — `species/hermes/README.md`.** Replace stale `hermes-orchestrator` desk identity with
  `hermes-research-director` and align only the named production profile identifiers with
  `species/hermes/dock-profiles.json`. This exact README is the sole excluded species content file in P-8.
- **P-7 — bounded stale lines.** In `docs/demos/dock.md`, align production profile ids to
  `species/hermes/dock-profiles.json`; in `docs/DEBT.md`, replace “active rung” with “work unless NEXT.md
  names it”; add a dated realized-consequences note to `docs/adr/0004-repository-golden-baseline.md`
  recording Golden acceptance, closed Phase 2/3, frozen R18, and `.mcp.json` retention for G11 F06; mark
  the “first institutional journey” in `docs/PRODUCT.md` as the planned Proof A destination, not present
  behaviour; move `docs/proposals/V2-SCOPE.md` to `docs/history/proposals/V2-SCOPE.md` and replace only its
  status line with `HISTORICAL — never approved; Golden route won; superseded by
  docs/plans/OFFICIAL-ROADMAP.md`.

#### P-8 through P-12 — gate and structure adjudications

- P-8 fingerprints every tracked file whose forward-slash path starts `collab-electron/`, `packages/`,
  `qf-kernel-schema/`, `species/`, or `tools/`; every tracked `**/bun.lock`, `**/bun.lockb`,
  `**/package-lock.json`, `**/pnpm-lock.yaml`, or `**/yarn.lock`; every tracked `.github/workflows/**`
  file; and the root `install.sh` entrypoint.
  The only content exclusion inside those trees is the targeted non-packaged
  `species/hermes/README.md`; the gate must name that exact exclusion and prove that source, package
  entrypoint, packaged resource, Canvas skill, and species runtime bytes are all fingerprint-bearing.
  Living authority prose outside that product set remains governed by documentation/status gates.
- P-9 accepts the truthful no-active-rung state and treats the completed R-ladder as history.
- P-10 preserves G11's immutable acceptance while checking its truthful `CLOSED` current status.
- P-11 preserves the pre-normalization `NEXT.md` bytes exactly at the path and hash above; `NEXT.md`
  becomes a short current pointer.
- P-12 moves the approved roadmap and contracts to `docs/plans/` and repairs all live links.

### Lean active-tree closure

This is structural isolation, not evidence deletion.

1. Move these 43 top-level historical process/order files from `docs/orders/` to
   `docs/history/orders/`: `AUTONOMY.md`, `SPRINT.md`, `GOLDEN-RUN.md`, and every existing `WO-*.md`
   other than this active order. The starting collision count is zero. All are byte-exact except the
   explicitly authorized `GOLDEN-RUN.md` status correction below.
2. Move `docs/plans/INSTITUTIONAL-BUILD-PLAN.md` to
   `docs/history/plans/INSTITUTIONAL-BUILD-PLAN.md` and mark it superseded without changing its
   historical body.
3. Move `docs/proposals/V2-SCOPE.md` to `docs/history/proposals/V2-SCOPE.md`, mark it superseded
   without changing its historical body, and retain
   `CAPABILITY-REGISTRY.md` as explicitly non-authoritative research.
4. Move `design/glacier/**` byte-exactly to `docs/history/design/glacier/**`. Its current README says
   cables do not exist and advertises a full visual swap; both statements are historical, not current.
5. At final closure move this order to `docs/history/orders/WO-POST-GOLDEN-FOUNDATION.md`.
6. Final top-level `docs/orders/` inventory is exactly `NEXT.md` and `PROTOCOL.md`; the existing
   `docs/orders/evidence/**` subtree stays intact and is not current authority.
7. Repair live references to moved material. Historical/evidence payloads remain byte-exact unless an
   existing gate requires a current pointer; do not rewrite accepted Golden evidence.
8. The only moved-file content exceptions are `GOLDEN-RUN.md` (R18 `active` → `frozen` plus one
   historical/superseded status marker), `INSTITUTIONAL-BUILD-PLAN.md` (one superseded status marker),
   and `V2-SCOPE.md` (one superseded status marker). The move manifest records each original Git blob,
   original SHA-256, post-annotation SHA-256, and exact authorized delta. The other 42 moved order files
   and all seven Glacier files must be byte-identical at destination.

### Retained-instrument decisions

- `.mcp.json`: KEEP as an explicitly invoked developer workflow; never product or Kernel truth.
- `collab-electron/packages/collab-canvas-skill/**`: KEEP as packaged product support; runtime resolution
  and `research-director-front-door` consume it.
- `tools/qf-vault-projection/**`: KEEP as a read-only developer instrument and current QA surface;
  `vault-projection`, `kernel-sole-writer`, `kernel-one-path`, and Golden dependency gates consume it.
- `design/glacier/**`: ARCHIVE as historical design reference.

No item may finish as `RETAIN_FAIL_CLOSED` in ordinary agent context. No product subsystem is removed by
this order.

## Allowed mutation surface

- `docs/orders/NEXT.md`, limited to P-5/P-11 plus the Router's Reader-to-Builder and final-close
  authority rotations required by this order;
- `START_HERE.md`, `AGENTS.md`, `docs/DOCTRINE.md`, `docs/PRODUCT.md`, `docs/DESIGN.md`, `docs/DEBT.md`,
  `docs/demos/dock.md`, `docs/adr/0004-repository-golden-baseline.md`, and only the moved status lines in
  `docs/history/plans/INSTITUTIONAL-BUILD-PLAN.md` and `docs/history/proposals/V2-SCOPE.md`;
- the exact moves above and live links needed because of those moves;
- `species/hermes/README.md` wording only;
- `qa/gates/hermes-production-inference-receipt.ts` and its unit test;
- `qa/gates/rung-ladder.ts`;
- `qa/gates/golden-g11-authority.ts`;
- `qa/gates/golden-phase3-requalification.ts` only to re-anchor the intentionally changed G11 gate and
  historical route path without weakening its census;
- `qa/run.ts` only for `repo-shape` enforcement and moved-path references;
- generator-owned `qf-atlas/atlas.json`, `qf-atlas/atlas.html`, and `qf-atlas/ATLAS.md` only, after all
  source moves/QA changes;
- this order and a bounded receipt under `docs/orders/evidence/post-golden-foundation/`.

## Hard exclusions

- no Electron product behavior, Canvas, Dock, Kernel, schema, generated ontology, runtime adapter,
  package manifest, dependency, lockfile, CI, installer, credential, data-provider, or FM-0 change;
- no Golden evidence edit or deletion;
- no Technique/provider/second-runtime selection;
- no broad prose rewrite and no new planning layer;
- no real provider call is required because accepted runtime bytes remain unchanged.

## Builder sequence

1. Reproduce the frozen identities and starting diff.
2. Freeze an exact source→destination move manifest with source blob and SHA-256; fail on collision.
3. Run starting focused gates and record inherited reds before mutation.
4. Apply F-1/F-2 and exact moves; repair only affected live pointers.
5. Make `repo-shape` enforce the final active-directory inventory.
6. Re-anchor `golden-g11-authority` only for the archived G11 order path and the truthful current
   `g11-status: CLOSED`. The permitted source delta is exactly: change the order-path constant from
   `docs/orders/WO-GOLDEN-G11.md` to `docs/history/orders/WO-GOLDEN-G11.md`, and change F01's accepted
   current-NEXT value from the former authorized-builder phrase to `CLOSED`. Preserve every F02–F10
   assertion and prove every unchanged bait remains RED. Re-anchor Phase 3 only by pinning and accepting
   that exact current G11 gate implementation while retaining the accepted historical gate/receipt proof;
   do not weaken its census, consumer proof, or any other historical comparison.
7. Run each changed gate's bait → RED → restore → GREEN.
8. Regenerate Atlas once; require HARD RED 0 and no stale paths.
9. Run the focused matrix, then the canonical Windows release verifier from this clean candidate.
10. Commit one immutable candidate and stop for a fresh independent Verifier.

## Acceptance matrix

- `git diff --check`
- `bun qa/run.ts doc-links`
- `bun qa/run.ts repo-shape`
- `bun qa/run.ts rung-ladder`
- `bun qa/run.ts golden-g11-authority`
- `bun qa/gates/hermes-production-inference-receipt.ts`
- `bun qa/run.ts golden-phase3-requalification`
- focused unit test for `hermes-production-inference-receipt`
- `bun qf-atlas/generate.mjs --check`
- `bun qf-atlas/ratchet.mjs`
- `bun qa/verify-release.ts`

Required structural assertions:

- canonical route is `docs/plans/OFFICIAL-ROADMAP.md`;
- contracts are `docs/plans/INSTITUTION-CONTRACTS.md`;
- `NEXT.md` has one status, one `active-order`, and a closed builder door at final candidate;
- completed Golden ladder contains no active rung and R18 is frozen/superseded;
- top-level `docs/orders/` is exactly `NEXT.md`, `PROTOCOL.md`;
- every move destination is unique; the 42 unchanged order moves and seven Glacier moves preserve exact
  source hashes; the three named annotation exceptions preserve original blob/hash plus post-change hash;
- accepted Golden evidence file hashes are unchanged;
- runtime/config/source fingerprint equals the accepted founder product fingerprint;
- no product/runtime/schema/package/dependency/lockfile/CI path changed;
- worktree and owned processes are clean/zero.

## Fail-capable baits

1. Product fingerprint: independently mutate one in-memory/in-isolated-copy byte in each of a product
   source, package entrypoint, packaged resource, packaged Canvas skill, and species runtime → RED each;
   change a living authority Markdown blob and the exact excluded `species/hermes/README.md` → remains
   GREEN; restore.
2. Rung state: mark one old rung active while `NEXT.md` says none → RED; restore → GREEN.
3. G11 status: substitute any value other than `CLOSED` → RED; restore → GREEN.
4. Repository shape: inject an in-memory top-level closed work order → RED; restore → GREEN.
5. Archive integrity: alter one recorded moved-payload hash in the isolated verifier input → RED;
   restore → GREEN.

## Independent verification and close

One fresh Verifier receives only the order, candidate SHA, and evidence path. It must rerun the focused
matrix, falsify the changed semantic gates (including all five product-fingerprint bearing classes and
unchanged G11 F02–F10 baits), confirm the move/hash manifest, confirm Golden evidence
immutability and product fingerprint identity, and run the canonical release verifier. It does not edit.

After PASS, Router fast-forwards and pushes `main`, confirms `main == origin/main`, removes no historical
evidence, records the accepted Foundation Baseline SHA, and leaves `NEXT.md` closed. **STOP before FM-0.**

## Reader questions

1. Does this order preserve Golden and the accepted product while making current authority and historical
   material unambiguous, with no second truth/route or hidden product change?
2. Are the exact move set, QA re-anchors, allowed paths, baits, release proof, and stop boundary sufficient
   to support the claims “authority-clean” and “repository-lean” without deleting necessary evidence?

Required verdict: **YES / YES**. Any NO narrows or repairs this order before a Builder opens.

## Finite founder amendment — 2026-09-05 (controls earlier conflicting mechanics)

Resume after Founder alignment: the Architect authorized F07's historical Atlas receipt
to resolve the accepted G11 Atlas blob at 1f81c469, preserving its falsifier and pinning the
exact anchor in Phase 3. Current Atlas regeneration/check/ratchet remain separate required
proofs. Retain the prepared exact F04 ADR appendix check and both historical hash facts.
Finalize CLOSED/archive/one immutable commit only if every required check is green.
If native release qualification stays red after one causal retry for an environmental/socket
timing issue, preserve credential-safe evidence and stop without broadening scope.

The founder explicitly assigned one Builder after a fresh independent semantic Reader returned
YES / YES on this amended scope. The saved checkout is the only checkout; no worktree, main edit,
merge, push, or FM-0 is authorized. Preserve the useful 80883799 thin normalization.
The historical 13cf85ee order and four approved documents are read-only content references;
replay content without cherry-picking stale commits.

- Freeze the clean 80883799d5c845339a08f6430360b471c3d0eb09 starting floor and exact move/hash manifest.
- This Builder may rotate NEXT to this one active non-rung order and close it at final candidate.
- Retain the existing two-line full-history checkout delta in .github/workflows/ci.yml;
  pin it separately as qualification-only, never as Golden product equivalence.
- Add install-kernel before windows-unit in qa/verify-release.ts, cwd packages/qf-kernel,
  bun install --frozen-lockfile, separate kernel install cache. Update qa/verify-release.test.ts.
  Native-Windows implementation judgment: add --linker isolated, matching the existing Hermes
  install convention. In a dependency-free source copy, the bare frozen command and copyfile
  backend both failed EPERM for the local schema package; isolated linking installed the same
  frozen dependency graph and the four real Kernel tests ran green, without a schema install.
  No manifest, dependency, lock, or separate schema install change.
- Historical P14-B mode validates historical accepted receipts only. Add an explicit current
  product fingerprint check: current HEAD equals Golden for collab-electron, packages,
  qf-kernel-schema, species, tools, all standard lockfiles, and install.sh. The exact
  species/hermes/README.md is the sole product content exclusion. CI is pinned separately.
  Historical receipt validity alone cannot satisfy current product proof.
- Preserve P-1 through P-7 intent while retaining compact 80883799 instructions. Restore four
  approved documents to docs/plans, changing only necessary location/status/link metadata.
- Archive 43 non-active top-level order/process files, both superseded plans, and seven Glacier
  files with original blob and before/after SHA-256. Only the three named status annotations
  may alter archived content. Preserve existing docs/orders/evidence completely.
- Re-anchor G11 only for CLOSED and the moved order path; preserve F02–F10 unchanged.
  F01 must recognize the already accepted archived order's exact CLOSED status and its
  independent Verifier/candidate/tree identity, as well as current NEXT's CLOSED G11 status.
  The prior two-substitution prescription was insufficient on 80883799: its order already
  records independent acceptance, not the earlier Builder-complete state. Preserve that
  order byte-exactly and pin these CLOSED-state substitutions in Phase 3 instead.
  Phase 3 must accept only that exact G11 delta and moved route path, retaining its census.
- qa/run.ts may enforce final active-tree shape and required moved paths. Regenerate Atlas last.
- Architect decision during this build — authorize the exact ADR appendix check. F04 must
  prove the entire original accepted ADR bytes plus one and only one byte-exact authorized
  `Realized consequences — 2026-09-05` appendix. Mutation, deletion, duplication or reordering
  of either part must go RED. Pin this exact implementation delta in Phase 3. No other G11
  assertion is broadened; this is the mechanical same-meaning re-anchor required by P-7.
  The accepted G11 ledger records ADR hash 8A867A4E… while its own accepted Git blob
  c648fba7… has SHA-256 A448FD71…. Keep both receipt fields unchanged and require the
  entire original canonical Git bytes (A448FD71…) before the exact appendix. Do not
  mislabel the historical recorded hash as the hash of that canonical Git blob.
- Falsify missing install-kernel in an isolated dependency-free copy using actual Kernel tests:
  real dependency failure, then restore install and green. Do not remove shared node_modules.
- Falsify five current fingerprint classes (source, entrypoint, resource, Canvas skill,
  species runtime), closed/rung state, G11 status, repository shape, and archive integrity.
  Each must go RED, followed by restored GREEN. Keep existing G11 baits.
- Run diff --check, doc-links, repo-shape, rung-ladder, golden-g11-authority, historical P14
  receipt, current product fingerprint, golden-phase3-requalification, focused units, Atlas
  check/ratchet, and the canonical native-Windows release verifier.
- If the pre-existing packaged-app socket-path readiness timeout reproduces, preserve the
  exact failure bundle and stop qualification; never increase timeouts, skip readiness, or
  weaken semantics. WSL-less CI is environmental, not current product proof.
- Write bounded evidence while active, then close NEXT with canonical pointers and honest
  candidate status, archive this order, commit one immutable candidate, leave the checkout
  clean with zero owned processes, and stop for independent verification. No self-approval.

## Qualification sequencing authority

The Architect explicitly authorized one UNACCEPTED qualification checkpoint commit with
this intended CLOSED NEXT/archive layout because the unchanged package guard requires a
clean checkout. This is not accepted closure. Preserve the pre-commit diff/manifest and
exact tree identity outside the checkout; run the canonical native-Windows release qualifier
against that clean checkpoint. If it passes, that exact immutable checkpoint is the candidate
for a fresh independent Verifier. If it remains red after one causal environmental/socket
retry, preserve the failure bundle and stop. No guard bypass, merge, push, or FM-0.
