# WO-103b — The write path, part 2: make the action surface honest before it is published (doctrine P2, rung 2 of 2)

status: open
assignee: builder
depends: WO-103 (done, merged `4b41a35`) · WO-H1 (done, merged `dff8ef8`)

## Objective

Adjudicate every declared-but-unexecutable action, collapse the second link store, resolve the
schema doc that describes a dead schema, and gate the one door that is about to open by itself —
all before WO-104 turns the action surface into a published tool surface.

## In plain terms

The system currently advertises six things it cannot do, and the next rung turns every advertised
thing into a button an AI can press. This order removes the six lies before they become buttons,
and adds an alarm on one door we know is unlocked.

## Why this rung is now and not later — the mechanism

WO-104 builds the generated tool plane. The generator emits **one tool per action** plus
`_get`/`_query` per object. Measured on `main` at `aeee874`:

```
77 tools = 23 objects × 2 (_get, _query) = 46,  plus  31 actions × 1 = 31
```

Verify it yourself before you start; a count you did not derive is a count you cannot defend:

```bash
bun -e 'const t=JSON.parse(await Bun.file("qf-kernel-schema/golden/tools.json").text());
const g=t.filter(x=>x.name.endsWith("_get")).length, q=t.filter(x=>x.name.endsWith("_query")).length;
console.log("total",t.length,"_get",g,"_query",q,"action-tools",t.length-g-q)'
```

So every dead action shipped into WO-104 becomes **a published tool that throws `Unknown command`
at `execute()`**. And `qf_observe_ticket` — the door ROADMAP debt #22 exists for — is *already
generated*, at `golden/tools.json:1060`. It is not served yet: nothing reads `tools.json` at
runtime and there are zero `ListTools` handlers repo-wide (both verified 2026-07-26). The door
opens on WO-104 **by codegen, with nobody making a decision**. That is what this rung interrupts.

## Context (measured 2026-07-26 against `main` at `aeee874` — cite these, do not re-estimate)

| Fact | Measured | Where |
|---|---|---|
| Object types | 23 | `schema.objects` |
| Creatable object types | **9** (was 3 pre-WO-103) | `creationCommands` |
| Link kinds | 15 | `schema.links` |
| Link kinds writable end to end | **7** | union-endpoint semantics, below |
| Link kinds not writable | **8** — 4 blocked by `pipelineFed`, 4 by a missing creation verb | below |
| Actions declared | 31 | `schema.actions` |
| Commands | 25 transition + 10 creation | `commands.ts` |
| **Dead actions** | **6** — `retry_run`, `close_run`, `request_approval`, `approve`, `deny`, `promote_type` | derived |
| Generated tools | 77 | `golden/tools.json` |
| Gates | 13 | `qa/run.ts` |

**The 8 unwritable link kinds, with cause:**

```
participates_in   no creation verb: competitor, market_event
settles           no creation verb: result, market_event
executes_in       no creation verb: execution_environment
assigned_to       no creation verb: task
offered_on        pipelineFed: instrument   AND no creation verb: market_event
quotes            pipelineFed: quote, instrument
lists             pipelineFed: instrument   AND no creation verb: venue
has_leg           pipelineFed: instrument
```

**Note the two compound rows.** `offered_on` and `lists` each have *both* kinds of blocker, so
lifting `pipelineFed` alone does not make them writable — `market_event` and `venue` still need
creation verbs. Only `has_leg` and `quotes` are unblocked by the ingest rung by itself. (Corrected
at the pre-build read, 2026-07-26; the original table listed only the `pipelineFed` cause.)

**Read that last line carefully.** `has_leg` is `ticket → instrument`. It is the link that records
*which selections a parlay is made of* — the founder's own primary use case, from real slips. It is
unwritable today because `instrument` is `pipelineFed` and no ingest path exists. **A parlay's legs
cannot be recorded as a graph.** WO-102 proved the shape is representable; WO-103 made links
writable; the one link that matters most is still blocked by a policy flag with no machinery behind
it. That is deliverable 5's ruling, and it is why the ruling is not a cleanup item.

**Counting rule (inherited from WO-103, still binding).** A link kind counts as writable only if at
least one legal `(from, to)` pair has both endpoints creatable through `execute()`. Union endpoints
count if *any* member qualifies — that is why `tests` (`run|strategy → hypothesis`) is writable via
`run` though `strategy` has no verb. Report only end-to-end writable kinds; list the rest with the
blocker named. **A capability count that includes unreachable capabilities is the failure doctrine
A5 names.**

---

## Deliverable 1 · The six dead actions — delete all six

These are architect rulings with reasons, not open questions. **If measurement contradicts a
reason, stop and say so** — do not implement around it.

| Action | Measured state | Ruling | Reason |
|---|---|---|---|
| `retry_run` | `run` terminal states are all `[]` (`succeeded: []`, `failed: []`, `cancelled: []`) — **no legal edge exists to wire** | **delete** | Adding `succeeded → queued` invents a workflow feature nobody asked for. Replay belongs to durable execution, ROADMAP debt #17, which is trigger-gated and not triggered |
| `close_run` | same — no legal edge | **delete** | same |
| `request_approval` | takes `subject_ref` for an approval that has **no object type anywhere** in the schema | **delete** | A verb whose subject type was never defined cannot be wired without first designing the type. That is its own rung |
| `approve` | takes `request_id` — an id for the same never-defined type | **delete** | same |
| `deny` | takes `request_id` + optional `reason` — same missing type | **delete** | same |
| `promote_type` | debt #19's subject; no named promotion authority exists | **delete** | Deleting it *strengthens* the guarantee. Promotion is the one-way door in this ontology; with no verb, it cannot happen at all without an order — which is the correct default for an irreversible operation. Record in debt #19 that the verb is gone and that restoring it is part of naming the authority |

**Predicted deltas — match these or explain the mechanism that moved them:**

```
actions       31 → 25
tools.json    77 → 71   (46 object tools unchanged, 31 → 25 action tools)
```

Deletion will trip `doc-action-surface`. **That is the forcing function for deliverable 3, not an
obstacle to route around.** Do not weaken that gate.

## Deliverable 2 · The `connection` ruling — one store, not two

Measured: `connection` carries exactly `{ kind, from_ref, to_ref }`, lifecycle `experimental`.
That is **a link stored as an object**, duplicating the `links` table WO-103 made writable. Two
mechanisms for one job is a soft breach of the One Rule.

The founder's cable use case is the real argument on the other side — dragging a tile to a browser
tile draws a visible cable, and a cable may be a *canvas gesture* rather than an ontology edge (the
cable principle, `SCOPES.md`). So this is a decision, not a cleanup.

**This deliverable is the ruling, in writing. It ships no schema diff.** (Sharpened at the
pre-build read, 2026-07-26 — the first draft described schema surgery while the declared
dependencies forbade object-type changes, so two builders could have done two different jobs and
both been honest.)

Concretely: **do not add, remove, or alter any property of `connection`, and do not delete the
type.** Deleting it is an object-type change that invalidates every `kernel.db`, which this order
explicitly does not do. Write the ruling; the diff lands in the rung that implements it.

**Rule on which of these it is, and say why:**

- **(a) Canvas presentation.** `connection` exists to draw a cable and carries no ontology
  meaning. Then its eventual shape is whatever the canvas needs, and `links` stays the only
  relationship store.
- **(b) A real edge.** Then it duplicates `links`, and the removal is scheduled — name the rung.

**Live constraint you must account for:** the cable design system is being designed right now, and
the founder's drag-to-browser-tile use case ("draw a cable to grant an agent control of that tile")
is the strongest argument for (a). Your ruling should state what happens to it under each option,
not assume the design away.

**The deliverable is one plain sentence answering *what is a cable*,** plus the ruling and its
reasoning, recorded in `SCOPES.md`. If you believe the ruling cannot be made without the schema
diff, stop and say so — that is a real finding, not a failure.

## Deliverable 3 · `docs/ONTOLOGY_SCHEMA.md` — ROADMAP debt #21, trigger fired

Re-measured 2026-07-26 (`grep -ow`, whole-word):

```
event 16 · market 10 · odds_series 2
market_event 0 · instrument 0 · venue 0 · quote 2 · mission 0 · policy 1 · environment 0
```

Zero mentions of the current market-plane names, two rungs after the rename. And the aggravating
fact: **`DOC_AUTHORITY_MAP.md:15` lists it under CURRENT — binding**, described as "Frozen schema
v0.1." The answer key says a doc describing a dead schema is authoritative.

**Read this before choosing — it corrects a defect in the first draft of this order (found at the
pre-build read, 2026-07-26).**

`doc-action-surface` **hardcodes the path** `docs/ONTOLOGY_SCHEMA.md` and requires **set equality**
between the `` `backticked` `` names under its `## Actions` heading and `schema.actions`
(`qa/gates/doc-action-surface.ts`). Two consequences the first draft got wrong:

1. **Demoting the file in the authority map does not move the gate.** Demotion is a
   *doc-authority* act; the gate is a *path* check. They are orthogonal. After deleting six
   actions, the gate reports `in doc but not code: approve, close_run, deny, promote_type,
   request_approval, retry_run` and stays **red** no matter what the authority map says.
2. **So syncing the `## Actions` list is mandatory under either option, and it is in scope.**
   Removing the six names from that list is required work, not drift.

Choose one and do it completely — **both include the `## Actions` sync:**

- **(a) Demote (recommended).** Sync `## Actions`; move the file to REFERENCE in
  `DOC_AUTHORITY_MAP.md` with a one-line reason; add a header banner to the file pointing readers
  at the generated, gate-checked `golden/ONTOLOGY.md`. Cheapest, honest, stops the bleeding. If you
  believe the gate should follow the demotion rather than keep guarding a reference doc, **say so
  as a finding — do not repoint or weaken the gate in this order.**
- **(b) Regenerate and gate.** Sync `## Actions`; then extend the generated surface to cover object
  types the way `doc-action-surface` covers actions, so it cannot rot again. Real machinery — if
  you pick it, say why the extra scope was worth it.

Either way `DOC_AUTHORITY_MAP.md:15` changes. A doc that is stale *and* labelled binding is worse
than a doc that is merely stale.

## Deliverable 4 · The `observe-door` gate — debt #22's check becomes a duty

ROADMAP debt #22 records that `observe_ticket` may be called by anything, and its check is a grep
somebody has to remember to run. **Three separate seats hand-wrote three triggers for this entry
on 2026-07-26 and all three read "safe" while exposed** (see Known findings). A trigger is a
document; documents rot. Make it a gate.

Add gate **`observe-door`** to `qa/run.ts`, using the `bunPackageGate` conventions WO-H1 extracted.
It asserts **two** things, and it must be red if either fails:

1. **The string surface.** `observe_ticket` / `qf_observe_ticket` appears only in this committed
   allowlist — measured today, code and generated artifacts only:
   ```
   qf-kernel-schema/src/ontology/research.ts   (definition)
   qf-kernel-schema/src/commands.ts            (command wiring)
   qf-kernel-schema/src/schema.ts              (aggregator)
   packages/qf-kernel/src/create.ts            (implementation)
   packages/qf-kernel/src/kernel.test.ts       (tests)
   qf-kernel-schema/golden/{ONTOLOGY.md,migration.sql,tools.json}  (generated)
   qa/gates/observe-door.ts  +  qa/run.ts      (the gate itself — see below)
   ```
   Any occurrence outside it — a `collab-electron` callsite, a `tools/` caller, an entry in
   `QF_EXECUTE_ALLOWLIST` — is **red**. Documentation paths (`docs/**`, `*.md`) are excluded;
   prose is not a call.

   **The gate's own files are on the allowlist, deliberately.** The gate must contain the strings
   it searches for, so without this it is red against itself forever — and a builder forced to
   dodge that would invent string-splitting or a blanket `qa/` exclusion this order never ratified.
   *(Added at the pre-build read, 2026-07-26: the first draft omitted this and the gate failed its
   own definition.)* Do not exclude `qa/` wholesale — name the two paths.

2. **The serving surface.** Nothing outside `qf-kernel-schema/` may, at runtime, either read
   `golden/tools.json` **or** call `generateMcp()`. Verified today: the only `tools.json` readers
   are `scripts/generate.ts` (writes it) and `src/generate.test.ts` (diffs it); `generateMcp` is
   called from those same two files and defined at `src/generate/mcp.ts:34`. All three are inside
   `qf-kernel-schema/`, so the clause is satisfiable today — confirm that before you build the
   gate rather than trusting this line.

   **Both halves are required.** *(Widened at the pre-build read: the first draft covered only the
   file read, and claimed that alone "catches WO-104." It does not — a tool server that imports
   `generateMcp(schema)` and serves the result directly opens the observe door without reading the
   file and without the string `observe_ticket` appearing anywhere in its code. Clause 1 would also
   stay green. That is the same class of hole this gate exists to close.)*

**State the gate's limit in your report, plainly.** Even widened, it does not prevent serving
`observe_ticket`, and it cannot cover a path nobody has thought of. What it does is make the two
known routes impossible to take **silently** — WO-104 will have to touch this gate, which forces
the decision into the open where a human sees it. A gate that is honest about what it does not
cover is worth more than one that implies coverage it lacks (WO-103's verification record set this
precedent, and the first draft of this very gate broke it).

Then update ROADMAP debt #22: replace its inline grep with a pointer to this gate. Leave the entry's
prose and trigger otherwise intact — it is the founder's wording.

## Deliverable 5 · Two rulings recorded, zero machinery built

**(a) The market-plane ingest seam — architect's ruling, record it.**

The contradiction: `instrument` and `quote` are `pipelineFed`, so `lintCommands` rejects creation
commands for them *by design* (`define.ts:535`). WO-107 says their rows "land through Kernel
commands." Both cannot be true, and 4 unwritable link kinds — including `has_leg` — sit on it.

**Ruling: `pipelineFed` stays. Ingest goes through `execute()` via a dedicated bulk command that
carries an ingest trace.** Reasons, both load-bearing:

- `kernel-sole-writer` is a hard law. An ingest path that bypasses `execute()` fails it, correctly.
- Dropping `pipelineFed` would let any caller hand-author a `quote` — a fabricated price. That is
  debt #22's fabrication risk one plane over, and it would be introduced *while* we are closing the
  first one.

**Your job is to record this, not build it:** a SCOPES contract for the ingest rung (objective ·
depends · in · out · gate) and a ROADMAP line noting the 4 link kinds it unblocks. If you find a
measurement that contradicts either reason, stop and say so.

**(b) The IPC allowlist — state the decision, change nothing.**

`QF_EXECUTE_ALLOWLIST` is one command wide (`publish_artifact`), enforced only at the renderer→main
IPC boundary (`ipc-kernel.ts:110`). Main-process callers reach `kernelExecute` unfiltered. That is
defensible as a trust tier — the renderer is untrusted, the main process is ours — but it is
currently an accident with a plausible story attached, not a decision.

Write the decision into `SCOPES.md` in one paragraph: what the tier is, why the boundary is where
it is, and what would have to be true to add a second command. **Do not change the allowlist.**
Adding to it is now debt #22's own trigger.

## Declared dependencies (do not discover these at commit time)

- `doc-action-surface` will go red on deliverable 1 until deliverable 3 lands. Sequence accordingly.
- `golden/` is regenerated, never hand-edited. `bun run generate`, then commit the diff.
- Deleting actions changes `golden/migration.sql`, `golden/ONTOLOGY.md`, `golden/tools.json`, and
  `golden/conformance.test.ts`. Expect all four; explain any you did not expect.
- `kernel.db` is unaffected — no object type or transition table changes here. If you believe one
  does, stop and say so.

## Gates

**G1 · Zero dead actions, enforced not asserted (builder-run).** After deliverable 1, a check
proves **set equality** between `schema.actions` and the actions covered by
`commands` + `creationCommands` — not merely that the counts agree. Prefer adding it as a lint in
the schema package so it cannot regress; if you assert it in a test instead, say why. **Falsify
it:** re-declare one deleted action without a command → **red** → remove → **green**. Paste all
four outputs.

**Known limit, state it in your report:** G1 goes green whether you *delete* the six or *wire*
them. It proves the surface is honest, not that you followed deliverable 1. G4's name-set check is
what pins the choice. *(Noted at the pre-build read, 2026-07-26.)*

**G2 · The `observe-door` gate, falsified both clauses (builder-run).**
- Clause 1: add `observe_ticket` to a file outside the allowlist (e.g. a scratch line in
  `collab-electron/src/main/kernel.ts`) → **red** → remove → **green**.
- Clause 2: add a runtime read of `golden/tools.json` from outside `qf-kernel-schema/` → **red** →
  remove → **green**.

Both, separately. A two-clause gate proven in one direction is a one-clause gate.

**G3 · `doc-action-surface` still does its job (builder-run).** Show it went **red** after the
deletions and **green** after deliverable 3. That transcript is the proof debt #21 was actually
closed rather than worked around. If it never went red, the deletions did not reach the surface it
guards — investigate before proceeding.

**G4 · Count predictions AND the name set (builder-run).** actions `31 → 25`, tools `77 → 71`,
gates `13 → 14`. State each measured value next to the prediction.

**Counts alone are insufficient here and the order says so rather than pretending otherwise**
*(sharpened at the pre-build read, 2026-07-26)*. Every one of those three numbers is reachable by a
wrong implementation: delete five dead actions plus one live one and wire a sixth, and you still
land on 25 with G1 green and the wrong surface. `77 → 71` follows from *any* six deletions and does
not say which six. `13 → 14` is satisfied by adding any gate at all.

So also paste **the exact set of removed action names**, and show it equals:

```
approve, close_run, deny, promote_type, request_approval, retry_run
```

and that the 25 survivors are exactly the previous 31 minus those 6. Set equality, not cardinality.
**Name the mechanism that produced each count.**

**G5 · Suite green, cold (verifier-run).** `bun qa/run.ts --all` from a fresh worktree, **exit code
stated explicitly on its own line** — do not pipe the runner; a pipe reports the pipe's exit code
(measured error, WO-102 verification). State schema and kernel test counts before → after and
explain any delta by mechanism.

## Evidence required in the report

G1–G4 transcripts, every bait both directions · the `doc-action-surface` red→green pair · your
one-sentence answer to *what is a cable* · which option you took for deliverable 3 and why · the
SCOPES contract text for the ingest rung · the allowlist paragraph · count predictions vs measured,
with mechanisms · confirmation `collab-electron` behavior is unchanged (the allowlist paragraph is
docs; no code edit) · **one paragraph on every place you exercised judgment where this order was
silent.**

That last paragraph is the most valuable thing you will write. Measured across this ladder: three
of WO-103's five defects were born in its *order text*, not its code, and a single adversarial read
found five more in minutes. **Assume this order contains one too**, and say where you think it is.

## Out of scope (hard)

MCP tool serving or any `ListTools` handler (WO-104) · building the ingest path (its own rung; you
write the contract only) · changing `QF_EXECUTE_ALLOWLIST` (debt #22's trigger) · real market data
(WO-107) · link **properties** and the `legs`/`has_leg` duplication — still a brief, still nobody's
rung · promoting any type to `active` (debt #19) · any `collab-electron` code change · the report
publication gate (WO-110).

## Known findings you inherit — recorded, do not re-derive

1. **`has_leg` is unwritable**, so parlay legs cannot be recorded as edges. Cause is `pipelineFed`
   on `instrument`. Deliverable 5(a) rules on it; the build is a later rung.
2. **`qf_observe_ticket` already exists** in `golden/tools.json:1060` and is unserved. Deliverable 4
   gates it.
3. **Three triggers, three seats, all wrong the same way.** On 2026-07-26 the observe-door risk was
   given three hand-written triggers — "more than one agent can write to the Kernel" (already true),
   "first callsite passing a command from a variable" (already true at `kernel.ts:75-81`), and a
   path-filtered grep (excluded `qf-kernel-schema/`, where the tool already sat). Each was a
   *proxy* for the danger rather than the danger itself. This is the reason deliverable 4 is a gate.
4. **A count-by-grep error, twice.** The tool surface was reported as 81 by
   `grep -c '"name"'` — parameter schemas carry `"name"` keys too. It is 77. Derive counts from
   parsed structure, never from line matches.
5. **`DOC_AUTHORITY_MAP.md` is itself unmaintained** — it labels a two-rungs-dead doc binding. WO-H1
   audited the debt register and found the same class. Consider whether the authority map needs the
   same treatment; **note it, do not fix it here.**

---

# PRE-BUILD READ — 2026-07-26 · seven findings, all in the order text

Run before any build, per `PROTOCOL.md` §"The reviewer role," trigger 2. Reviewer:
`cursor-grok-4.5-high` in a detached worktree at `b59c55b` — **not** the author (this order's
architect is a Claude seat) and not the builder who will execute it. Findings only; the reviewer
edited nothing. Full transcript and the exact prompt used:
[`evidence/wo-103b/prebuild-read.md`](evidence/wo-103b/prebuild-read.md).

**Result: seven findings, zero in the code, all seven in this order's text. All seven verified by
the architect against the codebase before being acted on** — reviews are testimony, not verdicts
(`PROTOCOL.md:31`). All seven are fixed above, each marked inline at the point of change.

| # | Severity | Defect | Fix |
|---|---|---|---|
| 1 | **High** | **D3(a) could not clear G3.** `doc-action-surface` hardcodes the path `docs/ONTOLOGY_SCHEMA.md` and requires set equality on `## Actions`. Demoting the file in the authority map does not move the gate — so the *recommended* option left the gate red, and a builder would have had to either stall or silently edit the action list unratified | D3 now states the mechanism and makes the `## Actions` sync mandatory and in-scope under **both** options |
| 2 | **High** | **G2 clause 1 failed its own definition.** The gate file must contain the strings it searches for, and the allowlist omitted it — red against itself forever, or dodged with unratified exclusions | `qa/gates/observe-door.ts` + `qa/run.ts` added to the allowlist by name, with a standing ban on a blanket `qa/` exclusion |
| 3 | **High** | **D2 was two jobs.** Option text described schema surgery while declared dependencies forbade object-type changes and the summary said the sentence was the deliverable. Two builders, both honest, different diffs | D2 is now explicitly **ruling-only, zero schema diff**, with the `connection` type frozen for this order |
| 4 | Medium | **G4's counts did not pin the six.** `31 → 25` is cardinality — delete five dead plus one live, wire a sixth, still 25, G1 green, wrong surface | G1 upgraded to set equality; G4 now requires the exact removed-name set |
| 5 | Medium | **G2 clause 2 was oversold.** It covered reads of `golden/tools.json` only, and the order claimed that "catches WO-104." A server importing `generateMcp(schema)` serves the observe door without reading the file — both clauses stay green | Clause 2 widened to runtime `generateMcp()` calls; the overclaim replaced with a stated limit |
| 6 | Low | Unwritable-link causes were incomplete — `offered_on` and `lists` each have **both** blocker kinds | Table corrected; the compound rows called out, since lifting `pipelineFed` alone does not free them |
| 7 | Low | `approve`/`deny` take `request_id`, not `request_approval`'s `subject_ref` | Table corrected; the underlying reason (no approval type exists) is unaffected |

**The base rate held.** WO-103 shipped five defects, three born in its order text. This order was
written by an architect who knew that, said so in the order itself, and still produced three High
findings — two of which (#1, #2) would have stalled the build outright, and one (#3) would have
split it into two incompatible interpretations. The read cost one model invocation and caught all
three before a builder ever opened the file.

**Standing consequence for WO-104 and after:** the pre-build read is no longer optional on any
order that adds a gate or deletes from a surface. Both of this order's High-severity gate defects
were of the form *the gate cannot fail, or can only fail* — which is exactly the pair of questions
the read asks, and exactly what neither the author nor a builder reliably sees in their own text.
