status: open
assignee: builder
depends: WO-004 (merged)
created: 2026-07-18
parallel: eligible — touches only `tools/runtime-proof/`, no overlap with WO-005

# WO-004a — Make the runtime proof assert what is actually true

> **Builder: this file plus `docs/orders/WO-004.md` (including its amended verdict) are your context.**
> This is a **correction order**. The code works; two assertions claim more than they measure.

## Objective

Replace WO-004's overstated assertions with ones that measure real values, so the proof cannot pass on a self-assignment or a lost race.

## Background (read the amended verdict in `WO-004.md` first)

`acp-main.ts` writes `toolLoopSessionId: acpSessionId` — a self-assignment. Nothing is passed into `agent.stream()`, and `ToolLoopAgent` has no session concept (`ai@7.0.31`). P1's third comparison is therefore a value against itself. The order that demanded it was wrong; the fix is to assert what the architecture genuinely provides.

## Deliverables (all inside `tools/runtime-proof/`)

1. **P1 restated — two layers, measured.** Assert exactly this and nothing more:
   - the ID AgentOS reports from `createSession` / `listSessions` **equals** the ID the guest minted in `newSession`, read back across the process boundary;
   - **delete** the `toolLoopSessionId` field and its assertion. Do not replace it with another echo.

2. **P1b — the loop is session-scoped, measured.** Prove the tool loop runs *inside* the session rather than carrying its identity:
   - every `sessionUpdate` the guest emits during the turn carries the session ID, asserted host-side from **received ACP notifications** (not from the receipt);
   - a prompt to an **unknown session ID is rejected**, proving the routing is real and not incidental.

3. **P4 strict.** `stopReason` must be **exactly `cancelled`**. Remove the `["cancelled","end_turn"]` alternation. If the mock can finish before cancel lands, slow it until the race is not close, and assert at least one chunk arrived **before** cancel and none after drain.

4. **P4 orphan-process assertion.** The agent is a **stdio child process**, so a leaked child is the real orphan risk and the current gate only counts listeners. Enumerate descendant processes before and after, and assert zero survive. (Behaviour is already correct — measured during verification — this makes it a gate.)

5. **Kill the `/tmp` receipt as a cross-layer oracle.** `/tmp/qf-runtime-proof-receipt.json` is a fixed path shared by every concurrent run, its write is swallowed by `catch {}`, and it is parsed with a cast. Replace with either ACP-native evidence (preferred — the notifications you already receive) or a per-run unique path that **fails loudly** on write error and is validated, not cast. No silent failure may be able to masquerade as a pass.

6. **One shared fixture.** P1/P1b/P3 each pay a full `AgentOs.create` today. Use one fixture per suite and pack the agent **once** (currently packed in `beforeAll` *and* again in the qa gate).

## Contract

- No live model, no API key, no credentials — unchanged from WO-004.
- Copy nothing from `~/QuantFlow` — unchanged.
- Do not touch `qf-kernel-schema/`, `packages/`, or `collab-electron/`. **WO-005 may be running in parallel; stay inside `tools/runtime-proof/` and `qa/run.ts`'s `runtime-proof` gate only.**
- If an assertion cannot be made honestly, **say so and leave it out** — that is the entire lesson of this order. A removed assertion beats a forged one.

## Acceptance gates

```bash
cd tools/runtime-proof && bun install && bun test
cd tools/runtime-proof && bunx tsc --noEmit          # exit 0

# COLD RUN — last, from repo root, paste output
rm -rf tools/*/node_modules qf-kernel-schema/node_modules packages/*/node_modules
bun qa/run.ts --all
```

**Prove your own P4 is real:** temporarily neuter the cancel path (e.g. make `cancel()` a no-op), show the suite **fails**, restore it, show it passes. Paste both outputs. An assertion that cannot fail is not an assertion.

## Out of scope

The Kernel (WO-005) · canvas (WO-006) · `commands.ts` (handled in WO-005) · the Zod-internals refactor and `schema.ts` split (tracked as debt in `ROADMAP.md`).

## Report back

1. Full `bun test` + `tsc` output; cold `qa --all` output.
2. The two neutered-cancel outputs (failing, then passing) from the gate check above.
3. What P1 now asserts, in one sentence, and what you deleted.
4. How you replaced the `/tmp` receipt.
5. Three decisions the order didn't specify; anything that fought you.

---

## Verification round 1 — REWORK (2026-07-18, verifier)

Verified cold in a detached clean-room worktree (`--detach`, so the branch stayed checked out in the founder's tree). **The clean room had zero `node_modules` on arrival — no deletion was needed at all.** All seven gates green, exit 0: `repo-shape`, `lockfile-committed`, `schema` (139 tests), `runtime-proof` (5), `kernel` (7; `illegal_transitions_rejected=7`, `replay_assertion=equal`), `kernel-sole-writer`, `no-canvas-domain-writes`.

**Scope: clean.** Only `tools/runtime-proof/` and `qa/run.ts`. Nothing touched `packages/`, `qf-kernel-schema/`, or `collab-electron/`.

**Confirmed by measurement, not by report:** `toolLoopSessionId` is gone (one comment remains); `qf-runtime-proof-receipt` has no references anywhere.

**Gates falsified independently by the verifier** — not on the strength of the builder's flag:
- corrupted what `listSessions` reports → P1 red;
- skipped `destroySession` → P4 red on `sessionGone`;
- restored both → green, `git diff` empty.

The builder's `QF_PROOF_NEUTER_CANCEL` falsification is legitimate: the flag skips the real `os.cancelSession()` call, a genuine behaviour change. Its polarity is safe — setting it makes the suite **fail**, so it cannot be used to quietly disable the gate.

### Defect D1 — a self-comparison survived the order that existed to kill self-comparisons

`tools/runtime-proof/src/proof.ts:96`:

```ts
const listedSessionId = listed.find((s) => s.sessionId === agentOsSessionId)?.sessionId;
```

This filters *by* the value and returns that same value, so `expect(run.listedSessionId).toBe(run.agentOsSessionId)` (`proof.test.ts:40`) is a tautology — it cannot fail.

**Measured consequence, before ranking it:** this is *not* a coverage hole. The verifier's corruption test failed loudly at the `throw` on line 97. The behaviour is genuinely guarded; the `expect` is decorative. That is materially milder than WO-004's `toolLoopSessionId`, which had no guard at all.

**Required fix — assert the table without filtering on the answer:**

```ts
const listed = os.listSessions();
// assert against the table's contents, not against a lookup keyed by the expected value
expect(listed.map((s) => s.sessionId)).toContain(run.agentOsSessionId);
```

Keep the loud failure for the "not listed at all" case. **Prove the new assertion can fail:** make `listSessions` report a different ID, show P1 red, restore, show green. Paste both outputs.

**Why this is a rework and not carried debt:** the founder's call, and the right one. A decorative assertion in *this* order is the one place it cannot be allowed to stand — the order's whole subject is assertions that claim more than they measure. Carrying it would have taught the opposite of the lesson.

Everything else in WO-004a is accepted. Fix D1 only; do not touch anything else. Same branch, `wo-004a`.

---

## Verification — round 2 (2026-07-18) · PASS

**Branch `wo-004a` @ `563c818`, verified cold in a detached clean-room worktree.**

What was re-run, independently of the builder's report:

- **Cold run:** fresh worktree at `563c818`, zero `node_modules` by construction, `bun qa/run.ts --all` → exit 0, all 7 gates green. P1 output now prints the real `listSessions` table.
- **Falsification, verifier's own hands:** `QF_PROOF_CORRUPT_LIST_SESSIONS=1 bun test` → **4 tests red**; flag removed → **5 green**. The corruption path rewrites every ID in the listed table, so a misreporting `listSessions` cannot pass.
- **Scope:** `git diff --stat origin/QuantFlow...origin/wo-004a` — five files, all inside the order (proof tool + `qa/run.ts`). Clean.

**D1 verdict:** fixed. `listedSessionIds` is the full table, mapped without keying on the expected value — the provenance is honest, which was D1's entire substance. `expect(run.listedSessionIds).toContain(run.agentOsSessionId)` is the assertion the rework demanded, verbatim in shape.

**Accepted with eyes open — one redundancy, charged to the architect, not the builder.** The retained `throw` in `proof.ts` guards the *same predicate* as the new `expect`, and fires first — so the `expect` line itself can never be the failing frame; every red surfaces as the throw's message. The round-1 rework note caused this: it said *"keep the loud failure"* and *"prove the new assertion can fail"* in the same breath, and those two sentences cannot both be satisfied when they share a predicate. The builder implemented the note faithfully; the red output being the throw's error is honest evidence, not evasion. Behavior is genuinely guarded (proved red/green twice, by two agents). Ruling: **redundancy, not defect** — two checks of one honest predicate, one of them dead weight. If `proof.ts` is ever touched again, delete the throw and let the `expect` be the sole check; it does not warrant a rework lap, and it does not enter the debt register because nothing is unguarded.

**Lesson for the archive:** this is the fifth architect-authored defect (the contradiction lived in the verifier's own rework note). It is the strongest evidence yet for the pre-build reviewer read in `PROTOCOL.md` — the architect's eyes are spent on the architect's text, including rework notes, which should get the same two-question read as orders.

**Rotation:** this commit merges `wo-004a`, flips the log to done, retires debt #2, and rewrites `NEXT.md` to WO-006a.
