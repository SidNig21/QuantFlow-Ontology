# WO-GOLDEN-G2-INTEGRATION — Resume preserved G2 over accepted prerequisite

status: **SEMANTIC READER YES/YES — BUILDER AUTHORIZED AFTER NEXT.md ROTATION**
order-type: Golden G2 bounded preservation-integration amendment
branch: `wo-golden-g2`
authority-head: `45c395f3af712cf3f30269d208a940e9f95b9b7f`
reader-task: `01a0380f-defb-76a0-a367-1e9564847225`
observe-door-reader-task: `01a03bf0-9713-79a3-87dd-7a6ec14a0d13`
full-g9: **PARKED AFTER G8**
main-authority: **NONE**
r18-authority: **FROZEN**

## Outcome

Resume preserved G2 deletion/proof work without losing the independently
accepted minimum G9 prerequisite. This amendment owns only the five-file overlap
and finite stale-log recovery. It does not expand G2, G8, full G9, G3, or R18.

## Frozen preservation inputs

- stash: `4e4dac24187f54a7187e5e61ab0459acbe7cd3ed`;
- patch parent: `615b347d6a8f9c7a5a1beca1f36ca87bcddc266a`;
- untracked parent: `c5186873abd4d83f9ca035c336698464840eff5e`;
- patch SHA-256: `5BFE74B5664C9C0C098DB0DA0224E3E05AE5F612754490C5DB1486BC106AD3BF`;
- manifest SHA-256: `55757E1F568EBAC4A17907A9233152D341EC17162D738C31627D181338FEA0BC`;
- recovery root: `C:\tmp\qf-g2-untracked-recovery`;
- recovery-final TSV: `91A46D62699A301881842443EAC3FE2E54D46D215028D946422B599FAA3A8D4A`;
- recovery TSV: `96D8482729F749D0E647086A7CDF6A9474F2146E723DEADDEDA83DE6B29A755C`;
- untracked archive: `A61801F0A00B8C7C55D42C56529BBA9C8D3C16DE4B0A3DE73D247AAAD3A0D758`.

Write an immutable recovery receipt binding these inputs and every path/hash
outcome before changing product or proof source.

## Exact restoration contract

Restore every non-overlapping tracked G2 patch path exactly. Restore the 13
recoverable untracked evidence files from the recovery root and require their
frozen manifest hashes: eight raw-exact and five exact after recorded CRLF-to-LF
recovery. Retain the stash until immutable G2 candidate and independent PASS.

These 12 old command logs cannot be recovered byte-for-byte and must be named
`LOST / UNRECOVERED / SUPERSEDED` with frozen manifest hashes:

```text
logs/21-matrix-01-shared-paths.txt
logs/21-matrix-02-research-director.txt
logs/21-matrix-03-kernel-market-lineage.txt
logs/21-matrix-04-governed-review.txt
logs/21-matrix-05-research-world-visible.txt
logs/21-matrix-06-artifact-root-retry.txt
logs/21-matrix-06-artifact-root.txt
logs/direct-artifact-root-semantic-copyfile-final.txt
logs/direct-artifact-root-semantic-correct-cwd.txt
logs/direct-artifact-root-semantic-junction.txt
logs/direct-artifact-root-semantic-symlink-final.txt
logs/direct-artifact-root-semantic.txt
```

Do not claim their historical bytes were restored. The complete G2 matrix
regenerates current receipts at the same paths. Record new UTC times, exits,
sizes and SHA-256 values in `COMMANDS.tsv`; retain old hashes as lost evidence.

## Five-file overlap

### `qa/gates/artifact-root.ts`

Keep current accepted bytes. The paused version differs only in formatting and
has identical install-plan, helper, ordering and failure meaning.

### `qa/gates/artifact-root/run.ts`

Preserve accepted production
`runTurn(sessionId, prompt, { finalize: true, skipPublish: false })` proof,
trajectory Artifact, exact bytes/hash/path/root, one `produces` link, session
close/runtime destroy, zero ordinary Reports, exact Report refusal with
unchanged Artifact/event counts, and all eight falsifiers:

```text
kind
producer-link-count
producer-link-identity
bytes-identity
hash-identity
storage-ref-identity
root-confinement
report-refusal
```

Remove only retired A2A artifact-store import/exercise, A2A publication gate,
A2A bus coupling gate, `sawA2a`, and A2A expected-publisher entry. Retain
root/install-plan proof, `MIN_MAIN_FILES`, `sawHost === true`, and exhaustive
production scanning.

Truthful direct-publisher expectation after deletion:

```text
agent-host.ts x1
index.ts x1
kernel.ts x4
ontology-gateway.ts x1
```

`agent-host.ts` is the accepted trajectory path. `index.ts` remains the
G9/proof-integrity Report-refusal path. Four `kernel.ts` calls and the
`ontology-gateway.ts` call remain visible G8-owned write-law/proof-integrity
surfaces. G2 neither deletes nor blesses them. Any other count/path is red.

### Generated Atlas outputs

Do not restore stale `qf-atlas/ATLAS.md`, `atlas.html`, or `atlas.json` bytes.
Restore non-generated G2 Atlas edits exactly, then regenerate all three from
combined accepted source. Only the seven G2 deleted product rows may disappear.
Surviving `reach` and `imports` remain byte-equal; importer changes are limited
to causal removal of deleted edges.

## Allowed paths

The allowlist remains exactly WO-GOLDEN-G2's 11 frozen deletions, five proof
files, `qf-atlas/decisions.json`, `falsify.mjs`, comment-only `generate.mjs` and
`reach.mjs`, generated Atlas outputs and `falsifiers.json`, and
`docs/orders/evidence/golden-baseline/g2/**`.

No other product, package, lockfile, schema, Kernel, Dock, Canvas, runtime, G8,
full-G9, G3, or R18 path may change.

## Mechanical observe-door classification repair

The first independent candidate verification proved matrix item 8 was a real
exit-1 receipt, not a green command. A fresh read-only Reader established that
all three reported surfaces predate G2, are byte-identical from original G2
start `6f73e61f74dc5f438da90d92e3f1ed3a33586297`, and cannot serve an action at
runtime. This is an ADR-0004 same-meaning proof-harness correction. The product
invariant remains unchanged: no production, app, or tool runtime may serve
`observe_ticket`.

Authorize exactly one proof-file edit to `qa/gates/observe-door.ts`:

- add only `qf-atlas/atlas.json` to `OBSERVE_ALLOWLIST`, because it is a
  generated developer projection and not a runtime input;
- add only `qa/gates/doc-action-surface.ts` and
  `qa/gates/governed-review.ts` to `SERVING_ALLOWLIST`, because they are static
  QA consumers that neither register nor advertise tools;
- add no blanket `qa/`, `.json`, `qf-atlas/`, generated-output, extension, or
  directory allowance.

The repair must prove all of the following before a replacement candidate:

1. `bun qa/run.ts observe-door` exits 0;
2. `bun qa/run.ts tool-plane` proves real MCP `tools/list` excludes
   `qf_observe_ticket`;
3. an unallowlisted production-scope file naming `observe_ticket` makes the
   gate red;
4. an unallowlisted JSON reference to `golden/tools.json` makes the gate red;
5. an unallowlisted QA consumer of `generateMcp()` makes the gate red;
6. `bun qf-atlas/generate.mjs --check` and the accepted G2 Atlas causal-diff
   remain green;
7. the three newly classified paths contain no `McpServer`, `registerTool`,
   transport, runtime action dispatch, or product import/read surface.

The Reader adjudication is preserved at
`docs/orders/evidence/golden-baseline/g2/OBSERVE-DOOR-READER-ADJUDICATION.md`.
No other matrix assertion, allowlist entry, timing, product path, or acceptance
meaning may change. After the repair, a fresh independent Verifier reruns the
complete matrix; only the already-frozen item-3 G8 red remains admissible.

## Builder and verification

The same G2 Builder resumes from clean `45c395f`, performs bounded recovery and
merge, then runs WO-GOLDEN-G2 matrix items 1–21 in exact order. Item 3 must
reproduce only the accepted pre-existing G8 red fingerprint; all other items
pass. Each prerequisite falsifier must turn only its intended receipt red. Stage
only the allowlist, run both diff checks, commit one immutable candidate, push,
and stop.

One fresh independent Verifier binds to the candidate, reruns the full matrix
except receipt-mode mutation, proves recovery/path/hash claims, publisher census
and Atlas causal diff, and requires
`sha_before == sha_after == CANDIDATE_SHA`, clean tree and zero processes.

## Stop conditions

Stop on missing loss-hash receipt, false restoration claim, recovery/hash
mismatch, unallowlisted change, weakened prerequisite proof, publisher drift,
unexpected Atlas delta, falsifier loss/failure, unexpected matrix red,
incomplete rerun, verifier SHA mismatch, or nonzero process census.

No founder stop is triggered by finite stale-log recovery. Any semantic request
outside this amendment is a new stop. Full G9 remains after G8.
