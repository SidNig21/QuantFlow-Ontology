# WO-002 — Docs graduation: the game plan lives in the repo

status: done
assignee: fable (architect; docs-only, no code — self-execution permitted, CI verifies shape)
depends: none
created: 2026-07-18

## Objective

Graduate the frozen vault documents into the repo and establish the work-order ladder, so that builders can see the entire plan from GitHub alone and the vault becomes research archive, not authority.

## Deliverables

1. `docs/BLUEPRINT.md` — mission, decided stack (exact tools per layer), canvas-seam laws, harvest rules, disciplines.
2. `docs/ONTOLOGY_SCHEMA.md` — schema v0.1, frozen as `experimental`.
3. `docs/ROADMAP.md` — definition of done, phase gates, numbered work-order ladder.
4. `docs/orders/PROTOCOL.md` — the workshop protocol (roles, loop, WO template).
5. `docs/orders/WO-001.md` — full order text (previously vault-only).
6. `START_HERE.md` + `DOC_AUTHORITY_MAP.md` updated: authority pointers to the new docs; `quantflow` → `QuantFlow` branch-casing drift fixed.
7. `.gitignore` whitelist extended so the new docs are tracked despite upstream's blanket `docs/*` ignore.

## Contract

- Docs-only: no source, no config beyond `.gitignore`, no schema *semantics* changes — `ONTOLOGY_SCHEMA.md` is the vault v0.1 verbatim in content, reformatted only.
- Roadmap details one phase ahead only; later phases are gates + named placeholders.

## Acceptance gates

```bash
git check-ignore docs/BLUEPRINT.md docs/ONTOLOGY_SCHEMA.md docs/ROADMAP.md; test $? -eq 1  # not ignored
bun qa/run.ts        # all gates green
```

CI green on push is the final verifier.

## Out of scope

Canvas-seam laws final wording if not yet supplied by founder (slot reserved in BLUEPRINT.md); WO-001 execution; any code.
