# WO-008 — The plug test: Hermes through the same socket
status: open — blocked on WO-007
assignee: builder
depends: WO-007
created: 2026-07-19

> **This is the order everything has led to: the first real agent enters QuantFlow, and the gate is that nothing had to change to admit it.** The dock contract's claim — a plug is data, not code — either survives this order or was never true.

## Objective

Hermes, wrapped as an AgentOS species package, admitted to the dock **by an admitting commit whose diff touches zero dock, canvas, or host source** — spawned from the dock into a session tile like any other species, with its lifecycle in the Kernel ledger.

## Founder prerequisites (before the builder starts — credentials never touch the repo, an order, or a builder)

1. Install Hermes on this machine: `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash` (vendor: hermes-agent.nousresearch.com/docs, Getting Started).
2. `hermes setup --portal` (or place a model key via `hermes config` / `~/.hermes/.env`) — model auth is yours alone; builders and gates must never need it.

## Context pack

1. `docs/ROADMAP.md` §dock contract (§1 "a plug is data, not code"; the WO-008 row: **the admitting diff is the gate**)
2. `docs/orders/WO-007.md` + its verification record — the registry, `register_agent_definition`, and the `linkSoftware` admission path this order rides on
3. `tools/runtime-proof/agent-package/` — the proven wrapper shape
4. `docs/BLUEPRINT.md` §open-host — the anti-Eve rules: Hermes' own memory/state (`~/.hermes/`) is **its** working state, never QuantFlow's ledger; the Kernel records sessions/artifacts only

## Measured facts (verifier, 2026-07-19 — every SDK-facing claim cited)

- **`hermes acp` exists:** vendor CLI reference (hermes-agent.nousresearch.com/docs/reference/cli-commands): *"Run Hermes as an ACP (Agent Client Protocol) stdio server for editor integration."* Same stdio-ACP guest shape the runtime proof validated.
- **Package wrapper shape** (measured, `tools/runtime-proof/agent-package/agentos-package.json`): `{ "name", "version", "agent": { "acpEntrypoint": "<command>" }, "registry": { … } }`.
- **Hermes state root:** `~/.hermes/` (config.yaml, .env, memories/, sessions/); `HERMES_HOME` overrides it (vendor configuration reference). Its config precedence: CLI args > config.yaml > .env > defaults.
- **Hermes is NOT currently installed on this machine** (`which hermes` empty) — hence the founder prerequisite.
- Runtime admission without host restart: `os.linkSoftware(...)` (measured `@rivet-dev/agentos-core` `agent-os.d.ts:554`, landed in WO-007).

## Deliverables

**0. Smoke first — the ACP handshake, no model call.** With Hermes installed: initialize an ACP connection to a spawned `hermes acp` (reuse the proof-tree's client wiring) → session created → log the guest-minted session ID. **Do not send a prompt** — model auth is the founder's half. If the *handshake itself* requires auth, or Hermes' ACP surface deviates from the proven protocol shape in any way (who mints the ID, cancel semantics), **stop and report the exact behavior — that finding redirects the order and is a success.**

**1. The species package: `species/hermes/`** (new top-level dir — deliberately outside `collab-electron/` and `tools/`).
- `agentos-package.json`: `name: "hermes"`, `agent.acpEntrypoint` naming a shim command.
- The shim: a small launcher that spawns `hermes acp` (resolved from `PATH`) and pipes stdio — no flags, no `HERMES_HOME` override (the founder's own auth/state must apply), no env injection, **no credentials touched**. If Hermes is absent, exit with a loud, typed message naming the founder prerequisite.
- A pack step producing the installable package, following the proof tree's pattern; packed output path is the row's `package_ref`.
- A registration one-liner, `species/hermes/register.ts`: calls `execute("register_agent_definition", { name: "hermes", role: "orchestrator", package_ref: <packed path> })` against the app's `kernel.db`. It lives with the species, not in the app.

**2. THE ADMITTING COMMIT — the phase gate itself.** The final commit on the branch contains **only** `species/hermes/**` (and its lockfile). Zero changes under `collab-electron/src/**`, `packages/**`, `qf-kernel-schema/**`, `qa/**`. Paste `git diff --name-only <commit>^..<commit>` in the report — **that output is the gate.** If you find yourself needing to touch dock, host, kernel, or gates to admit Hermes, **stop: that is a WO-007 defect to report, not something to patch around.** (Anything genuinely needed outside `species/` belongs in *earlier* commits on the branch, argued explicitly in the report — the admitting commit stays pure.)

**3. Admission, live:** run `register.ts` → the dock shows Hermes (WO-007's push/refresh) → spawn from the dock → `linkSoftware` path admits the package without app restart → `create_agent_session` with the **Hermes-minted** ID → session tile streams whatever the handshake yields pre-auth → cancel → honest terminal states. Paste the log lines and the receipt query showing `agent_definition.registered` (id `hermes`) and the session events.

**4. Nothing else.** No permissions manifest columns, no tool allowlists (deferred exactly as WO-007 stated), no Hermes config management UI, no second registration path.

## Contract

- **No credentials in the repo, in env files, in test fixtures, in logs pasted to reports.** If a step seems to need one, stop.
- Hermes' `~/.hermes/` is its own working state — QuantFlow reads nothing from it and writes nothing to it. The Kernel ledger records only what crosses the seam: session lifecycle events and any published artifacts.
- All standing gates stay green untouched: `agent-path`, `dock-registry`, `one-skin`, seam gates. **You may not edit any gate in this order.**
- The app builds and runs with Hermes absent (species row present, spawn fails loudly per WO-007's typed error) — Hermes is a plug, not a dependency.

## Acceptance gates — builder-run (paste unedited)

1. Deliverable 0's handshake log: `hermes acp` session created, guest-minted ID shown, no prompt sent.
2. `git diff --name-only` of the admitting commit — nothing outside `species/hermes/`.
3. Admission evidence per deliverable 3: dock screenshot with Hermes present, spawn/cancel logs, receipt query output.
4. `cd collab-electron && bun run build` — exit 0 (nothing in the app changed; prove it anyway).
5. With Hermes binary temporarily renamed (simulate absent): spawn fails with the typed error, app stays healthy; restore. Paste both.

**Never `bun qa/run.ts --all`, never delete `node_modules`** — the cold run is the verifier's.

## Out of scope

A2A / bindings / Critic (next order) · permissions manifests and deny-by-default enforcement · Hermes model/config management from QuantFlow · MCP integration between Hermes and the Kernel · any change to dock/canvas/host/kernel/gates (that's the point).

## Report back

1. Gates 1–5, unedited.
2. Deliverable 0 verdict first: Hermes' actual ACP behavior vs. the proven protocol shape — identical, or the exact deviations.
3. Where anything outside `species/` proved necessary (should be: nowhere) and why.
4. Three decisions the order didn't specify, one line each.
5. Anything ambiguous or unbuildable — stop and say so.

---

*After this order passes and the founder checkpoint demo runs (skin + dock + Hermes in a tile), the A2A order follows: a second species with a host binding, the Kernel recording `delegates_to` as the receipt — transport per the AgentOS bindings docs, never canvas cables.*
