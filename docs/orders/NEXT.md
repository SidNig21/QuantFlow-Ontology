# NEXT — the current order (rotated 2026-07-30 after WO-D2b PASS)

> **Builder: this file is your complete entry point.** It always points at the single unblocked
> order. Do not choose another order or proceed past it.
> **Founder:** give a fresh builder this file: *“Follow `docs/orders/NEXT.md`.”*

## Current order: **[WO-K3b](WO-K3b.md) — every production artifact writer uses the canonical shelf**

Read `START_HERE.md`, this file, WO-K3b, and `PROTOCOL.md` in the required order. Branch from the
verified WO-D2b merge on `main`, use an isolated worktree, build the bounded correction as one batch,
then run its focused acceptance once and stop for independent verification.

**In plain terms:** failed Dock launches are now safe. Before renaming the app, fix the one remaining
A2A report writer that still saves files under the old app folder instead of QuantFlow's canonical
artifact shelf.

## Build priority

1. Re-measure the read-only founder Kernel artifact/old-root counts; never mutate operator data.
2. Route the production A2A default through the same resolved artifact root as agent reports while
   preserving its explicit test override and all delivery behavior.
3. Expand `artifact-root` to dynamically prove that exact production helper and exhaustively reject
   any third ungoverned production publisher.
4. Run unit/build and the two focused architecture gates once after the whole batch, then perform
   the two required production baits.

## Hard boundaries

- Never place, execute, or automate a bet or trade; never handle credentials.
- No existing artifact migration/deletion, schema/dependency change, A2A redesign, model call,
  prompt, or network access.
- Do not begin the product rename inside this order. WO-N1 follows immediately after verification.

## Behind WO-K3b

WO-N1 is pre-build audited and queued next. It will move product identity and app-local state to
QuantFlow with atomic migration, including Electron browser state and existing workspace metadata.
Caller-bound per-profile grants still close before unscripted WO-109 collaboration; Bovada, browser
tiles, and RL remain on their doctrine rungs.

---

*The order log in [README.md](README.md) wins on status. The verifier rotates both this builder door
and [VERIFYING.md](VERIFYING.md) in the same passing merge.*
