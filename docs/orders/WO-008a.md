# WO-008a — Permission bridge + tool policy (Hermes live turn)
status: done — verified + merged 2026-07-20
assignee: builder
depends: WO-008c (merged — host ACP handshake; runTurn still forbidden on host_acp)
created: 2026-07-20

## Objective

Wire ACP **permission requests** on the host bridge (deny-by-default, founder-visible grant) and unlock `runTurn` for `host_acp` sessions so the founder can complete a **real Hermes turn** from the dock/session tile — without reviving legacy auto-approve (`acp-agent.ts`, debt #14).

## Why this order exists (measured)

WO-008c landed host `hermes acp` admit (Outcome A) + Kernel `created`/`started`. `requestPermission` already **denies**; `runTurn` **throws** on `host_acp` until this order. Hermes will request approvals on tool use ([Hermes ACP](https://hermes-agent.nousresearch.com/docs/user-guide/features/acp) — allow once / always / deny; timeout → deny). Without a bridge, a live turn is either stuck or silently cancelled.

## Context pack

1. `docs/orders/WO-008c.md` verification records (host client, launch routing, deny-by-default)
2. `species/hermes/host-acp-client.ts` — sole deny/spawn/`ClientSideConnection` impl
3. `collab-electron/src/main/agent-host.ts` — `admitAndStartSession` / `runTurn` (host_acp branch)
4. ROADMAP §WO-008a — per-species tool allowlist; founder live turn; extract-first on shell JS

## Deliverables

**0. Permission bridge (host).**
Replace handshake-only deny with a real ACP permission path: pause for founder decision on dangerous calls; map outcomes to Hermes options (`allow_once` / `allow_always` / deny). Default remains **deny** on timeout / missing UI / cancel. Do **not** copy `acp-agent.ts` auto-approve. Prefer extending `host-acp-client.ts` (or a thin sibling) so smokes and Electron share one client.

**1. `runTurn` for `host_acp`.**
Lift the WO-008c forbid. Prompt → stream chunks into the session tile (same IPC/events shape as AgentOS turns where possible). Cancel mid-turn tears down cleanly (no orphan hermes pids — reuse WO-008c orphan check).

**2. Per-species tool policy.**
Enforce an allowlist at the host seam (species data / `agent_definition` — **stop and report** if a schema column is truly required). Falsify: unlisted tool via critic-mock or a test species → denied; listed harmless tool → may proceed under the permission bridge rules.

**3. Founder live path (documented, not a secret).**
Register Hermes → dock Spawn → **Run turn** with a short harmless prompt the founder chooses. Paste: session id, that chunks arrived, permission UI evidence (grant or deny once). No credentials in logs.

**4. Gate hygiene (carry-forward from WO-008c verify).**
`kernel-sole-writer-app` allowlists `host-acp-bridge.ts` but the SDK import lives in `species/hermes/host-acp-client.ts` (outside the gate walk). Either: allowlist + scan that path, **or** move the shared client under a package/`collab-electron` home the gate already covers. Neuter → red → restore → green.

## Contract

- Deny-by-default. No auto-approve. No credentials in repo/fixtures/logs.
- Do not weaken AgentOS `agent-path` falsifications.
- Extract-first: `renderer.js` / `tile-manager.js` must not grow; keep `agent-host.ts` under 1k (extract if needed).
- Launch routing from WO-008c stays deploy-true (`launch.json` + packed `*.meta.json`). Optional cleanup: drop source-manifest fallback if it no longer earns its keep — not required if behavior is unchanged.
- Standing static gates green; `cd collab-electron && bun run build` exit 0.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).
2. Sole-writer ACP perimeter fixed (deliverable 4) — falsify red/green pasted.
3. Host Hermes **Run turn**: chunks > 0; orphan check after cancel/close.
4. Permission: at least one deny path and one grant path exercised (or documented UI + automated deny-timeout).
5. Tool allowlist falsify (unlisted → denied).
6. `cd collab-electron && bun run build` — exit 0.
7. Line counts: `renderer.js` / `tile-manager.js` / `agent-host.ts` before/after.

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Out of scope

A2A · removing debt #14 legacy `acp-agent.ts` (may land here only if spawn registration can die cleanly without scope creep) · putting Hermes back inside AgentOS guest exec · schema churn unless deliverable 2 forces a stop-and-report.

## Report back

1. Gates 1–7, unedited.
2. Where permission + allowlist enforcement live (one path each).
3. Three decisions the order didn't specify, one line each.
4. Anything ambiguous or unbuildable — stop and say so.

---

## Verification — round 1 (2026-07-20) · PASS

**Branch `wo-008a` @ `8ef69e3`, cold `/tmp/verify-008a`: 12/12 gates, exit 0. Build exit 0. Line counts: renderer 1746, tile-manager 969, agent-host 654→683.**

**Science re-run:**
- `host-acp-policy.test.ts` — 5/5 (unlisted deny, listed may grant, empty deny-all).
- `host-turn-smoke.ts` — session minted, `chunks=2`, text=`pong`, orphan check OK (Hermes ACP prompt path).
- Sole-writer ACP perimeter: scans `species/hermes/host-acp-client.ts` + policy; bridge must not import SDK. Falsify: bait `@agentclientprotocol` in `species-tools.ts` → FAIL; restore → OK.
- AgentOS `agent-path` still green. Deny-by-default on timeout / missing UI; allowlist before founder prompt.

**Thermo (structure PASS):** allowlist + permission in `Client.requestPermission` (model cannot bypass via host API); `runHostAcpTurn` extract keeps dual-path growth contained; shared client still shared. Legacy `acp-agent.ts` auto-approve correctly left as debt #14.

**Accepted decisions:** committed `tools-allowlist.json` (no schema column); host turns skip `publish_artifact`; permission timeout single-waiter on Electron bridge only. Order mapped `allow_once` / `allow_always` / deny (Hermes `allow_session` optional later).

**Carry-forward (not blockers):**
- `host-turn-smoke` can exit 0 with zero chunks — harden to require `chunks > 0` on next Hermes touch.
- Session-tile single permission slot — queue concurrent requests when multi-tool turns hurt.
- Shared session terminalizer AgentOS vs `host_acp` (lifecycle copy drift).
- Sole-writer still not hermes-wide for a *new* SDK home file under `species/hermes/`.

**Rotation:** merge · WO-008a → done · NEXT → **WO-009** (datasets bootstrap; order written this commit). WO-012 unblocked, order not yet written.
