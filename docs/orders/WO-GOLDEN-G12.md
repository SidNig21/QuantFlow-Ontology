# WO-GOLDEN-G12 — Windows package and operations hardening

This final Phase-2 group makes the current Windows product packageable,
installable, update-target-correct, relaunchable, and cleanly stoppable without
orphaning a gate-owned process or run-owned root.

status: PREPARED — semantic Reader YES / YES required before Builder
kind: Golden Baseline Phase 2 final surgical group
owner: Router
depends: G11 independently accepted at `1f81c469371fbb4db3e4e8bdac1248f0a0d3d51c`
build-authority: **CLOSED — no Builder until one fresh Reader returns YES / YES**
phase-3: **CLOSED / SEPARATE — G12 does not perform or claim clean-source full-repository requalification**
r18: **FROZEN — no R18 acquisition, domain expansion, or first-use work**

## Frozen identity and inherited reds

| Item | Exact value |
|---|---|
| G12 starting commit | `1f81c469371fbb4db3e4e8bdac1248f0a0d3d51c` |
| G12 starting tree | `921b5e90ea91c914b4027f85d1d50d8ae62dde63` |
| G11 Verifier task | `01a050fc-c682-7333-8079-c021c8c0743c` |
| Starting checkout | clean native-Windows worktree |
| Reusable unchanged product bundle | SHA256 `3006C94B2B7638B295F09CACF2BE5CD4F70831230BCE203DAABCE71B14BCD681` until a product/config byte changes |
| G12 inherited operations red | packaged Hermes cleanup reaches Windows `EACCES`, leaves a `pty-sidecar`/run root, and reports `roots_remaining=1` with a leaked root |
| G12 inherited domain red | `qf-bovada-football` Windows `fsync EPERM`; assertions must execute on supported Windows filesystem behavior |

The preserved G5 receipts name roots `x0W0CL` and `Fz8BQs`. They are evidence,
not cleanup targets. G12 uses new run-scoped isolated roots and never deletes an
unattributed pre-existing root.

## Exact scope

The literal scope is
[`G12-SURFACE-MANIFEST.tsv`](evidence/golden-baseline/g12/G12-SURFACE-MANIFEST.tsv).
`ALLOW_IF_CAUSAL` is not blanket edit permission: a changed path must be the
shortest causal repair for a frozen red and must have a paired falsifier.

G12 owns these current-product boundaries:

1. frozen Bun install and Windows NSIS package creation;
2. packaged resource closure and runtime staging, including Hermes profiles,
   schema upgrades, native PTY payload, and update metadata;
3. unpacked and installed application readiness, clean shutdown, same-state
   relaunch, and current update-target inspection;
4. Electron → PTY → sidecar process ownership, awaited teardown, PID/socket
   breadcrumb cleanup, and exact run-root attribution;
5. `qf-bovada-football` durable artifact behavior on supported Windows
   filesystems, with the real domain assertions reached;
6. the canonical native-Windows release stage list where it directly composes
   these boundaries.

It does not own Kernel/Ontology semantics, Canvas behavior, research meaning,
new product capability, Linux/macOS qualification, publishing a release,
credentials, network update download/install, signing, R18, or Phase 3.
Update acceptance is deterministic local inspection/unit behavior and emitted
metadata targeting `SidNig21/QuantFlow-Ontology`; no live GitHub mutation or
download is authorized.

## Starting matrix and Fast Mode

Before mutation the Builder copies
[`G12-STARTING-MATRIX.tsv`](evidence/golden-baseline/g12/G12-STARTING-MATRIX.tsv)
into a generated run receipt with command, exit, duration, full-output SHA256,
run ID, isolated roots, owned PIDs, and exact failure stage. No row may be
silently omitted. Known reds are evidence, not permission to broaden scope.

Golden Fast Mode order:

1. freeze S01–S14 once, changed-surface tests first, then the focused G12 gate;
2. reuse exact-SHA receipts and the product bundle only while relevant bytes
   are identical; rebuild once after a material product/config change;
3. run one complete package/install/current-product lifecycle after focused
   green, because G12 materially owns that boundary;
4. freeze one immutable candidate and stop for one independent Verifier.

`bun qa/verify-release.ts` must not conceal an owned failure behind an earlier
wrapper/preflight success. The Bovada assertions, packaged readiness, shutdown,
relaunch, and cleanup-zero checks must be observed directly.

## Deliverables and acceptance assertions

1. One registered `golden-g12-package-operations` gate orchestrates focused
   proof with unique IDs and isolated Kernel, artifact, user-data, install, and
   temp roots. It emits a machine-readable receipt and never touches ambient
   founder state.
2. Cold frozen install succeeds without an unbounded retry or ambient cache
   dependency; package inspection proves every required runtime resource and
   the QuantFlow update target.
3. The unpacked package and the installed NSIS artifact each reach explicit
   canvas/Dock readiness, use the isolated Kernel, shut down through
   `app.shutdown`, exit zero, remove endpoint/PID breadcrumbs, and leave zero
   gate-owned processes.
4. Relaunching the installed artifact against the same isolated state reaches
   readiness and read-only agreement, then shuts down cleanly. This is package
   operations proof, not Phase-3 founder traversal.
5. A real packaged Hermes lifecycle reaches its current-product assertions and
   finishes with `processes=0 roots_remaining=0 leaked=[]`. Cleanup is awaited,
   bounded, run-attributed, and safe under Windows handle-release timing.
6. Bovada's real artifact/domain assertions execute and pass on Windows. An
   `fsync EPERM` wrapper skip, swallowed error, or success before assertions is
   RED.
7. `bun qa/verify-release.ts` passes natively on Windows with the exact owned
   stages visible. No Linux compatibility result substitutes for it.
8. Product/config changes remain inside the manifest; accepted G1–G11 evidence,
   Kernel/Canvas semantics, Phase 3, and R18 stay byte-unchanged.

## Fail-capable falsifiers

Each is bait → RED → restore → GREEN in an isolated copy or run. The Builder
must retain assertion meaning.

| ID | Bait | Required RED |
|---|---|---|
| F01 | remove one staged Hermes profile or schema upgrade | package inspection/readiness fails before launch acceptance |
| F02 | rewrite packaged update owner/repo to the legacy upstream target | exact update-target mismatch |
| F03 | leave one gate-owned child alive after `app.shutdown` | owned PID and launch-tree receipt; cleanup nonzero |
| F04 | retain the PTY sidecar PID/socket breadcrumb or one run root | `processes/roots/leaked` assertion fails |
| F05 | relaunch with a different Kernel/root identity | same-state/read-only agreement fails |
| F06 | restore the Windows Bovada `fsync EPERM` path or skip its domain assertion | gate fails and proves assertion was not reached |
| F07 | make installer/unpacked readiness return before canvas/Dock readiness | lifecycle gate fails closed |
| F08 | omit an owned stage from the canonical release route | release composition test fails |
| F09 | point cleanup at a pre-existing/unattributed root | safety refusal; no deletion occurs |
| F10 | mutate a protected Phase-3/R18/Kernel/Canvas path | scope gate fails |

## Cleanup, rollback, and evidence ordering

All destructive cleanup resolves exact absolute run-owned paths beneath the
run's isolated root, proves attribution before removal, and refuses ambient or
pre-existing paths. Failure preserves the receipt and returns RED; it never
claims cleanup zero.

Rollback is one normal inverse commit from the immutable candidate to the G12
starting identity. Generated package artifacts and run roots are excluded from
Git and removed only by the same attribution-safe cleanup. No reset, rebase,
force-push, main mutation, dependency addition, or release publication is
authorized.

Atlas ordering is precomputed: run read-only Atlas checks at the clean starting
tree; after the candidate code/proof commit, regenerate `bun qf-atlas/generate.mjs`
only if the tracked map changes; commit any generated Atlas delta before final
verification; then run clean-tree Atlas verification. Evidence that embeds the
candidate SHA is written only after the candidate exists and, if committed,
forms an evidence-only head whose product/config identity is explicitly
proved. One immutable product candidate—not a chain of repaired candidates—is
offered to one independent Verifier.

## Reader brief

The fresh Reader reads `START_HERE.md`, `PROTOCOL.md`, `ADR-0004`,
`GOLDEN-RUN.md`, `NEXT.md`, this order, both G12 manifests, the G11 independent
closure, and the cited G5 inherited-red receipts. It answers exactly:

1. Does this packet give one bounded G12 meaning with exact current Windows
   package/install/update/runtime-stage/process-cleanup surfaces, fail-capable
   assertions, and no Phase-3 or R18 absorption?
2. Is the starting identity/matrix, causal allowlist, Fast Mode lifecycle,
   cleanup safety, rollback, Atlas ordering, immutable candidate, and
   independent-Verifier boundary sufficient to open one G12 Builder?

Required verdict: **YES / YES**. Any NO or ambiguity keeps Builder authority
closed and returns the finite defect list to the Router.

