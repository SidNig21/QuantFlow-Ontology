# WO-008d — Hermes tile = real TUI (standing: native TUI forever)
status: done — verified + merged 2026-07-20
assignee: builder
depends: WO-008a (merged — host Hermes exists; ACP session tile is the wrong UX)
created: 2026-07-20

## Objective

Dock **Spawn Hermes** opens a canvas **terminal tile running Hermes’s real TUI** (`hermes --tui`) — not the ACP session-tile / “Run turn” chrome. Founder sees Hermes as Hermes. Record a standing rule: **interactive agents with a native TUI always surface that TUI in a term tile; QuantFlow never fakes their chat UI.**

## Why this order exists (founder)

WO-007→008a proved host plumbing (Kernel session, host process, permissions). The founder’s acceptance bar is different: **the tile must be a clear mirror of Hermes’s TUI.** ACP host bridge remains useful substrate; it is **not** the Hermes desk UX.

Hermes TUI docs: [TUI](https://hermes-agent.nousresearch.com/docs/user-guide/tui) — launch with `hermes --tui`.

## Context pack

1. `collab-electron/src/main/pty.ts` + `pty:create` IPC — existing term tiles (xterm + node-pty)
2. Dock spawn path: `qf:sessions:spawn` → `admitAndStartSession` (today opens session tile)
3. `species/hermes/launch.json` + host binary resolve (`HOST_ACP_BIN` / `HERMES_BIN` in mounts)
4. This order’s standing rule (also lands in ROADMAP / BLUEPRINT this rotation)

## Deliverables

**0. Measure PTY spawn of a non-shell command.**
Can `pty:create` / `createSession` launch `HERMES_BIN --tui` (cwd + env) today, or only an interactive shell? Paste the measured API. If only shell: add the **smallest** extension (command + args + env from host/species data — never from renderer free text) so a term tile can run Hermes TUI. Prefer reusing term tile machinery over a new tile type.

**1. Dock Spawn Hermes → TUI term tile.**
`Spawn` on Hermes:
- Still creates/adopts a Kernel `agent_session` (lifecycle: created/started; cancel/close kills the PTY child — no orphan hermes).
- Opens a **term** tile whose process is the Hermes TUI (measured argv, e.g. `hermes --tui`), not the ACP session-tile Run-turn UI.
- Screenshot in report: tile shows Hermes TUI chrome (banner / composer), not QuantFlow “Run turn.”

**2. Standing surface rule (data-driven).**
Species that ship a native TUI declare it once (e.g. `surface: "native_tui"` + argv template next to `launch.json` / meta — pick smallest shape; **stop and report** if you need a schema column). Dock Spawn for those species always takes the term-tile path. AgentOS / ACP session tiles remain for guests that are ACP-only (toolloop, critic-mock). No `if (species === "hermes")` scattered in the renderer — one host resolve path.

**3. Do not delete host ACP.**
Keep `host_acp` client / allowlist / permission modules. They may stay unused on the Hermes Spawn UX path this order, or power a secondary/debug path — but the **default founder path is TUI**. Document one sentence in `species/hermes/README.md`.

**4. Hygiene.**
Extract-first: `renderer.js` / `tile-manager.js` must not grow. `agent-host.ts` stays under 1k. Static gates green; build exit 0. Orphan check: close tile / cancel session → zero leftover hermes pids.

## Contract

- **Acceptance is visual + process:** founder sees real Hermes TUI; Kernel row exists; orphans zero.
- No credentials in repo/logs. Paths only.
- Do not weaken AgentOS `agent-path` for toolloop.
- Do not invent a second “fake Hermes chat” React UI.
- Standing rule applies to **all future** native-TUI species, not Hermes-only hacks.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).
2. Deliverable 0 measurement (PTY can / cannot spawn command today + what you added).
3. Dock Spawn Hermes → term tile with Hermes TUI visible (screenshot).
4. Kernel `agent_session` for that spawn (`created`/`started` or current legal pair).
5. Cancel/close → orphan check OK.
6. `cd collab-electron && bun run build` — exit 0.
7. Line counts: `renderer.js` / `tile-manager.js` / `agent-host.ts` before/after.

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Out of scope

Datasets (**WO-009** parked) · rewriting Hermes itself · A2A · removing ACP host bridge · AgentOS guest TUI (WASM wall still holds).

## Report back

1. Gates 1–7, unedited.
2. Exact argv + env used to launch the TUI (paths only).
3. Where `surface: native_tui` (or equivalent) lives — one path.
4. Three decisions the order didn't specify, one line each.

---

## Verification — round 1 (2026-07-20) · PASS

**Branch `wo-008d` @ `2f32e6d`, cold `/tmp/verify-008d`: 12/12 gates, exit 0. Build exit 0. Line counts: renderer 1746→1745, tile-manager 969→969, agent-host 683→763.**

**Science re-run:**
- `bun ./tui-pty-smoke.ts` — exit 0; orphan check OK; `looksLikeTui=true`; independent PTY capture rewritten (~1023-byte text / ~2104 raw) showing Hermes TUI chrome (`Nous Rese…`, `Session:`, `Available Tools`, `/help for commands`).
- Packed meta deploy-true: `"surface":"native_tui","argv":["--tui"]`.
- Public `pty:create` / `createSession` remains shell-only (`resolveTerminalTarget`); host-only `createHostCommandSession` not on renderer IPC.
- Dock spawn → `create-term-tile` when `info.surface === "native_tui"` + pre-created `ptySessionId` (`ipc-kernel.ts`).
- No `hermes` string in `renderer.js` — surface resolve is data-driven (`species-surface.ts` → `admitNativeTuiSpecies`).
- AgentOS `agent-path` still green among the 12.

**Thermo (structure PASS):** extract `canvas-place.js` kept renderer from growing; native TUI admit extracted to `host-native-tui.ts`; Kernel UUID vs tile `ptySessionId` split is clear; PTY-exit → Kernel close with cancel clearing the map first avoids double-close.

**Accepted decisions:** host UUID Kernel id; PTY id on term tile; PTY exit auto-closes Kernel row. **Visual gate:** dock GUI screenshot blocked in builder/verifier environments; PTY capture + smoke substitute accepted — **founder: one Spawn in the desk confirms the term tile face.**

**Carry-forward (not blockers):**
- Default `argv: ["--tui"]` when `native_tui` omits argv is Hermes-shaped — fine until a second native-TUI species needs a different default.
- `launch: host_acp` still in meta while desk surface is `native_tui` — surface wins; leave ACP as substrate (order deliverable 3).
- `looksLikeTui` heuristic is weak (substring `hermes`); capture content is the real proof — tighten smoke if a second TUI lands.

**Rotation:** merge · WO-008d → done · NEXT → **WO-009** (unpark datasets bootstrap).
