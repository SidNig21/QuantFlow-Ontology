# WO-008b — Hermes reachability: bundle vs authorized mount
status: done — PROBE HELD 2026-07-20; in-guest Outcome A unbuildable (WASM-only exec); scope → WO-008c
assignee: builder
depends: WO-007b (merged); WO-008 Outcome B (guest cannot see host Hermes install)
created: 2026-07-20

## Objective

Make Hermes reachable from the AgentOS guest so deliverable-0 can claim **Outcome A** (ACP handshake) — without prompting Hermes and without marrying the host to one vendor forever. Probe the SDK's typed mount surface first; only then implement the winning path (authorized mount **or** self-contained bundle).

## Context pack

1. `docs/orders/WO-008.md` — Outcome B evidence: shim stderr `hermes-acp-shim: HERMES_BIN not found:` while host binary exists; positive classifier in `species/hermes/d0-smoke.ts`
2. `species/hermes/` — shim reads caller/manifest `HERMES_BIN` + `HOME` only; no PATH lookup
3. WO-007b — `admitAndStartSession` (no prompt) · manifest `agent.env` is the per-species env channel · dock spawn never prompts
4. Typed AgentOS options (`@rivet-dev/agentos-core` `AgentOsOptions`): `mounts?: MountConfig[]`, `rootFilesystem?` — **not** a public `hostMounts` knob (private `_hostMounts` exists; do not invent APIs). Measure `mounts` / native mount plugins from the installed SDK docs + d.ts.

## Deliverables

**0. Probe first — mount vs bundle (stop-or-proceed).**
Throwaway harness under `/tmp` (or `species/hermes/` scratch that is **not** the admitting commit): try to expose a known host path into the guest via `AgentOs.create({ mounts: … })` (exact shape from the typed surface you measure). Guest must prove visibility (read a marker file or `existsSync` on the mounted Hermes binary path). Paste the exact create options (paths only, never secrets) and the guest evidence.

- **If mount works and is narrowly scoped** (read-only path to Hermes install — not the whole home by default): proceed with **authorized mount** for deliverables 1–2. Document security notes (what is visible; deny broader mounts).
- **If mount fails or is unusably broad/unsafe:** stop after the probe write-up; implement **bundle** path instead (deliverable 1b) — architect accepts either on evidence, not preference.

Do **not** send a prompt to Hermes in the probe. Handshake-only / file-existence proofs only.

**1a. Authorized-mount path (only if D0 chose mount).**
Wire the measured mount into the **host** AgentOS create site (`agent-host.ts` `AgentOs.create`) as a **generic**, data-driven capability — not a Hermes hardcode. Prefer: mount specs sourced from species data / a small allowlisted host config the founder controls; renderer must not supply mount paths. Then set Hermes `HERMES_BIN`/`HOME` (manifest `agent.env` and/or host opts.env) so the existing shim sees the binary **inside the guest**. Re-run `species/hermes/d0-smoke.ts` → **Outcome A** (exit 0), paste log.

**1b. Bundle path (only if D0 chose bundle).**
Package a self-contained Hermes (or documented founder install step that lands binaries inside the package tree the guest already sees). Update `species/hermes/` accordingly. Re-run d0-smoke → **Outcome A**. No credentials in the package.

**2. Dock admit (handshake only).**
`register.ts --db <path>` → dock lists Hermes → **Spawn from dock** (now legal: admit-only, no prompt) → Kernel shows `created`+`started`, tile opens, **no chunks until** an explicit turn. Do **not** click Run turn on Hermes — that waits for **WO-008a** (permissions). Paste dock screenshot + session id + event types.

**3. Contract gates stay green.** Extract-first still binds if you touch `renderer.js` / `tile-manager.js` — they may not grow.

## Contract

- No prompt / live turn to Hermes (WO-008a's lane).
- No credentials in repo, fixtures, or logs. Paths only in env/mounts.
- Do not weaken `agent-path` falsifications. No schema/Kernel changes unless a mount registry truly requires it — prefer host + species; if you need schema, **stop and report**.
- Prefer generic mount/env plumbing over `if (species === "hermes")`.
- Standing gates green; `cd collab-electron && bun run build` exit 0.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).
2. Deliverable 0 probe log (mount success **or** failure + reason) and the chosen path (1a or 1b).
3. `bun ./d0-smoke.ts` under `species/hermes` → **Outcome A**, exit 0 (full log).
4. Dock admit evidence (spawn without Run turn).
5. `cd collab-electron && bun run build` — exit 0.
6. If host files changed: line counts for `renderer.js` / `tile-manager.js` before/after (must not grow).

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Out of scope

Permission bridge / tool allowlists (**WO-008a**) · A2A · rewriting the dock contract · expanding guest visibility to the entire founder home “for convenience.”

## Report back

1. Gates 1–6, unedited.
2. Chosen path (mount / bundle) in one line, with the D0 evidence pointer.
3. Three decisions the order didn't specify, one line each.
4. Anything ambiguous or unbuildable — stop and say so.

---

## Verification — round 1 (2026-07-20) · PROBE HELD (scope change)

**Branch `wo-008b` @ `89e39a2`, cold `/tmp/verify-008b`: 12/12 gates, exit 0.** Mount path **works** (narrow RO mounts; marker + Hermes tree visible; `auth.json` not). Guest stdin had no `.pipe()` — shim bridged. **Outcome A blocked:** guest exec is WASM-only — `#!` Hermes shim and ELF python both refused (`ERR_NATIVE_BINARY_NOT_SUPPORTED`). Bundle cannot bypass that. d0-smoke with mounts → UNKNOWN (exit 3), not B; without mounts still B.

**Accepted:** founder mounts file + generic `host-mounts.ts` plumbing (kept for data mounts). **Not accepted as complete:** in-guest Hermes handshake / dock admit of Hermes via AgentOS `createSession`.

**Architect decision:** Hermes becomes a **host-bridged ACP** species (**WO-008c**). AgentOS stays for Node/WASM guests (toolloop, critic-mock, optional future Pi-in-AgentOS). Dock UX unchanged: Spawn → session tile.
