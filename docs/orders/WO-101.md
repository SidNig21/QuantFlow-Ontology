# WO-101 — Research + Agent plane charter (doctrine P1, rung 1 of 2)

> **Status:** open · **Branch:** `wo-101` · **Depends:** none (P0 banked) · **Ladder:** `docs/ROADMAP.md` forward ladder · **Why:** `docs/DOCTRINE.md` Part IV + amendments A1/A3
> Builder reads `START_HERE.md`, then this file, end to end, before any edit. Standing rules inherit: cold-state, gate falsification (bait → red → restore → green, both outputs in the report), no credentials, commit from a worktree, `PROTOCOL.md` roles.

## Context (measured, 2026-07-24)

`qf-kernel-schema/src/schema.ts` holds 19 objects · 13 links · 27 actions in one 650+ line file with plane comment headers. Define-time description lint exists for objects (`define.test.ts`). `run.kind` = `ingestion|feature_build|backtest|analysis`. There is **no `report` type** despite the README naming it in the Research plane. Conformance tests are generated from transition tables (118 at last count). The charter is this package **evolved in place** — never a parallel `ontology/` module (doctrine A1; a second truth store fails `START_HERE.md` §5.2 on sight).

## Deliverables (one meaning each)

**1 · Split by plane, zero behavior change.** Move `defineObject/defineLink/defineAction` blocks into `src/ontology/research.ts`, `src/ontology/market.ts`, `src/ontology/agent.ts` (links/actions live with their owning plane; cross-plane links live where their `from` side lives). `schema.ts` becomes a pure aggregator re-exporting the same names — **every existing import and the codegen path work unchanged**. Retires ROADMAP debt #5. Prove zero-change: full test suite green before and after the move with **identical** golden output (paste the diff command showing empty).

**2 · `report` object + the mechanical gate.** Add `report` (`experimental`) to the research plane: the publishable synthesis, `DERIVED_FROM artifact`, gated by evaluation. Add `publish_report` to the command layer with the rule: **rejects with a typed error unless a linked `evaluation` row in a passing state exists**. Read the current `evaluation` type and state its passing predicate explicitly in your report (one sentence); wire the check at the command layer as WO-006a did for artifact conflicts. Tests: reject-without-evaluation, reject-with-failing-evaluation, accept-with-passing — plus bait (disable the check → red → restore → green).

**3 · Descriptions become agent context (research + agent planes only; market plane waits for WO-102).** Rewrite every object and property description in those two planes to answer: *what must an agent know to act on this correctly?* Style: first sentence = what it is; second = the rule that governs it (see `run`'s current description — "Never clone types per pipeline step" — that is the register). No description may be restatable from the field name alone ("The name of the workspace" is a defect).

**4 · Seed the RL names (doctrine A3 — names only).** `policy` and `environment` as `experimental` objects with descriptions and minimal properties, linked (`policy PRODUCED_BY run`, `environment BINDS market-plane` — exact link naming is yours to propose), **zero actions, zero implementation**. `run.kind` gains `"training"`. `trajectory` must NOT appear as a type — it is `artifact.kind` territory (Silo rule).

**5 · `mission`, decided.** The doctrine charter names `Mission` (standing research intent, `CONTAINS hypothesis`). `workspace` (spatial canvas container) is a *different concept*. Recommendation: add `mission` as its own experimental type and leave `workspace` alone; if you disagree, argue it in one paragraph in the report and implement your argument.

**6 · The anti-pattern lints (doctrine Part VI as code).** Extend the existing lint layer with, each bait-tested:
   - **Silo lint:** no object type name may embed another type's kind-enum value (`backtest_run` must fail).
   - **Active-freeze lint:** removing or retyping a property on a `lifecycle: "active"` type fails against a committed baseline snapshot (`schema-baseline.json`, regenerated only by an explicit script — that script IS the governed change ritual). All types are `experimental` today, so prove it with a test fixture, not a live type.
   - **Property-description lint:** every property carries a non-empty `.describe()` (verify whether this already exists; if it does, cite the test and move on).

## Gates

**G1 · Lint falsification (builder-run, CI):** three sabotages each go red — a missing description, a `backtest_run` clone, a property removed from an `active` fixture type. Bait transcripts in the report.

**G2 · The fixture protocol (verifier/founder-run, evidence in report):** ship `qa/fixtures/charter-101.md` containing (a) hand-written fixture rows for hypothesis → dataset → run → artifact → evaluation → report loaded through commands (loading green in CI), and (b) the question protocol: a cold agent is given **only** the three plane files and asked *"Which runs from hypothesis X produced evaluations above threshold Y, and which report did they gate?"* A wrong answer is a **description defect** — fix the description, not the protocol. This gate is run by the verifier with a live seat, not by CI.

**G3 · Suite:** full conformance suite regenerated and green, cold (`bun install --frozen-lockfile` from clean worktree); count before → after stated (must grow — new types generate new tests).

## Evidence required in the report

Zero-change diff proof for deliverable 1 · passing-predicate sentence for deliverable 2 · three bait transcripts (G1) · fixture file path + loading output (G2a) · conformance counts (G3) · one paragraph on any deliverable where you exercised judgment.

## Out of scope (hard)

Market-plane types (WO-102) · any codegen/tool emission (P2) · any `collab-electron` change · any new package or dependency · `workspace` rename.
