# WO-102 — Market plane reframe: betting becomes rows, not types (doctrine P1, rung 2)

> **Status:** open · **Branch:** `wo-102` · **Depends:** WO-101 (done, merged `d4356cc`)
> **Ladder:** `docs/orders/SCOPES.md` · **Scope shape:** schema package only.
> **Zero `packages/qf-kernel` changes. Zero `collab-electron` changes.**
> Builder reads `AGENTS.md`, then `START_HERE.md`, then this file end to end before any edit.
> Standing rules inherit: cold-state, gate falsification (bait → red → restore → green, both
> outputs in the report), no credentials, commit from a worktree, `PROTOCOL.md` roles.

## In plain terms

Right now the part of the system that describes betting markets is written in the language of one
sport — competitors, events, odds. This order rewrites it in neutral language, adds a home for the
sportsbook itself, and teaches the system that a betting slip can arrive from a human who already
placed it, not only from a strategy that proposed one. Then it proves the whole thing by
representing a real single and a real five-leg parlay — void leg and all.

**If it goes wrong:** the system can't hold the bets you actually place, and you find out at the
point where it was supposed to start being useful.

## Founder direction binding this order (doctrine A7, stated 2026-07-25)

**Bovada sportsbook only.** Crypto/HyperLiquid is explicitly out — not later in this order, not as
a fixture, not as a comment. Founder, restated 2026-07-25: *"lets not get confused and only focus
on bovada sportsbook."*

**Restated again, sharper, same day:** *single bets and parlays.* Not futures, not outrights, not
season-long anything. Those are the two shapes the founder places and the only two this order
models.

This has a real consequence you must not paper over: **the market plane's abstraction claim goes
untested.** Read "The abstraction claim is untested, deliberately" below before you report
anything about these type names. G3 is now a *representability* gate, not a falsification one.

## Context (measured 2026-07-25 against `main` at `d4356cc` — cite these, do not re-estimate)

| Fact | Value | Where |
|---|---|---|
| Market plane today | `competitor` · `event` · `market` · `odds_series` · `result` | `src/ontology/market.ts` |
| Market links | `participates_in` · `offered_on` · `quotes` · `settles` | same, `:102`–`:133` |
| Market actions | `start_event` · `settle_event` · `void_event` | same, `:134`–`:160` |
| **`event` is stateful** | `scheduled → live \| void`, `live → settled` | `src/transitions.ts` |
| `event` command edges | **3**, in `commands.ts:56-58` | `commands.ts` |
| Conformance lines naming `event` | **33** | `golden/conformance.test.ts` |
| Objects · links · actions | **22 · 13 · 27** | `src/schema.ts` |
| Suite | **143 tests, 3 files** | `cd qf-kernel-schema && bun test` |
| `pipelineFed` occurrences in code | **0** | repo-wide grep |
| `venue` occurrences as a type | **0** — Bovada has nowhere to live | repo-wide grep |

**This rung is larger than it reads.** `event` is not a schema-only rename: it moves a transition
table, three command edges, and 33 generated conformance lines. Budget accordingly.

## Ritual, stated up front — this order invalidates every existing `kernel.db`

The generated migration is bare `CREATE TABLE` applied only to fresh databases. There is **no
`ALTER` story and no migration runner.** Renaming market tables means an older database no longer
matches the schema and is recreated empty on next open.

**Founder consent obtained 2026-07-25.** Measured before asking: the two live databases
(`~/.collaborator/dev/…/kernel.db`, `~/.qf-peer-bus/kernel.db`) hold agent-session lifecycle
history and trajectory artifacts from the July 20 peer-bus work — **zero market rows** — so this
rename does not touch a single row in either. Nothing is deleted *by* this order; recreation
happens on next open.

**Builder duty:** do not delete any database. Confirm in your report that a **fresh** `kernel.db`
opens clean against the regenerated migration. That is the only database claim this order makes.

## The source of truth for this modelling — real Bovada slips

The founder supplied four real settled slips on 2026-07-25. **They are not in this repo and must
not be** — the repo is public and slips carry reference numbers and amounts. What follows is the
de-identified structure, which is all the schema needs. A synthetic example stands in:

```
Venue: Bovada
Ticket "2 Team Parlay", graded WIN, placed <timestamp>, external ref <opaque string>
  RISK <amount> · COMBINED ODDS <the figure that actually pays> · WINNINGS <amount>
  Leg 1  bout "Fighter A vs Fighter B" (starts <timestamp>)
         selection "Fighter A Wins by KO, TKO or DQ"   market type "(Bout) Method of Victory"
         price at selection <american odds>            leg outcome WON
  Leg 2  bout "Fighter C vs Fighter D" (starts <timestamp>)
         selection "Fighter D Wins Inside Distance"    market type "(Bout) Alternate Method of Victory"
         price at selection <american odds>            leg outcome WON
```

Four structural facts measured from the real slips, each load-bearing below:

1. **The same bout carries many distinct selections.** Two slips bet the same fight, placed the
   same minute, on different selections at different prices under different market types. So the
   bout and the betable selection are **different objects** — the bout is the occurrence, the
   selection is the instrument.
2. **Market type is a property, never a type.** Three distinct categories appeared across four
   slips — method of victory, alternate method of victory, straight fight winner. A type per
   market category is the Silo anti-pattern; these are values in a property enum.
2a. **The combined price is the one that pays; per-leg prices are inputs to it.** Founder,
   2026-07-25. Both must be stored: the ticket's `combined_price` for realized return, and each
   leg's price at selection for CLV and for attributing which leg was mispriced. Storing only one
   loses a question the evaluation loop must answer. The current type has `combined_price`; the
   per-leg prices survive only inside the `legs` blob (see finding 1).
3. **A leg carries its own price and its own outcome.** One slip contains a **void** leg inside a
   losing ticket; another contains a leg with **no outcome at all** — never graded, because the
   parlay was already dead. A leg's state space is *not* the ticket's state space.
4. **A slip has an external identity** — the sportsbook's own reference string — which is what
   makes re-importing the same slip idempotent rather than duplicating it.

## Deliverables

### 1 · Rename the market plane to market-agnostic vocabulary

| Today | Becomes | What it is, from the slips |
|---|---|---|
| `event` | **`market_event`** | The bounded occurrence a market resolves against — a bout, a game, a match. **Keeps its state machine and all three commands.** |
| `market` | **`instrument`** | The betable thing: one selection under one market type — a method-of-victory pick, a moneyline. **Must not hard-depend on `market_event`** (see the abstraction note); today every instrument has one, and nothing may assume it forever. |
| `odds_series` | **`quote`** | One priced observation at a timestamp: American odds, or bid/ask. |
| — | **`venue`** (new) | Where instruments are listed and priced: a sportsbook, an exchange. **Bovada has no home today.** |
| `competitor` | `competitor` (kept) | A participant in a contest. Justify retention in your report — see below. |
| `result` | `result` (kept) | Settled truth for a `market_event`. Justify retention in your report. |

Links rename with their endpoints: `offered_on` and `quotes` must read correctly against the new
names, and `venue` needs a `lists` link (`venue → instrument`). Keep link count changes minimal
and state the delta.

**`venue` is the one new object type this order may add, and it is not an exception to WO-108's
zero-new-types gate** — that gate governs *adding a second market*, and this is the first.

**On `competitor` and `result`:** the doctrine's charter names four market types and neither of
these is among them. They are kept because a bout genuinely has participants and a settled
outcome, and deleting them would lose information the slips contain. **But you must argue it in
one paragraph.** If your argument is that either is sport-specific and should go, implement your
argument — that is a legitimate outcome of this deliverable, not a deviation from it.

### 2 · Market-plane descriptions to the WO-101 register

Every object and property description in `market.ts` rewritten to: **first sentence what it is;
second the rule that governs it.** WO-101 deliberately left these alone so they would be written
once. `run`'s live description — ending *"Never clone types per pipeline step"* — is the bar. A
description restatable from the field name is a defect.

### 3 · `pipelineFed` — build the mechanism or drop the word

`pipelineFed` has **zero occurrences in the codebase.** It is doctrine vocabulary awaiting
machinery, and shipping the flag without an enforcer would repeat exactly the failure doctrine A5
names: *declaration is not capability.*

**Ruling: build it, with its enforcement, in this order.** A flag with no consumer is not
permitted; neither is dropping the Golden Hammer rule.

- Add `pipelineFed?: boolean` to the object definition surface.
- **`instrument` and `quote` are `pipelineFed: true`.** They are discovered and priced by a feed.
- **`venue`, `competitor`, `market_event` and `result` are not.** Note what this forces you to
  notice: `market_event` has three governed commands, so it *cannot* be pipeline-fed. That is the
  rule doing real work rather than decorating.
- **The enforcer, which must be able to go red:** a lint failing if any `pipelineFed: true` type
  is named by a creation command or by a transition command edge. The machinery exists —
  `lintCommands` already walks both lists.

WO-104 later reads the same flag to emit no write tools. This order gives it teeth today.

### 4 · The two `ticket` defects (doctrine A6)

**4a · The description points away from the founder's primary use case.** It currently reads *"one
proposed wager **emitted by a strategy**."* The founder's first use case is the inverse: a real
slip, already placed, supplied by a human, already graded by reality. An agent handed a real slip
and reading that description has been told this type is not for it. **Rewrite so it covers both
origins as equally primary.**

Add `origin` — an enum of `strategy_proposed | operator_supplied` — with a description that tells
an agent what each implies about trust and provenance. This is the field that makes the two
directions of doctrine A6 (forward product, backward calibration) distinguishable in data.

**4b · Fields the founder's real slips need and the type does not have.** All are property
additions to an `experimental` type, so the active-freeze lint does not object:

- `external_ref` — the venue's own ticket reference. The description must say it is the
  idempotency key for re-importing an operator-supplied slip, so the same slip twice is one row.
- `placed_at` — when the wager was struck. **Without it CLV cannot be computed**, which is the
  headline metric in the doctrine's evaluation loop. WO-101's verification already logged
  "nothing on the proof path carries a timestamp" as a finding; this is its first concrete cost.
- `payout` — realized return, distinct from `stake` and `combined_price`. Nullable while pending.

Fix `stake`'s description: it currently reads *"Simulated stake assigned by the sizing policy"*,
which is false for an operator-supplied slip carrying real money.

**4c · What you may NOT do here.** SCOPES routes the *creation* of an already-settled ticket to
**WO-103**, which owns creation. Do **not** add a transition edge, a command, or a creation path
for arrival-settled tickets. Adding a transition forces a `commands.ts` edge (`lintCommands`
rejects a legal transition with no command) and that is WO-103's call, not yours. Describe the
gap in your report; leave the machinery alone.

## Declared dependencies (do not discover these at commit time)

- **`transitions.ts` and `commands.ts` must both move** when `event` → `market_event`. Three
  command edges and one transition table. This is expected and in scope; it is the one place this
  order legitimately touches `commands.ts`.
- **`golden/` must be regenerated and committed** (`bun run generate`). Expect real movement:
  table renames, +1 table for `venue`, and conformance lines re-emitted under the new type name.
  **State the conformance count before → after and explain the delta**; a rename should move
  names, not counts, unless `venue` or a link change adds edges.
- **`qa/gates/doc-action-surface.ts`** asserts set equality between the documented action surface
  and `schema.actions`. Renaming `start_event` and friends will trip it if the docs are not
  updated in the same commit. **This is the gate working — do not weaken it.**
- **`packages/qf-kernel`** may contain string literals naming renamed types. Check
  `ARTIFACT_KINDS`-style constants and any event-type strings. **If a Kernel change is required,
  stop and report** — it is out of scope and means the order is wrong.

## Gates

**G1 · `pipelineFed` enforcement, falsified (builder-run).** Add a creation command or a
transition edge for `quote` → the lint goes **red** → remove → **green**. Both transcripts in the
report. A `pipelineFed` flag whose lint has only ever been green is decoration.

**G2 · No sport-specific noun survives as a type name (builder-run).** A grep over
`schema.objects` names for the retired vocabulary. **State the exact command and paste its
output.** Note this gate is about *type names*, not property values — "(Bout) Method of Victory"
as an enum value is correct and must survive.

**G3 · The real-slip representability fixture — this is the gate that matters (builder-run).**

Hand-write a fixture, in a test, that represents **the two bet shapes the founder actually
places** — nothing else. Founder direction A7, restated 2026-07-25: *single bets and parlays.*
**No futures, no outrights, no crypto, no second venue.**

- **A single.** Venue Bovada; a bout as `market_event`; a method-of-victory selection as
  `instrument`; an American-odds `quote`; a `ticket` with `kind: "single"`, `origin:
  "operator_supplied"`, an `external_ref`, a `placed_at`, a terminal grade and a `payout`.
- **A five-leg parlay containing a void leg**, modelled on a real observed slip: five bouts, five
  selections, five prices at selection; leg outcomes won / lost / won / won / **void**; ticket
  graded `loss`.

The test asserts both construct through the schema's own definitions, with **zero new object
types**.

> **What this gate is for, stated precisely so nobody mistakes it for something else.** It is
> **not** an abstraction test — see "The abstraction claim is untested" below. It asks a narrower
> and more useful question: *can this schema hold a slip the founder actually placed?*
>
> **Part of the answer is already known to be no, and that is the point.** Per-leg price and
> per-leg outcome have no structural home — `has_leg` is a property-less edge and `legs` is an
> opaque JSON array. So the market-plane side (`venue`, `market_event`, `instrument`, `quote`) must
> represent the parlay **fully and structurally**, and that is what the gate asserts. The
> ticket-side facts that had to be stuffed into the `legs` blob because no structure exists for
> them must be **enumerated exactly** in your report.
>
> That enumeration is the deliverable. It converts "links should probably have properties" into a
> precise, measured brief for **WO-103**, written from the founder's own primary use case rather
> than from doctrine. Do **not** fix it here — WO-103 owns link structure.

**G4 · Fresh database opens clean (builder-run).** Apply the regenerated migration to a **new**
`kernel.db` in a temp directory and confirm it opens without error. Paste the command and output.
Do not touch any existing database.

**G5 · Suite green, cold (verifier-run).** `bun qa/run.ts --all` from a fresh worktree. State the
count before → after and **explain the delta by mechanism** — do not assert that growth proves
anything on its own. *(WO-101's G3 claimed "the suite must grow because new types generate new
rows"; that was false — conformance is generated from `transitions`, so stateless types generate
zero tests. Do not reuse that shape.)*

## Known findings — carry forward, do not fix here

Logged for **WO-103's author**. Recording them is in scope; acting on them is not.

1. **A parlay leg is a first-class fact the schema cannot express.** Measured from real slips: each
   leg carries its own price at selection *and* its own outcome, and one observed leg was `void`
   inside a losing ticket while another was never graded at all. `has_leg` is a property-less edge
   (`defineLink` takes only name, description, endpoints), so that data survives only inside the
   opaque `legs` JSON blob, which nothing can traverse. **This is the strongest known argument for
   link properties**, and it comes from the founder's own primary use case.
2. **`legs` and `has_leg` say the same thing twice** — a JSON array and a link both describing
   which markets a ticket bets. One of them is a second truth store. WO-103 decides which.
3. **A leg's state space is not the ticket's.** `ticket.grade` is `pending → win|loss|push|void`;
   observed legs additionally need "not graded." One enum is doing two incompatible jobs.
3a. **Void-leg repricing — measured, and the answer is "not on a losing slip."** Founder,
   2026-07-25, from the real slip: the displayed combined odds **did not recompute** after a leg
   voided (*"Selection did not Start/Participate"*). That is the observation; take it as fact.
   **What it does not settle** is the case that actually matters for money — a slip whose
   remaining legs all *win* while one leg voids. There the book must price something, and whether
   `combined_price` is one immutable number or two (at selection, at settlement) is still open.
   **Do not resolve this in WO-102** — it interacts with per-leg state, which is WO-103's. Record
   it, model `combined_price` as the figure at selection, and note in your report that realized
   return may not be derivable from it alone. `PROTOCOL.md` external-surface rule: the winning-
   with-void case is **unprobed**, and no order may assert a book's settlement behaviour from
   inference. It resolves when such a slip settles, not before.
4. **Creation of an arrival-settled ticket** — deliverable 4c above. WO-103 owns it.
5. Findings 1–10 already logged in [`WO-101.md`](WO-101.md) stand unchanged.

If you find an eleventh, add it here rather than acting on it.

## The abstraction claim is untested, deliberately — read this before believing the type names

The market plane comes out of this order named `venue` / `instrument` / `quote` / `market_event`,
which *sounds* market-agnostic. **Nothing in this order tests that claim, and you must not report
that it does.**

Testing it requires a bet shape structurally unlike the ones being modelled — one where an
`instrument` has **no bounded `market_event`**. A crypto perpetual is one such shape; a
season-long outright is another. **The founder has ruled out both**, and the ruling is right on
its own terms: neither is a bet they place, and building for either is scope the product does not
need.

The consequence, stated plainly rather than hidden inside a type name: **every bet shape this
schema has been pointed at is "one bounded event with selections hanging off it."** Within singles
and parlays on bout-like events there is no structural discriminator — a parlay differs from a
single at the *ticket* level, not at the instrument-to-event level. So these four types may be a
genuine market abstraction, or a bout schema with good names. **From inside this order's scope
those two are indistinguishable, and no gate here can separate them.**

This is written down instead of quietly tested-around because the repo's own rule — *declaration
is not capability* (doctrine A5) — binds the architect too. Naming a type `instrument` does not
make it one. **Logged as ROADMAP debt #20 with a trigger:** the first bet shape that is not
one-bounded-event-with-selections. Until then the honest claim is *"a sportsbook plane with
market-agnostic names,"* and Part IV's market-agnostic language describes an intention, not a
verified property.

**What you must still do, and it costs nothing:** keep the names, and keep `instrument` free of
any hard dependency on `market_event` — no non-nullable link, no required field, no code path that
assumes a bout exists. That is the whole difference between *untested* and *foreclosed*.

## Evidence required in the report

Rename map as implemented · the `competitor` / `result` retention argument (one paragraph) ·
G1 bait transcripts both directions · G2 command + output · G3 fixture path and test output ·
G4 fresh-database transcript · conformance count before → after **with the mechanism explained** ·
confirmation that `packages/qf-kernel` and `collab-electron` were not touched · one paragraph on
**every** place you exercised judgment where this order was silent.

That last paragraph is the most valuable thing you will write. WO-101 was reissued because its
first version asserted three things measurement disproved, and its *second* version still shipped
a gate whose stated mechanism was false. Assume this order contains one too.

## Out of scope (hard)

Any `packages/qf-kernel` change · any `collab-electron` change · creation commands or creation
paths of any kind (WO-103) · link **writes** (WO-103) · link **properties** (WO-103) · the ticket
arrival-settled state machine (WO-103) · MCP tool emission or codegen changes (WO-104) · ingesting
any real data (WO-107) · a second market **pipeline** (the G3 fixture is not a pipeline) · any new
package or dependency · the report publication gate (WO-110).

---

## Verification record — PASS (2026-07-25, verifier)

**Seat disclosure, for the stranger reading this later.** This verifier authored nothing on this
rung — not the order, not the build, not the scoping — but had earlier the same day checked the
architect's post-build report as testimony, so some branch files were warm. Compensation: every
number below was predicted from the spec *before* reading any builder report, then compared.

**Re-derived cold (clean detached worktree from `origin/wo-102` @ `6648f92`, no `node_modules`
by construction).** `bun qa/run.ts --all`: exit 0, **12/12 PASS, zero FAIL**. First run's exit
code was masked by a `| tail` pipe (verifier error, caught and re-run without the pipe — the
pipeline's exit is `tail`'s, not the runner's; do not repeat this).

**Predictions vs. measured — all four matched before any report was read:** tools 65→**73**
(23 types × get+query + 27 actions) · tables 24→**25** (+`venue`) · conformance **118 → 118**
(generated from `transitions`; the rename moves the key, `venue` is stateless) · suite
**143 → 147** (+4, all in `define.test.ts`: 15→19 — the G1 enforcement tests, the G3 fixture,
the pipelineFed-flags assertion).

**G1 re-baited, both shapes, both typed errors verbatim:**
- `market_event` marked `pipelineFed: true` → red: `Pipeline-fed type "market_event" must not
  have transition command edges (start_event: scheduled→live)` → restore → green.
- creation command naming `quote` planted in `commands.ts` → red: `Pipeline-fed type "quote"
  must not have creation commands (publish_artifact)` → restore → green.
- Both enforcement sites confirmed present in `lintCommands` — the order demanded creation *and*
  transition coverage, and both exist.

**G2 re-derived with the verifier's own command,** not the builder's: all 23 `schema.objects`
names enumerated; exact-match grep for `event|market|odds_series` → no survivors. Falsified via
the G3 bait below (a planted `odds_series` appears in the enumeration).

**G3 pin falsified — including one honest miss.** The verifier's first bait was botched: two sed
insertions landed `odds_series` in the *import block* only, never in `schema.objects`; the count
stayed 23 and the pin test **correctly** stayed green. Nearly filed as a false REWORK finding;
caught by reading the file state instead of trusting the edit. Redone with a real registration:
red at `Expected length: 23, Received length: 24` → restore → green. Lesson, same one as always:
verify the bait planted before judging the trap.

**G3 enumeration confirmed complete** against the order's four structural facts, with two items
beyond them (per-leg quote id, leg sequencing) — the WO-103 brief the gate existed to produce.

**G4 re-run** through `bun:sqlite` on a fresh temp `kernel.db`: migrated clean, 25 tables,
`instrument`/`market_event`/`quote`/`venue` present, zero retired tables. No existing database
touched.

**Slip safety re-scanned independently,** extended to a surface the build report did not claim:
the order commits on main (`d4356cc..3b78a81`). Clean — fixture is `BV-SYNTH-*` / lettered
fighters / round numbers, order carries `<amount>` placeholders only.

**The two architect questions, ruled:**

1. **Action names (`start_event` on `market_event`) — the build is correct; the order was.**
   D1's rename table is the instruction and covers objects only; its own `market_event` row says
   "keeps its state machine and all three commands." The declared-deps line anticipating a rename
   was a conditional warning, not a requirement — an authorship wart. The builder's split
   (domain-event strings renamed `event.*` → `market_event.*`, action names kept) is coherent.
   **Routed:** action-name alignment is decided by **WO-104's author**, where actions become
   agent-facing tool vocabulary; renaming then also forces the `ONTOLOGY_SCHEMA.md` §Actions
   update through `doc-action-surface`, which is the gate working.
2. **`docs/ONTOLOGY_SCHEMA.md` drift — real, pre-existing class, not chargeable here.** The doc
   already lacked WO-101's `mission`/`policy`/`environment` before this rung; WO-102 widened it
   (`event` ×12, `market` ×8, `odds_series` ×2, zero new names). The file is edit-by-order-only,
   the order listed it neither in nor out, and the builder rightly left it alone. **Logged as
   ROADMAP debt #21** with the structural point: the hand-written schema doc duplicates the
   generated, gate-checked `golden/ONTOLOGY.md` — one maintained by machine, one by nobody.

**Not charged, per SCOPES routing:** findings 1–4 above (link properties, `legs`/`has_leg` dual
truth, leg state space, arrival-settled creation) — all WO-103's. Debt #20 (abstraction untested)
was logged by the order itself.
