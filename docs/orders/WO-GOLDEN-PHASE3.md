# WO-GOLDEN-PHASE3 — whole-product Golden requalification

status: **PREPARED / FRESH SEMANTIC READER REQUIRED / BUILDER CLOSED**
depends: G1–G12 independently accepted; G12 candidate `d14ceb36659d86f157b4856b927581616dbaaa56`

## Objective

Requalify the complete remaining supported repository and current Windows product from the purified Phase-2 source, freeze one immutable candidate, obtain one independent Verifier PASS, and only then designate the Golden SHA.

## In plain terms

Prove from a clean start that the whole product Ryan has today is intentional, installs and runs on Windows, tells the same truth on screen as in its durable record, completes a real research journey, survives reopening, and shuts down cleanly.

## Authority and starting identity

- purified source candidate: `d14ceb36659d86f157b4856b927581616dbaaa56`
- purified source tree: `b84d4641a5f37e76c3e62a7df54d8019e0869ffe`
- purified source sole parent: `200d45a92fbef4238b65436f2695b50e54434199`
- G12 independent Verifier: task `01a051b0-75f7-7272-954b-b52d8c64bc20`
- G12/Phase-2 closure: [immutable acceptance](evidence/golden-baseline/g12/VERIFIER-ACCEPTANCE-20260830.md)

Read `START_HERE.md`, `docs/orders/NEXT.md`, this order, `docs/orders/PROTOCOL.md`, ADR-0004, `docs/orders/GOLDEN-RUN.md`, `qf-atlas/ATLAS.md`, and every Phase-3 TSV in full. The exact authority commit containing this packet must receive a fresh semantic Reader YES / YES before a Builder exists.

## One meaning

Phase 3 is whole-repository/current-product requalification, not a thirteenth purification group and not R18. Accepted G1–G12 are inherited and remain byte-immutable. Do not deeply rerun a prior group merely for ceremony; reuse its exact-SHA receipt unless Phase-3 composition directly exercises that boundary. Phase 3 must nevertheless account for every current file, dependency, package hook, operational root, current claim, gate, and supported consumer path as one product.

## Deliverables

1. A mechanically generated current census that accounts for 100% of tracked files, direct dependency declarations, Electron package declarations/hooks, operational roots, generated resources, and registered gates. Reconcile every change from the Phase-1 provenance (`1,150`, `153`, `19`, `40`) through accepted G1–G12 dispositions; historical counts are provenance, never the current denominator.
2. A complete disposition proving zero unexplained production, package, compatibility, future-route, authority, or test defects. “Known,” “legacy,” skipped, unreachable, or future-useful is not an explanation without an accepted disposition.
3. Clean frozen install, build, package, installer, first launch, normal shutdown, and relaunch from the purified source on native Windows x64. Ambient installs, caches, or previously built product bytes may not substitute for this proof.
4. Honest composition of every current registered gate and test. Each current product claim has a fail-capable owner; no wrapper-only green, mock/fixture presented as live capability, weakened assertion, dead falsifier, silent skip, or duplicate authority counts.
5. A naive Windows app traversal of the supported current consumer surface. Every visible state/claim must agree with read-only Kernel truth; projections may not remember durable truth.
6. Four independent current-floor proofs, P14-A through P14-D: fresh successful Bovada production capture; fresh real packaged-app Hermes Director turn; fresh accepted R17 deterministic guided loop and reopen; and explicit zero-delta refusal of the not-yet-implemented live capture→Dataset→Technique composition. No proof may impersonate another, and no bet or trade is placed.
7. Close the application and reopen it against the same canonical Kernel. Prove the Mission, sessions, Tasks, evidence, Evaluation, result, and steering history remain visible and agree with Kernel truth.
8. Normal final shutdown with `processes=0`, `roots_remaining=0`, and `leaked=[]`.
9. Exact Atlas, law, gate, command, output-hash, duration, candidate/tree/parent, changed-path, and cleanup receipts. Atlas must be generated-current, pass its check and ratchet, and report HARD RED 0.
10. One immutable Phase-3 candidate followed by one fresh independent Verifier. Only a later evidence-only Router rotation after that PASS may name the Golden SHA.

The exact matrix is [`PHASE3-STARTING-MATRIX.tsv`](evidence/golden-baseline/phase3/PHASE3-STARTING-MATRIX.tsv). The exact scope/disposition map is [`PHASE3-SURFACE-MANIFEST.tsv`](evidence/golden-baseline/phase3/PHASE3-SURFACE-MANIFEST.tsv). The finite denominator and output schemas are [`PHASE3-CENSUS-SCHEMA.tsv`](evidence/golden-baseline/phase3/PHASE3-CENSUS-SCHEMA.tsv) and [`PHASE3-WINDOWS-TRAVERSAL.tsv`](evidence/golden-baseline/phase3/PHASE3-WINDOWS-TRAVERSAL.tsv).

## Finite accounting contract — P05, P06, P08, P12

Reader authority SHA `746d29a0` freezes the seed outputs, counts, and SHA-256 values in [`PHASE3-SEED-HASHES.tsv`](evidence/golden-baseline/phase3/PHASE3-SEED-HASHES.tsv): **1,440 tracked files, 20 exact-leaf `package.json` manifests, 103 direct-dependency rows, 6 Electron build/package hook scripts, 15 exact-leaf `bun.lock` roots, 122 tracked test/spec files, and 72 registered gates**. The authorized clean-install repair changes only the direct-dependency denominator to **104** by adding exactly `collab-electron/package.json` `dependencies["@agentclientprotocol/sdk"] = "0.18.2"`; the new execution-source census records the mechanically generated 104-row output hash and exact one-row delta from the immutable 103-row seed. No G5/G7 history is rewritten. The Census TSV defines exact extractors, grammars, normalization, and outputs. `agentos-package.json` is a packaged resource, not a package manifest.

After a fresh Reader YES / YES, the resumed Builder may change only `collab-electron/package.json`, `collab-electron/bun.lock`, the already-authorized `qa/gates/golden-phase3-requalification.ts` and `qa/run.ts` registration, exact `INSTALL_ORCHESTRATION` (`qa/verify-release.ts` and `qa/verify-release.test.ts` only), Phase-3 evidence, and generated Atlas outputs. No source, Kernel, UI, protocol behavior, test, gate, config, or product file outside those exact rows is authorized. The prepared gate must mechanically emit:

- `tracked-files.tsv`, `direct-dependencies.tsv`, `package-hooks.tsv`, `package-lock-roots.tsv`, `packaged-generated-resources.tsv`, `tests.tsv`, `gates.tsv`, `claims.tsv`, and `supported-consumers.tsv`, one stable sorted row per item;
- `dispositions.tsv`, exactly one row for every item in those ledgers with `current-production`, `package`, `compatibility`, `authority/evidence`, `test/proof`, or `retired-by-accepted-Gn` disposition plus exact evidence;
- `claim-proof-map.tsv`, exactly one row per current claim with its supported consumer, test/gate, named falsifier selector and expected RED, live-versus-fixture tier, and Kernel oracle where visible;
- `completeness.txt`, which is PASS only when every ledger key occurs exactly once in dispositions, every current claim has at least one fail-capable proof/falsifier, no unknown extra row exists, and recomputed totals equal the frozen starting census plus the enumerated Phase-3 proof-only delta.

Falsifiers must delete one ledger row, duplicate one disposition, remove one claim mapping, point one claim at a fixture-only proof, and add one unregistered gate. Each must RED. P06 is satisfied only by zero blank, `unknown`, `legacy`, `future`, `unreachable`, or unsupported dispositions.

Every registered gate is classified in [`PHASE3-GATE-CLASSIFICATION.tsv`](evidence/golden-baseline/phase3/PHASE3-GATE-CLASSIFICATION.tsv) as `CURRENT_HEAD` or `HISTORICAL_EXACT_SHA`; set equality with the 72-row Reader-authority `bun qa/run.ts --list` output plus the single enumerated Phase-3 registration delta is mandatory. `golden-g11-authority` is permanently `HISTORICAL_EXACT_SHA` at accepted G11 candidate `1f81c469371fbb4db3e4e8bdac1248f0a0d3d51c`; it is never rewritten to bless current authority. A historical gate runs from a fresh isolated `git archive` of its recorded SHA, records archive/output hashes, and reconciles the current gate name, description, registration bytes, implementation-path bytes, and accepted receipt to that historical identity. Running it from any other SHA must RED with `wrong-historical-authority`. All other starting gates are `CURRENT_HEAD` and run from the immutable Phase-3 execution source.

## Non-circular execution source and isolated Windows run — P09 and P10

Execution sources `9ee5c688` and `9721db6d22e789715e1c1780c65dc36aef53ccac` remain immutable rejected ownership/install-topology evidence. Their bounded orchestration descendant `ca48db40ab4a926460ffbbb7196e5ad52590feb1` (tree `846e88427f654bbedc450ec448b1d1abc44c2ab6`, sole parent `9c709c58e5053460233b2aa630fb795ae9d1248a`) is a third immutable rejected canonical-install source: Windows units and native-TUI were green, but the later G12 Bovada consumer could not resolve package `qf-kernel`, module `qf-kernel/portable`, because its own frozen root was not preinstalled. [The exact ca48 identity and receipts are frozen here](evidence/golden-baseline/phase3/REJECTED-EXECUTION-SOURCE-CA48DB40.md). No rejected source is amended, called candidate, or reused for P09–P17.

After Reader YES / YES, the Builder creates a new clean `PHASE3_EXECUTION_SOURCE_SHA` whose exact parent is this amendment authority. The two direct declarations remain exact and distinct: `species/hermes/package.json` owns direct/source execution of `host-acp-client.ts`; `collab-electron/package.json` owns the packaged Electron consumer/bundle; both declare runtime `@agentclientprotocol/sdk` exactly `0.18.2`. Hermes was already one of the 103 seed rows and Electron remains the sole new row, so the denominator stays 104. A third declaration, root workspace manifest, resolver alias, `NODE_PATH`, source relocation, service, credential, network path, or broader architecture change is forbidden.

The additional authorized delta remains only `qa/verify-release.ts` and `qa/verify-release.test.ts`. Before `qa/windows-unit.ts` or any subsequent consumer, the verifier performs exactly three preinstalls, in order, each with a distinct run-owned cache: (1) `collab-electron`: `bun install --frozen-lockfile`; (2) `species/hermes`: `bun install --frozen-lockfile --linker isolated`; (3) `tools/qf-bovada-football`: `bun install --frozen-lockfile --backend copyfile --linker isolated`. The test freezes the complete classification of all 15 lock roots, the exact three-root set/order/options/distinct-cache contract, and baits missing, moved, extra, wrong-option, and shared-cache roots. It may not install all 15 roots.

All three manifests and locks remain byte-identical to `ca48db40...`; the direct-dependency denominator remains 104. The measured shared-cache and distinct-cache EPERM receipts and the Hermes isolated-linker correction remain immutable evidence. P09–P17 run only from a fresh archive of the new source. Byte equality against `ca48db40...` is mandatory outside the verifier/test orchestration, permitted evidence, and unconditional generated Atlas outputs.

All cold work occurs in a new run-scoped directory outside the shared checkout and records archive SHA-256 plus execution-source SHA/tree/sole-parent. It must use a new empty Bun cache/install cache and may use only committed lockfiles with frozen installs. It must not delete, modify, or reuse shared-checkout `node_modules`, build output, package output, database, profile, or cache.

The receipt records the exact commands: frozen installs for every lock root through the canonical `bun qa/verify-release.ts` orchestration; `bun run build`, `bun run package:unsigned`, `bun qa/run.ts windows-cold-boot`, `bun qa/run.ts windows-installer`, and the canonical verifier from the isolated source. The run receives unique absolute `QF_KERNEL_DB`, temp, user-data, artifact, profile, package-output, install, and receipt roots. Before launch and after final shutdown, record PID/parent/name/command-line censuses. Cleanup may remove only roots created and ownership-marked by this run after proving their resolved paths lie beneath the run root. Any pre-existing or unowned process/root is observed, never killed or deleted. Final owned counts are zero.

The focused repair proof retains the Electron/Hermes ownership baits and additionally requires every one of the all-15 classification and exact-three-root baits to RED. Green requires distinct empty caches, the exact commands/options/order above, Windows units, build/package/first launch/normal shutdown/relaunch, credential-free host-ACP handshake, native-TUI admission/shutdown, G12 Bovada assertions, Atlas, and cleanup. These prove canonical install orchestration, not new behavior.

After the repair the Builder runs `bun qf-atlas/generate.mjs` unconditionally and includes every changed generated Atlas output, then runs `bun qf-atlas/generate.mjs --check`, `bun qf-atlas/ratchet.mjs`, and the Phase-3 Atlas assertion with `HARD RED 0`. An unchanged Atlas result is still recorded; generated outputs are never hand-edited.

## Exact Windows consumer and current-floor traversal — P13 through P16

[`PHASE3-WINDOWS-TRAVERSAL.tsv`](evidence/golden-baseline/phase3/PHASE3-WINDOWS-TRAVERSAL.tsv) is the checklist. It covers Research Director admission, Mission canvas/card and inspect view, Dock inventory and specialist tiles, Task plan/assignment/status/steering history, Dataset/Run/Artifact/Evaluation/Report lineage, current-versus-history state, close, reopen, and normal shutdown. Every visible claim is transcribed and compared to a read-only query through the generated Kernel read surface against the same isolated DB; direct UI writes or a second oracle are forbidden.

P14 is exactly four mandatory independent proofs:

- **P14-A — live capture.** One fresh successful production Bovada NFL capture creates a stored Kernel source Artifact and must still admit the exact eligible `market_event`, related `instrument`, and timestamped `quote` objects/links. Record source identity, observed/source timestamps, content hash, admitted IDs, and exact cardinality. If admission is RED after a successful capture, preserve that RED and authorize one evidence-only aggregate analyzer over the exact stored source Artifact from that run (or an exact reproduction), with no refetch. Evidence sources are split: the original production process/error receipt and reached selection error code bind HTTP status `200`, approved-final-origin validation, and JSON media-type validation; the stored-Artifact analyzer binds body content hash/bytes, available Artifact/Kernel timestamps, coupon/FOOT/NFL/event counts, sequential predicate rejection counts through competition, live, status, future, competitors, Game Lines, Moneyline, market status, period description, live, main, outcomes, and ambiguity, reach counts, earliest/latest timestamps, and eligible count. The analyzer must not claim it independently measured transport metadata and must not print raw JSON, teams, competitors, odds, headers, or names. Bind both evidence sources to execution-source SHA and source Artifact hash, and record the exact source Artifact/object/link delta—never falsely claim zero source delta. Reaching source data is not successful market admission, and P14-A remains RED pending classification. In a separate isolated failure run, schedule a production capture within 24 hours and cancel it by normal app shutdown; require the RPC cancellation receipt and exact pre/post equality of all market/source objects and links. A malformed CLI/argument refusal, client-side validation, or network refusal does not count. One successful live capture with successful admission remains mandatory.
- **P14-B — real Director turn.** One fresh non-synthetic turn launches the packaged Windows app's default Hermes Research Director profile. Kernel truth is limited to the exact `agent_definition`, `agent_session`, links, and lifecycle events. Execution evidence is a separate run-owned external terminal transcript with byte count and SHA-256; it is explicitly not Kernel truth and no nonexistent durable turn object is claimed. Provider/model identity remains exactly `provider-model-identity-unprovable` unless trusted Hermes runtime metadata independently supplies it; model/self-report never counts and no Kimi or other provider/model claim is made without that metadata.
- **P14-C — guided deterministic research.** Run the accepted R17 guided-settled-results path freshly with exact Technique/Strategy id, semantic version, and content hash, then the supported deterministic sample Dataset→Run→Artifact→independent Evaluation→exactly one current Report. Label every Dataset/input/output visibly and durably as sample/deterministic. Close and reopen the same Kernel and prove exact IDs, hashes, links, cardinality, current Report selection, and labels persist. Accepted R17 receipts are provenance, not a substitute for this fresh run.
- **P14-D — missing live composition capability preflight.** Perform a read-only inventory of the current generated actions, served action surface, and supported product controls and prove no production capture→Dataset admission action exists. In the evidence layer, classify the result exactly `R18 seam unavailable`. Compare exact pre/post Kernel sets and require zero delta for Dataset, Run, result/report Artifact, Evaluation, and Report; P14-A capture objects remain intact. This is Verifier evidence, not a product-emitted refusal or shipped refusal feature; no visible product refusal or attempted mutation is required. Record no trade/bet and keep R18 open/frozen.

P13–P17 must run freshly from a descendant of execution source `9a0e6359` or the final current source that supersedes it under this authority. Named falsifiers are `sample-claimed-current`, `provider-identity-self-report`, `fixture-as-live`, `missing-technique-identity`, `fabricated-coverage`, `ui-kernel-mismatch`, `steering-ui-only`, `persistence-drop`, `extra-lineage-object`, and `wrong-current-report`. Each must RED, with the first five mandatory independent Verifier reruns. Accepted G12/R17 semantics and receipts remain binding provenance but cannot substitute for P14-A/B/C/D.

P14-A, P14-B, P14-C, and P14-D are independent execution units but all remain mandatory. A RED in one unit blocks P18 candidate freeze and Golden designation, but does not stop fresh execution or preservation of the other three. No preserved green reclassifies or closes a RED unit, and the P14-A aggregate diagnostic is evidence only, not product behavior or acceptance.

## Candidate, Verifier, rollback, and Golden chain — P18 through P20

Allowed Phase-3 mutation is limited to the repair, gate/registration, generated Atlas, and evidence paths in the manifest. After P09–P17, the Builder commits only the already-complete P01–P17 receipts as the sole child of the new `PHASE3_EXECUTION_SOURCE_SHA`; that commit is the immutable Phase-3 candidate. Its delta is exactly those enumerated receipts. Product/source/test/existing-gate/config/generated-runtime bytes equal the new execution source byte-for-byte, and accepted `g1/**` through `g12/**` evidence equals purified source byte-for-byte. No receipt inside the candidate may name or predict its own identity or freeze time. No edit follows freeze.

The fresh Verifier first resolves the candidate from Git and records its exact SHA, tree, sole parent, commit timestamp, independent freeze-observation timestamp, clean status, complete tree delta, and candidate-versus-execution-source equality in the Verifier acceptance, which necessarily exists outside the candidate. It then archives either the candidate (whose executable bytes are proven equal) or `PHASE3_EXECUTION_SOURCE_SHA`, records which, and runs the complete P01–P19 matrix, all completeness falsifiers, `wrong-historical-authority`, the four mandatory Windows falsifiers, clean package lifecycle, canonical verifier, live research, close/reopen, and cleanup. It never runs from a dirty tree and does not edit, regenerate candidate bytes, kill unowned processes, or repair a red.

Rollback exists only inside the isolated execution root and deletes only ownership-marked run-created files after path confinement proof. It never rewrites the candidate, shared checkout, accepted evidence, `main`, or any remote ref.

After Verifier PASS, Golden designation is one receipt-only descendant whose sole parent is the exact passed candidate. Its tree delta is enumerated and limited to the Verifier acceptance, Golden designation receipt, and `NEXT.md` closure text; it may contain no product/test/gate/config change. `main`, remotes, and R18 remain untouched. R18 stays frozen until a separate later `NEXT.md` rotation.

## Execution and stop conditions

Freeze P01–P08 before mutation. Run fast static/focused checks before expensive clean-source and packaged traversal, but execute P09–P17 once in full on the final product bytes. Independent read-only commands may run concurrently only when they cannot mutate shared state.

Stop immediately and return the smallest exact route when any of these occurs:

- the authority SHA/tree/parent, G12 closure identity, or clean starting state differs;
- any current item cannot be accounted for or any defect remains unexplained;
- a product, semantic authority, supported-platform, compatibility, scope, or acceptance-meaning change is required;
- any existing test/gate must be weakened, deleted, skipped, or reinterpreted to pass;
- any manifest/lock/dependency change, install-all-15 strategy, root workspace manifest, resolver alias/`NODE_PATH`, source relocation, service, credential, network update, signing path, trade/bet execution, R18 capability, unsupported platform, or broader architecture change would be required;
- clean install/build/package, canonical release, UI↔Kernel agreement, live research, persistence, Atlas/law, or cleanup is red;
- evidence cannot distinguish live capability from fixture/mock proof;
- any non-receipt byte changes after the candidate is frozen.

Exact exception for independent P14 continuation: a RED in P14-A, P14-B, P14-C, or P14-D stops and preserves only that unit, permits fresh execution/preservation of the other independent units, and permits only the bounded P14-A diagnostic above. It still absolutely forbids P18, candidate freeze, Verifier acceptance, or Golden designation until all four units are green. Every other RED follows the immediate global stop.

A red is not permission to repair. The Builder freezes the evidence and stops. The Router may make a same-meaning receipt/isolation/invocation correction only after proving it is purely mechanical under ADR-0004; every product or semantic red requires a separately bounded authority amendment and fresh Reader before editing.

## Rollback

Before any authorized repair, record the exact authority HEAD/tree and changed-path manifest. Rollback is the ordinary inverse of only the separately authorized Phase-3 change, restoring the purified source behavior and configuration without rewriting shared history, deleting evidence, changing accepted G1–G12 bytes, or touching `main`. Re-run the focused falsifier and the matrix rows affected by that inverse. If exact restoration is not possible, stop; do not improvise a second baseline.

## Acceptance and handoff

The Builder reports evidence, never PASS: the Git-resolved candidate SHA/tree/sole parent in the external handoff message (not a candidate-contained receipt), full changed-path manifest, each P01–P17 result and receipt path/hash, red/green bait transcripts for any new or changed gate, current accounting totals, Atlas HARD RED count, UI↔Kernel comparison, research lineage IDs, close/reopen proof, and final process/root counts. The worktree must be clean. The independent Verifier owns the durable P18 identity/freeze record.

A fresh independent Verifier repeats the mandatory matrix and falsifiers under P19. If and only if it returns PASS on the immutable candidate, the Router may write the strictly bounded receipt-only descendant under P20. Phase 3 does not open R18; `NEXT.md` must be separately rotated after Golden closure.

## Reader brief — exactly two questions

1. Can every Phase-3 deliverable and matrix row actually fail, including full accounting, clean-source package lifecycle, honest gate composition, UI↔Kernel agreement, live research, persistence, cleanup, and independence?
2. Does this packet have exactly one meaning—complete current Windows product requalification from the immutable purified source, inheriting rather than ceremonially rerunning G1–G12, with any repair stopped and separately routed before one final candidate, independent Verifier, and Golden SHA?

The Reader returns `YES / YES` or a finite defect list against the exact authority commit. No Builder creation, product/test/gate edit, Phase-3 execution, Golden designation, or R18 work is authorized before YES / YES.

## Proposed current-defect Builder authority — Reader pending — 2026-08-30

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` identified finite defects that are corrected in [the proposed current-defect amendment](evidence/golden-baseline/phase3/PHASE3-CURRENT-DEFECT-AMENDMENT-20260830.md). It did not approve its own corrected authority. The amendment remains Reader-pending and opens no Builder work at this commit. Every other deliverable, acceptance criterion, falsifier, stop, isolation rule, candidate chain, and R18 freeze in this order remains unchanged.

Only after an actual fresh Reader returns YES / YES may a separate receipt/rotation commit open the amendment's exact Builder surface. Until then, no capture, analyzer, product, test, gate, or execution change is authorized. P18, candidate freeze, Verifier acceptance, Golden designation, `main`, remotes, and R18 remain closed until all P01-P17 rows are green.

## Proposed Run-result outcome-control amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite defect classification at execution source `60f82076019445b36f7344f513b30dc59eac9d20` / tree `c8db09546ec85126e88bb32e206ce80113a7cfc0`. [The proposed amendment](evidence/golden-baseline/phase3/PHASE3-RUN-RESULT-OUTCOME-AMENDMENT-20260830.md) limits the next repair to the exact Run-result Artifact's Dock Inspect outcome control in `qa/gates/technique-outcome-loop.ts` and exact `run_id` projection ownership in `collab-electron/src/main/research-world-projection.ts`.

That amendment is not self-approving. Builder authority is closed until a fresh Reader returns YES / YES against its exact commit and a separate receipt/rotation opens only those two paths. Existing lifecycle/lineage repairs are preserved; P14-A parser mutation remains closed and measurement pending; the proposed repair may repair P14-C/P13-P16 but does not close P15 by itself. Candidate/Golden, `main`, remotes, and R18 remain closed.

## Proposed production-continuation sequencing amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite gate-only defect at product commit `75515398d8e8357a73a18d636332653a02040036` / tree `0ce5bb4d00bcf1bdfe2a8dbfa99b9c9e421d2033`. [The proposed sequencing amendment](evidence/golden-baseline/phase3/PHASE3-PRODUCTION-CONTINUATION-SEQUENCING-AMENDMENT-20260830.md) permits only `qa/gates/technique-outcome-loop.ts` and `qa/oracles/r17-technique-outcome.json` to observe the already-scheduled production review continuation, bind its exact terminal identities, and preserve exact 15/17 pre-settlement and 17/21 settled/reopened worlds.

The amendment is not self-approving. Builder authority remains closed pending a fresh Reader YES / YES against its exact commit and a separate receipt/rotation. Product bytes, R15 uniqueness, independent review/publication, earlier lifecycle/lineage and Run-result repairs, P14-A parser closure/measurement status, P15, candidate/Golden, `main`, remotes, and R18 remain unchanged.

## Proposed P13/P16 pre-R18 continuation amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite one-file gate defect at source `513cc25722bdcadbe9b0635b155489bc4d400ca8` / tree `022b19eae8a837ae8e822a7284c8bd8bd7b52d71`. [The proposed P13/P16 amendment](evidence/golden-baseline/phase3/PHASE3-PRE-R18-CONTINUATION-SEQUENCING-AMENDMENT-20260830.md) permits only `qa/gates/pre-r18-coherence.ts`: retain the initial Dataset/Technique fixture load, remove the later fixture-review call, await and dynamically bind one terminal production continuation, reveal exact 15/17, settle to 17/21, and reselect the exact Run-result after refresh and reopen.

The amendment is not self-approving. Builder authority remains closed pending a fresh Reader YES / YES against its exact commit and a separate receipt/rotation. The oracle and all product files remain unchanged; existing semantics and every parser, P14-A, P15, candidate/Golden, `main`, remotes, and R18 restriction remain binding.

## Proposed Dock refresh-coordination amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite Dock refresh defect at source `4c9964dff57b08c9b9d3d39c6e0bafa37a7721e6` / tree `547c08c5718d06607c7cab466f1f833faf6e8323`. [The proposed amendment](evidence/golden-baseline/phase3/PHASE3-DOCK-REFRESH-COORDINATION-AMENDMENT-20260830.md) permits only Dock implementation/test plus the existing pre-R18 proof surface to replace dropped overlapping invalidations with coalesced single-flight complete snapshots whose newest pass wins.

The amendment is not self-approving. Builder authority remains closed pending a fresh Reader YES / YES against its exact commit and separate receipt/rotation. The same-ID Kernel closed, runtime stopped, work completed, recovery restartable invariant remains exact across Kernel/Canvas/Dock/Inspect. All parser, P14-A, P15, candidate/Golden, `main`, remotes, and R18 restrictions remain binding.

## Proposed P13/P16 view-state amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite gate-only view-state defect at source `bf6754ad72a631c9be8eba5c4a660495483f22a1` / tree `9a59f3eeaea5301220ec1b318c9faebe2a66e6ef`. [The proposed amendment](evidence/golden-baseline/phase3/PHASE3-PRE-R18-VIEW-STATE-AMENDMENT-20260830.md) permits only `qa/gates/pre-r18-coherence.ts`: Back first proves `ORDINARY_CANVAS`; visible exact-Mission navigation establishes `CURRENT_MISSION`; only then may `Show full lineage` establish `FULL_LINEAGE`; reopen repeats the deliberate exact-Mission navigation before identity/cardinality comparison.

The amendment is not self-approving. Builder authority remains closed pending a fresh Reader YES / YES against its exact commit and separate receipt/rotation. Product behavior and exact object/link membership remain unchanged, and every parser, P14-A, P15, candidate/Golden, `main`, remotes, and R18 restriction remains binding.

## Proposed LOCAL cable-semantics amendment — Reader recheck required

Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the finite LOCAL cable defect at authority base `b32e3fce598e5cfc6a782b7952fd3de6088b93e6` / tree `67fc9aef9e481346afba018bd901346e51845fc8`. [The proposed amendment](evidence/golden-baseline/phase3/PHASE3-LOCAL-CABLE-SEMANTICS-AMENDMENT-20260830.md) permits only `research-world.js` and its test: current overview remains exact 17/12; LOCAL shows all and only links whose endpoints are within those 17 objects—exact 21 in settled R17—with exact selected/unrelated opacity semantics; Back clears and Mission reopen restores unselected 17/12.

The amendment is not self-approving. Builder authority remains closed pending a fresh Reader YES / YES against its exact commit and separate receipt/rotation. The preserved unstaged pre-R18 gate and its assertion remain unchanged. Product truth, FULL/historical exclusion, parser/P14-A, P15, candidate/Golden, `main`, remotes, and R18 restrictions remain binding.

## Reader-adjudicated selected cable-label placement repair

Fresh semantic Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on a real C14 product defect at authority base `f0d0634868b5b89e8b58f6600c2dff8cb129552a` / tree `197f1b643469aca71ebb5997cc2da69a9da455a9`. [The bounded authority](evidence/golden-baseline/phase3/PHASE3-SELECTED-CABLE-LABEL-PLACEMENT-AUTHORITY-20260830.md) adds only `cable-overlay.js` and `cable-overlay.test.ts`: deterministically sample the rendered selected cable path at fixed arc-length fractions, try existing offsets, exclude only exact endpoint rectangles, and choose the first candidate that avoids every unrelated tile.

The scope owner is label placement because the cable path, topology, selected semantics, and research-world membership are already correct; the concrete red is a hypothesis→Evaluation label knowingly overlapping the unrelated worker-evidence Artifact. All exact 17/12/21 membership, selection, C14 assertions, preserved dirty work, parser/P14-A, P15, candidate/Golden, `main`, remotes, and R18 restrictions remain binding.

## Reader-adjudicated P15 deterministic responder repair

Fresh semantic Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on a P15 QA responder regression introduced at `82011c5f934aca5d15b692bed883d1addfc19245`. [The bounded authority](evidence/golden-baseline/phase3/PHASE3-P15-SYNTHETIC-RESPONDER-AUTHORITY-20260830.md) adds only `collab-electron/cli/qf-hermes-synthetic-responder.mjs` and its existing focused test: `nextReview` accepts an exact parsed `qf.task.second_opinion.v1` delivery and otherwise retains the strict existing `parseCriticMission` path.

The scope owner is deterministic QA dispatch because Main, Kernel, gateway, peer identity, timeouts, and the P15 gate contract are unchanged. Exact review-ID acknowledgement, malformed/wrong-contract rejection, no state carry/regex/raw shortcut, and the normal strict `QUANTFLOW_MISSION` path are mandatory. All preserved dirty work, P14-A parser closure, candidate/Golden, `main`, remotes, and R18 restrictions remain binding.

## Reader-adjudicated P15 steering-hold lifecycle repair

Fresh semantic Reader task `01a05212-f0e4-7101-a0d9-8e59df8a3f08` returned YES / YES on the deterministic responder's steering-hold lifecycle. [The bounded authority](evidence/golden-baseline/phase3/PHASE3-P15-STEERING-HOLD-LIFECYCLE-AUTHORITY-20260830.md) remains within the existing responder and focused test: after readiness the hold path owns and awaits `consumeDeliveries(reader)` through reader close, then returns; only the normal research path may background-consume concurrently with its tool turn.

The scope owner is worker lifetime because detached consumption plus separate close waiting can swallow delivery progress. Exact three delivery contracts/IDs, 15-second gate timeout, strict parsing, Main, Kernel, gateway, peer identity, worker2/critic tests, all preserved dirty work, P14-A parser closure, candidate/Golden, `main`, remotes, and R18 restrictions remain binding.
