# Work orders

**Builders** start at [`NEXT.md`](NEXT.md). **Verifiers** start at [`VERIFYING.md`](VERIFYING.md).

Work happens **only** through a work order. Builders: read `/START_HERE.md`, then `PROTOCOL.md`, then your order, then nothing else. The roadmap ladder lives in `/docs/ROADMAP.md`.

## Log

| Order | Title | Status | Assignee |
|---|---|---|---|
| WO-000 | Cut the fork (vault) | done — this repo is the result | fable |
| [WO-001](WO-001.md) | Codegen spike `qf-kernel-schema` | **done** — verified + merged 2026-07-18 | builder (Cursor) |
| [WO-002](WO-002.md) | Docs graduation + roadmap | done | fable |
| [WO-003](WO-003.md) | Full schema v0.2 + transition tables + conformance tests | **done** — verified + merged 2026-07-18 | builder (Cursor) |
| [WO-004](WO-004.md) | Runtime ownership proof: one session ID, no second server | **done** — PROOF HELD (NARROWED); P1 overstated, see WO-004a | builder (Cursor) |
| [WO-004a](WO-004a.md) | Proof correction: assert what is actually true | **done** — verified + merged 2026-07-18; D1 fixed in rework round 1, falsified red/green by builder and verifier independently | builder (Cursor) |
| [WO-005](WO-005.md) | Kernel v0: SQLite from generated migration, command/event layer, Law E gates | **done** — verified + merged 2026-07-18 | builder (Cursor) |
| [WO-006a](WO-006a.md) | Creation commands: the Kernel can publish an Artifact (+ debt #0 gate) | **done** — verified + merged 2026-07-18 after 3 rounds (D1–D5); doc↔code gate falsified both directions | builder (Cursor) |
| [WO-006b](WO-006b.md) | Kernel in the app: Artifact tile + Law D cold-reopen demo | **done** — verified + merged 2026-07-19 (round 2); **Law D PASSED** | builder (Cursor) |
| [WO-006c](WO-006c.md) | One agent path end-to-end: spawn → stream → tool → Artifact → lifecycle | **done** — verified + merged 2026-07-19 (round 2); 10 gates cold from zero installs; **v0.1 phase gate closed**; founder demo folded into the post-WO-008 checkpoint (founder decision 2026-07-19) | builder (Cursor) |
| [WO-006d](WO-006d.md) | One skin: token sheet + one-skin gate (design coherence) | **done** — verified + merged 2026-07-19; hex 55→0, fonts 33→0; look-verdict at the post-008 checkpoint | builder (Cursor) |
| [WO-007](WO-007.md) | Dock v1: species registry + spawn surface + cube empty-state | **done** — verified + merged 2026-07-19 (round 2); 12 gates cold; dock is a Kernel projection | builder (Cursor) |
| [WO-008](WO-008.md) | The plug test: Hermes through the same socket — admitting diff is the gate | **done** — verified + merged 2026-07-19 (round 2); Outcome B (guest cannot see host install); admitting commits pure; D1–D3 fixed | builder (Cursor) |
| [WO-007b](WO-007b.md) | Host seams: spawn split, generic env, tile-control parity | **done** — verified + merged 2026-07-20; admit≠turn; manifest env YES | builder (Cursor) |
| [WO-008b](WO-008b.md) | Hermes reachability: bundle vs authorized mount (probe `mounts`) | **done** — PROBE HELD 2026-07-20; mounts YES; guest WASM-only blocks native Hermes; → WO-008c | builder (Cursor) |
| [WO-008c](WO-008c.md) | Hermes host-bridged ACP (Outcome A outside guest exec) | **done** — verified + merged 2026-07-20 (rework D1–D2); host_acp + launch meta + one client | builder (Cursor) |
| [WO-008a](WO-008a.md) | Permission bridge + tool policy (deny-by-default; unblocks the founder's live turn) | **done** — verified + merged 2026-07-20; host runTurn + allowlist + founder permission UI | builder (Cursor) |
| [WO-008d](WO-008d.md) | Hermes tile = real TUI (standing: native TUI for all future interactive agents) | **done** — verified + merged 2026-07-20; Spawn → term tile `hermes --tui` | builder |
| [WO-008e](WO-008e.md) | A2A proof: 4 Hermes tiles (orchestrate → fan-out → review → talk-back) | **done** — verified + merged 2026-07-20 (rework D1–D5); `a2a-core` + spawn/dispatch IPC | builder |
| [WO-PEER-BUS](WO-PEER-BUS.md) | MCP peer plane beside the TUI (cold harness) | **done** — verified cold 2026-07-20 | builder |
| [WO-PEER-BUS-CANVAS](WO-PEER-BUS-CANVAS.md) | Peer-bus visual PASS — dock seat spawn + founder live turns | **open** — current (park ignored for this PASS); plan `docs/plans/2026-07-20-001-req-peer-bus-canvas-pass-plan.md` | builder |
| [WO-009](WO-009.md) | Datasets I: bootstrap ingestion (Parquet + Kernel pointer) | **parked** until canvas PASS holds | builder |

## Doctrine ladder (P1→P5, eleven rungs — `SCOPES.md`)

| Order | Title | Status | Assignee |
|---|---|---|---|
| [WO-101](WO-101.md) | Research + agent plane charter (P1 rung 1) | **done** — verified + merged 2026-07-25; both new lints re-baited by the verifier against the live schema; G2 re-run cold with two fresh readers | builder (Cursor `gpt-5.3-codex-high`) |
| [WO-102](WO-102.md) | Market plane reframe — betting becomes rows, not types | **done** — verified + merged 2026-07-25; G1 re-baited both shapes, G3 pin falsified (24 vs 23), G4 re-run, all four count predictions matched before reading reports | builder (Cursor) |
| [WO-103](WO-103.md) | The write path (1/2) — creation, edges, and a Kernel regression | **done** — verified + merged 2026-07-25 after 1 rework round + 1 correction; 13 gates cold, exit 0. D0's regression fixed and the maps derived from schema. Arrival-settled rule rebuilt as a **verb split** (`create_ticket` / `observe_ticket`) after the checking seat defeated the original `origin`-field version with a one-word relabel — **the gate that existed to catch that had passed**. Weak gates named rather than rewritten: [`evidence/wo-103/VERIFICATION.md`](evidence/wo-103/VERIFICATION.md) | builder (Cursor `composer-2.5`) |
| [WO-103b](WO-103b.md) | The write path (2/2) — make the action surface honest before it is published | **done** — verified + merged 2026-07-26; 14 gates cold, exit 0. Six dead actions deleted, tool surface **77 → 71** (count re-derived independently, `23×2 + 25`). `connection` ruled canvas-presentation, debt #21 closed by demotion, debt #22's prose trigger replaced by the `observe-door` gate. **First rung in this sequence to get a pre-build adversarial read** — seven order-text defects fixed before a builder saw it, and it needed **zero rework rounds** | builder (Cursor `composer-2.5`) |
| [WO-H1](WO-H1.md) | Debt register audit and sweep (housekeeping, **off-ladder**) | **done** — verified + merged 2026-07-26; 13 gates cold, exit 0, suite counts unchanged (147/23). All 18 open debts re-measured with a `path:line` per verdict; #10 and #12 swept and struck; #13 recorded **could not measure** rather than inferred. Both refactored gates re-baited independently against *real* regressions, not synthetic ones | builder (Cursor `gpt-5.3-codex-high` — pre-dates the composer-2.5/grok-4.5 seat constraint) |
| [WO-104](WO-104.md) | The read plane — schema-driven reads, link traversal, MCP read server (P3 rung 1 of 3) | **done** — verified + merged 2026-07-26 after 1 rework round; 15 gates cold, exit 0. **69 read tools served over real MCP** (`23 objects × 3`, derived independently), the missing `_links` traversal exists and answers against raw SQL, and **zero action tools served — asserted by a permanent gate**, not a one-shot transcript. Registration iterates `schema.objects` only, so serving an action is structurally impossible in this server. Blind-spot residual recorded honestly: a *different* server iterating `schema.actions` passes `observe-door` green — the sixth boundary-drawing attempt to fall; close remains debt #22, decision lands in WO-105. Record: [`evidence/wo-104/VERIFICATION.md`](evidence/wo-104/VERIFICATION.md) | builder (Cursor `composer-2.5`) |
| [WO-105](WO-105.md) | Action tools and the two gates (P3 rung 2 of 3) | **done** — verified + merged 2026-07-26 after 1 rework round; 15 gates cold, `GATE_RUNNER_EXIT=0`. **GATE 1 now exists** — before this rung `execute()`, the sole write path, validated nothing about input shape (doctrine A5's exact failure: schemas published to MCP and enforced nowhere). One strict parse site at `execute.ts:122`, sited before the creation/transition fork so both branches are covered. **24 action tools served with `qf_observe_ticket` structurally absent** — flag-driven via `operatorOnly`, never name-matched, the seventh boundary attempt and the first not resting on a string match; verified by enumerating the registration, not by reading the harness. Counts proven derived, not asserted (same assertion emits `97/72/25` on a fixture schema vs `93/69/24` on the real one); `golden/` regenerates byte-identical, `tools.json` holds at 94 against a served plane of 93. **Rework caught 2 blockers the builder reported green**, the severe one invisible in every report: GATE 1 rejected `reason` on `fail_agent_session`, which three live app callers send from a boot path inside a rethrowing catch — **the app would have failed to start after any unclean shutdown**. Found by sweeping all 28 `kernelExecute` callsites; the first seat had deleted the field from the gate that *models* that function. Fix re-baited by hand: remove `reason` → `agent-path` exit 1 → restore → green. **Debt #24 decided by the founder 2026-07-26** (fix the advertisement; routed to WO-106 Ruling 1). **Post-merge review later found two more:** debt #25, `publish_artifact` is an arbitrary file-read primitive now served to agents (trigger FIRED → WO-106 D6/G7), and debt #26, GATE 1's strictness is top-level only so nested undeclared keys persist. Both re-measured and confirmed; the record was also corrected for one verifier overclaim. Record: [`evidence/wo-105/VERIFICATION.md`](evidence/wo-105/VERIFICATION.md) | builder (Cursor `composer-2.5`) |
| [WO-106](WO-106.md) | The cold seat, and retirement (P3 rung 3 of 3) | **done** — verified + merged 2026-07-26, **zero rework rounds**; 19 gates cold, `GATE_RUNNER_EXIT=0` (15 → 19), kernel suite 28 → 30. **The write catalogue stopped lying about itself** — measured with the checking seat's own MCP client: **0 of 24 action tools advertise zero properties** (was 24 of 24), `qf_start_run` now carries `required:["run_id"]`; served total unchanged at 93, `golden/` holds at 94 with `qf_observe_ticket` generated-but-unserved. Ruling 1 held: action transport is still `passthrough()`, so MCP did not become a second validator. Hand-written read verbs deleted across 16 files; the renderer's IPC method name correctly survives. **D5 completed during verification with a real model** — same prompt, no tool named: **4 attempts and a dropped field before, 1 attempt and `params` intact after.** **The rung's most important result is a finding against the architect:** Ruling 3 predicted "every gate would stay green", and the gate written to catch it *modelled* the boot path instead of watching it — the builder proved it vacuous by editing real app code and watching all 19 pass, then rewired it to the production callsite; re-baited independently by the checking seat (`limit: null` → `100` ⇒ FAIL exit 1, naming the file). Three of five builder-found defects were the same one-source-two-sides shape. **D6 split to [`WO-106b`](WO-106b.md)** — it was added after the adversarial read, was the only unreviewed deliverable, and was wrong in three measured ways. Record: [`evidence/wo-106/VERIFICATION.md`](evidence/wo-106/VERIFICATION.md) | builder (Cursor) |
| [WO-106b](WO-106b.md) | Close the `publish_artifact` file-read surface | **done** — verified + merged 2026-07-27 at `2730a00`, **zero rework rounds**. 20 gates cold, `GATE_RUNNER_EXIT=0` (19 → 20, `publish-artifact-root` added). **ROADMAP debt #25 closed.** The confinement was falsified by hand against every escape shape with real files on disk — legitimate-inside accepted; prefix-sibling, `..` traversal, absolute-outside, symlink-inside-resolving-outside and non-existent all rejected. Implementation uses `relative(root, resolved)` with `..`/absolute checks — **boundary-safe, never a bare `startsWith`** — with `realpathSync.native` on both sides. **Fail-closed binds BOTH doors**, which was the pre-build read's High finding: root configured → 24 tools, path check rejects; root absent → 23 tools, not listed and not callable. That mattered because `tools/list` is served by `installToolsListHandler` (`discovery.ts`) from an independent path that never consults the registry — a list-only filter would have passed a weaker gate with the hole wide open. `packages/qf-kernel` untouched, so the app callers, the founder's file-picker, `agent-path` and the Hermes smoke script are unaffected. Record: [`evidence/wo-106b/VERIFICATION.md`](evidence/wo-106b/VERIFICATION.md) | builder (Cursor) |
| [WO-V1](WO-V1.md) | The reading vault — Kernel → Obsidian projection (**off-ladder**) | **done** — verified + merged 2026-07-27 at `eaa5fa6`, two rework rounds. Cold `GATE_RUNNER_EXIT=0`; missing-type skip + real body/wikilink observation; `kernel-one-path` allowlist for vault gate. Thermo PASS (gate relocate under `qa/` is carry-forward). Record: [`evidence/wo-V1/VERIFICATION.md`](evidence/wo-V1/VERIFICATION.md) | builder |
| [WO-K1](WO-K1.md) | One Kernel path, and they take turns (identity rung 1 of 3) | **done** — verified + merged 2026-07-27 at `20488f8`, **zero rework rounds**. 21 gates cold, `GATE_RUNNER_EXIT=0` (`kernel-one-path` added). Platform Kernel at `~/.quantflow/kernel.db` (26 tables, WAL); four out-of-repo pins stripped; three peer-bus seats Connected again. G1 bait red→green; G2 control fails at `busy_timeout=0`; G4 child `provenance=default` under sandboxed HOME. Debt #29 **half-closed** (one path); bytes/orphan remain for WO-K3. Record: [`evidence/wo-k1/VERIFICATION.md`](evidence/wo-k1/VERIFICATION.md) | builder |
| [WO-K2](WO-K2.md) | The gate can see the door, and readers are readers (identity rung 2 of 3) | **done** — verified + merged 2026-07-27 at `c9c3bf0`, **zero rework rounds**. 21 gates cold, `GATE_RUNNER_EXIT=0`. Debt **#28 closed**. Per-claim allowlists; create opt-in; G1b open∩¬write; falsify through scanner; G3b production create-ban. Record: [`evidence/wo-k2/VERIFICATION.md`](evidence/wo-k2/VERIFICATION.md) | builder |
| [WO-CI1](WO-CI1.md) | Restore the production build verifier (off-ladder repair) | **done** — independently verified + merged 2026-07-28 after one architect rework. Package exports resolve directly; one canonical cold command now owns install → 258 unit tests → production build → every QA gate. Six baits red→green; debt #31 closed. Record: [`evidence/wo-ci1/REWORK-ROUND-1.md`](evidence/wo-ci1/REWORK-ROUND-1.md) | builder + verifier (Cursor Composer 2.5) |
| [WO-K3](WO-K3.md) | Bytes follow truth, and drift refuses writes (identity rung 3 of 3) | **done** — independently verified at `f525c3d` and merged 2026-07-29 after two rework rounds. The platform Kernel and new artifact bytes share `~/.quantflow/`; writable object-registry drift fails closed while readonly projections warn. The production writer, resolver, confinement, and cold shipped-form verifier were independently falsified and restored. Verification record is appended to the order | builder (Cursor Composer 2.5) + verifier |
| [WO-K3-COLD](WO-K3-COLD.md) | K3 gates install the local package that owns their imports (off-ladder integration repair) | **done** — direct gate dependency candidate `badb2c3` rejected by fresh-worktree measurement; launcher repair `80eb866` passed independently in both cold execution orders. Exact install-plan path baits and original semantic baits red→green; no manifest/lock/runtime behavior change | builder (Cursor Composer 2.5) + verifier |
| [WO-CI2](WO-CI2.md) | The shipped app contains its required runtime files (off-ladder release repair) | **done** — independently verified + merged 2026-07-30 at b1dd8f7. Two rejected implementation lines never merged. Two pristine worktrees proved standalone package closure and the complete install → unit → build → package → QA release path. The unsigned Linux package contains qf-toolloop + four Hermes files; removing Hermes fails by unresolved reference. Merge-risk review also narrowed cleanup to owned outputs and bound receipts to the exact Builder log | builder (Cursor Composer 2.5) + independent verifier/reviewer |
| [WO-D1](WO-D1.md) | Every Dock profile has a Kernel identity | **done** — independently verified at `4617e06` and merged 2026-07-30; zero semantic rework rounds after two cold integration repairs. Distinct profiles sharing one runtime retain exact session identity; the founder's pre-D1 Kernel upgrades without guessed historical links; the Linux ASAR contains and byte-matches all three SQL authorities. Record: [`evidence/wo-d1/VERIFICATION.md`](evidence/wo-d1/VERIFICATION.md) | builder (Cursor Composer 2.5) + independent verifier |
| [WO-D2](WO-D2.md) | One Dock Catalog, one definition-driven launch path | **done** — independently verified at `457446a` and merged 2026-07-30. Four package-owned defaults bootstrap idempotently; exact definition/profile argv and `spawned_from` links hold; Peer Seats is deleted; authorized PTY delivery and native failure cleanup are bait-proven in the real Linux package. Record: [`evidence/wo-d2/VERIFICATION.md`](evidence/wo-d2/VERIFICATION.md) | builder + independent verifier |
| [WO-D2b](WO-D2b.md) | Compensate the remaining ACP launch paths | **done** — independently verified at `3970ea1` and merged 2026-07-30 after one integration-gate rework. Host ACP and AgentOS now share exact-owner compensation; cold package plus all QA passed. Record: [`evidence/wo-d2b/VERIFICATION.md`](evidence/wo-d2b/VERIFICATION.md) | builder + independent verifier |
| [WO-K3b](WO-K3b.md) | Every production artifact writer uses the canonical shelf | **done** — independently verified at `1203ff2` and merged 2026-07-30. A2A and agent reports use the canonical root; the gate exhaustively permits exactly those two production publishers. Record: [`evidence/wo-k3b/VERIFICATION.md`](evidence/wo-k3b/VERIFICATION.md) | builder + independent verifier |
| [WO-N1](WO-N1.md) | Product identity: QuantFlow, not Collaborator | **done** — independently verified + merged 2026-07-30 at `1aaa82b`; cold release/package PASS, three production baits red→green, widened Electron migration probe safe. Debt #30 closed. Record: [`evidence/wo-n1/VERIFICATION.md`](evidence/wo-n1/VERIFICATION.md) | builder + independent verifier |
| [WO-107b](WO-107b.md) | Market-plane bulk ingest | **open — current** — one atomic replay-safe `execute()` command for instrument/quote rows, generated `0002` upgrade, provenance, derived quote links, and public write-surface closure | builder |
| WO-107…WO-111 | Real market · the loop · mechanical review · the one-shot proof | contracts only, promoted one verified rung ahead | — |
