# Work orders

Work happens **only** through a work order. Builders: read `/START_HERE.md`, then your order, then nothing else. Format and protocol live in the founder's vault (`Workshop Protocol`); orders graduate into this directory from WO-002 onward.

## Log

| Order | Title | Status | Assignee |
|---|---|---|---|
| WO-000 | Cut the fork (vault) | done — this repo is the result | fable |
| WO-001 | Codegen spike `qf-kernel-schema` (vault) | building | builder |
