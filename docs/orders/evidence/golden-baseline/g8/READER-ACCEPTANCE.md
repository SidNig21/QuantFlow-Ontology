# G8 semantic Reader acceptance

status: **READER ROUND 1 NO / NO — EIGHT FINITE ORDER DEFECTS; BUILDER CLOSED**
order: `docs/orders/WO-GOLDEN-G8.md`
reader_task: `01a04716-ef0f-72d1-aaa1-be295596f893`
reader_round: `1`
reader_authority: `00490e8284ef923ccbc12bb60583d744295d7fe0`
reader_verdict: **NO / NO**
builder_authority: **CLOSED**
same_reader_reread: **REQUIRED AFTER THIS ORDER-ONLY AMENDMENT**

## Reader answers

1. **NO.** The order did not yet make all eight semantic defects fail-capable
   with finite evidence and exact restoration.
2. **NO.** The order did not yet give every candidate, support-write door,
   packaged identity, saved-state seam, and lifecycle declaration one finite
   meaning with an executable falsifier.

## Eight finite defects recorded by the Reader

1. **The 13 kernel-one-path rows were not individually dispositioned.** The
   order named the paths but lacked one exact table row per offender stating
   whether it was a real violation, child-process-isolated `QF_KERNEL_DB`
   setup, read-only QA oracle, disposable fixture, or deliberate bait; it also
   lacked row-specific allowed repair and red/restore-green evidence.

2. **The kernel-one-path bait could be detector-missed.** A red result did not
   require `caught===true`, `result.ok===false`, exact bait/path identity, and
   verified bait/process/root cleanup before the restored green run.

3. **Law-B support-write doors were not fully classified.** The order did not
   enumerate the `qf_review_*` mutations in
   `packages/qf-kernel/src/governed-review.ts` and the Main wrappers/callers in
   `collab-electron/src/main/kernel.ts`, distinguish permitted Kernel-owned
   action support from required repair, or provide a direct bypass falsifier.
   The independent-Evaluation-lineage-only Report rule and G9 boundary needed
   to remain explicit.

4. **Generated internal command declarations were not joined to runtime.**
   The order did not require a derived handler map or exact completeness test
   comparing generated `internalCommands` with both
   `INTERNAL_TASK_ACTIONS` and `INTERNAL_APP_ACTIONS`, nor a missing-handler
   bait that names the missing action.

5. **The packaged result proof had no isolated live modes.** It did not require
   a live packaged `missing-result-observation` mode that suppresses the real
   Director notification, nor a live `worker-complete-is-result` mode that
   substitutes worker completion. In-memory or hard-coded receipt fixtures
   could therefore masquerade as proof.

6. **The packaged receipt identity was under-bound.** The order did not require
   exact Director PTY identity together with `to_role` and `to_session_id`; a
   merged `captureFor()` terminal PTY or arbitrary result row could be used as
   evidence.

7. **Malformed activation restoration was incomplete.** The three malformed
   activation modes did not each require an exact post-restoration exit-0
   receipt. A pending restoration was therefore left capable of being called
   acceptance.

8. **The 89 lifecycle declarations had no executable semantic invariant.**
   Generated byte equality alone did not enumerate the exact declarations,
   assert every lifecycle remained `experimental`, or prove a one-declaration
   promotion bait went red and restored green.

## Amendment disposition

This Reader receipt is historical authority for the eight defects only. The
order-only amendment adds the exhaustive K1 disposition table, detector-catch
fields, Law-B door inventory and bypass, generated-command/runtime join,
packaged live identity/modes, finite saved-state predecessor matrix, exact
malformed restoration receipts, and executable 89-declaration lifecycle
invariant. No product, test, or gate code is changed by this receipt, no G8
Builder authority opens, G9/G10/G11/G12 remain outside scope, and R18 remains
frozen. The same Reader must re-read the amended order before any Builder door
can open.
