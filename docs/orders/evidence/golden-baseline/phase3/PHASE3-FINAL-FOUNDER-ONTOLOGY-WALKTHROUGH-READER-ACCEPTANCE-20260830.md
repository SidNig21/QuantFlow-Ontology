# Phase 3 final founder Ontology walkthrough — Reader acceptance

status: **YES / YES**

authority commit: `d7936f96662405e1bb7b5adee7dc6a2950027632`

Reader task: `/root/final_founder_receipt_reader`

## Verdict

1. **Fail-capable: YES.** The bounded receipt/gate/test surface can independently falsify exact product identity, one UI-operated user turn, five ordered provider rows, four ordered governed tool calls, exact Task/session/link identities, four Artifact hashes, final answer, close/reopen truth, two cleanup checkpoints, and the updater observation without another provider call.
2. **One meaning: YES.** The claim is only that Ryan operated the rendered packaged app, created one governed standalone Task, and one real Hermes/Kimi turn read that Task through four existing Ontology tools before answering visibly. It does not claim Mission research or any R18 capability.

## Adjudications

- Five `opencode-go` / `kimi-k3` API calls are the expected shape of one tool-using user turn: four tool steps plus one final answer. They are not unrelated retries. The one `Turn ended` row must bind `api_calls=5`, `tool_turns=4`, and `finish_reason=stop` to the same Hermes runtime session.
- The visible updater HTTP 406 is an honest, non-blocking unavailable release channel for the unsigned internal package. G12 excluded live GitHub release publication/download/install. The receipt must preserve the visible `Update failed — retry`, exact 406 cause, and classification; hiding it must RED.
- The authorized proof-only surface is sufficient. No product edit or second provider call is required.
- There is no R18 expansion: no Mission, Dataset, Technique, Run, Evaluation, Report, market acquisition, or autonomous research capability is claimed.

## Builder precision

- Retain the earlier nonce receipt byte-for-byte as historical evidence.
- Bind the packaged product identity to `a91b5deec9bb49538136c76a031648be2c08e05e` / tree `e7213a4420f109a8f71bd38970f570a7b4b6295e`.
- Prove any proof-only descendant by exact product-tree equivalence.
- Do not require `RELEASE-STATUS.build.commit_sha` to equal a later receipt-only HEAD. The tested package is `a91b5dee...`; later commits may contain only the authorized receipt/gate/test/evidence delta.
