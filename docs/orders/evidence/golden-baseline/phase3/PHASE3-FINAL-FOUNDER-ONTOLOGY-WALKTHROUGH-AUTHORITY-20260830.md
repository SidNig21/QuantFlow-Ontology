# Phase 3 final founder Ontology walkthrough authority — 2026-08-30

status: **FOUNDER AUTHORIZED / OBSERVED ON EXACT PRODUCT CANDIDATE / RECEIPT READER REQUIRED**

## Plain language

Golden must finish by proving that Ryan can use the real packaged QuantFlow app to give a real Hermes Research Director governed work, and that Hermes can read that exact work from QuantFlow's Ontology through the connected tools before answering visibly in the same tile.

## Bound identity

- product candidate: `a91b5deec9bb49538136c76a031648be2c08e05e`
- product tree: `e7213a4420f109a8f71bd38970f570a7b4b6295e`
- packaged identity shown in the rendered app: `A91B5DEEC9BB49538136C76A031648BE2C08E05E`
- package timestamp shown in the rendered app: `2026-08-31T03:39:01.739Z`
- isolated run root: `C:\tmp\qf-golden-founder-a91b5dee-20260830`
- founder Task: `Golden Founder Ontology Walkthrough`
- Task id: `task-5d9e9b6a-26cc-4059-914c-d4f479a47731`
- AgentSession id: `cafcad5c-fe94-4055-ac6c-2375c33b004a`
- Hermes runtime session: `20260830_204733_f93524`

The run used no `QF_UI_PROOF`, `QF_HERMES_SYNTHETIC_TEST`, or `QF_DOCK_QA_MODE` flag. Founder state was not touched.

## Exact accepted founder route

Router-owned Computer Use was the user. Product-semantic actions occurred only through the rendered packaged application:

1. Open the packaged QuantFlow executable in a visible Windows window.
2. Click `TIDY`, open Dock `CATALOG`, and pointer-click the real `Research Director` launch control.
3. Observe one real participant tile and Hermes readiness; click `TIDY` again.
4. Resize the tile with its visible southeast handle until the full Hermes TUI is usable.
5. Click `Create Task`, enter the exact title and completion description, select Research Director, and click `Create`.
6. Select the rendered Task and inspect its exact title, `open` status, assignee, delegator, and relationships.
7. Focus the real rendered Hermes prompt, type the frozen Ontology query, and press Enter.
8. Observe the real response and `QF_GOLDEN_ONTOLOGY_OK` in the same tile.
9. Close normally, prove owned process/socket/PID zero, reopen the same isolated world, inspect truthful stopped/history state, close normally again, and prove owned process/socket/PID zero.

No RPC, `qf.dock.spawn`, direct PTY write, direct Hermes invocation, direct OpenCode call, synthetic actor, manually fabricated Kernel row, or QA-only spawn path satisfied a user action.

## Frozen prompt

`Use the connected QuantFlow MCP/Ontology tools to inspect the Task named "Golden Founder Ontology Walkthrough". Do not answer from assumptions or terminal context. Read the governed QuantFlow state. Tell me the Task's title, current status, and the participant/role it is assigned to. End your response with QF_GOLDEN_ONTOLOGY_OK.`

## Required semantic proof

The accepted receipt must prove one intended successful user turn, not one provider HTTP call. A tool-using turn may contain multiple sequential provider calls. This walkthrough contained exactly five successful `opencode-go` / `kimi-k3` API rows because Kimi made exactly four governed tool calls before its final answer:

1. `mcp_quantflow_ontology_qf_task_query`
2. `mcp_quantflow_ontology_qf_task_links`
3. `mcp_quantflow_ontology_qf_agent_session_links`
4. `mcp_quantflow_ontology_qf_agent_definition_query`

The single successful `Turn ended` row must bind `api_calls=5`, `tool_turns=4`, the same Hermes runtime session, `finish_reason=stop`, and the final response. Every API row must use provider `opencode-go`, model `kimi-k3`, positive latency, and nonzero input/output/total tokens. There may be no fallback provider/model, synthetic responder, unrelated retry, second user turn, credential exposure, or fabricated tool result. `provider_request_id=not_exposed` remains the truthful upstream limitation.

This strengthens P14-B from a nonce echo to a real governed read. It does not authorize R18 research behavior.

## Exact Ontology truth

Read-only Kernel evidence must prove:

- one exact Task with the bound id, title, description, and status `open`;
- exactly one `assigned_to` and one `delegated_by` link from that Task to the bound AgentSession;
- exactly one `spawned_from` link from that AgentSession to `hermes-research-director`;
- exactly four `produces` links from that AgentSession to the four immutable Ontology-call Artifacts;
- every Artifact's tool, arguments, result, role, session id, created time, nonce, bytes, and SHA-256;
- the final answer agrees with those exact rows and ends with `QF_GOLDEN_ONTOLOGY_OK`.

The UI is corroboration, the Kernel is semantic truth, the isolated Hermes session/log is execution truth, and the immutable Ontology-call Artifacts are tool-call truth. None may independently satisfy the complete claim.

## Close/reopen truth

The accepted receipt must record:

- first normal shutdown: QuantFlow/Hermes/OpenCode processes `0`, socket absent, PTY PID absent;
- reopen against the same isolated root: live `0`, closed `1`; participant session closed, runtime stopped, work blocked, recovery restartable; exact Task title remains visible; History exposes the same participant; Inspect exposes the same participant and Task identity;
- Kernel AgentSession status after stale-session reconciliation: `failed`, surfaced honestly by History while the tile's derived runtime state is `stopped`;
- second normal shutdown: QuantFlow/Hermes/OpenCode processes `0`, socket absent, PTY PID absent.

`output Not recorded` and `Mission binding Not recorded` are truthful for this bounded standalone Task and may not be reclassified as missing Task truth.

## Visible observation to classify, not hide

On reopen the unsigned internal package's automatic GitHub update check rendered `Update failed — retry`. The log bound this to HTTP `406` while resolving `/releases/latest`; it did not affect Dock, Canvas, Kernel, Hermes, persistence, or shutdown. The final Reader must classify whether this is an honest unavailable release channel for the internal unsigned package or a Phase-3 blocker. It may not be omitted from the receipt.

## Authorized evidence/proof surface after Reader YES / YES

Only these surfaces may change:

- one credential-safe final founder walkthrough receipt under this evidence directory;
- `qa/gates/hermes-production-inference-receipt.ts`;
- `qa/gates/hermes-production-inference-receipt.test.ts`;
- directly caused Phase-3 evidence and `NEXT.md` rotation;
- generated Atlas only if the proof-only change legitimately affects it.

No product, renderer, Kernel, schema, generated ontology, MCP bridge, Hermes runtime, provider configuration, package configuration, dependency, R18, bet, or trade change is authorized.

The receipt gate must retain the immutable earlier nonce inference as historical evidence while binding current product-tree equivalence to `a91b5dee...` and validating this stronger current-byte founder proof. It must add fail-capable mutations for wrong tool order, wrong exact Task/session ids, missing Artifact hash, wrong API cardinality, zero tokens/latency, fallback provider/model, second user turn, wrong final answer, missing close/reopen truth, and hidden updater observation.

## Next steps and stop

One fresh semantic Reader answers:

1. Can the strengthened receipt actually fail every required conjunct without a second provider call?
2. Does it have exactly one meaning: one real founder-operated packaged QuantFlow turn in which real Hermes/Kimi reads one real Kernel Task through the governed Ontology tool chain, persists truth across reopen, and shuts down cleanly without implying R18?

After `YES / YES`, one proof-only Builder may write the receipt and bounded receipt gate/test. One fresh independent final Verifier then runs the current canonical Phase-3 qualification against the immutable final candidate. Golden designation follows only after PASS, and execution stops before R18.
