# Phase 3 selected cable-label placement authority — 2026-08-30

The selected hypothesis-to-Evaluation cable label can knowingly fall back across an unrelated worker-evidence Artifact, so only deterministic collision-free selected-label placement may be repaired.

status: **FRESH SEMANTIC READER YES / YES / TWO-FILE PRODUCT REPAIR OPEN**

- Reader task: `01a05212-f0e4-7101-a0d9-8e59df8a3f08`
- Reader adjudication: **YES / YES — real Phase-3 C14 product defect owned by cable-overlay label placement**
- authority base commit: `f0d0634868b5b89e8b58f6600c2dff8cb129552a`
- authority base tree: `197f1b643469aca71ebb5997cc2da69a9da455a9`
- added product path: `collab-electron/src/windows/shell/src/cable-overlay.js`
- added focused test path: `collab-electron/src/windows/shell/src/cable-overlay.test.ts`

Scope reason: the selected hypothesis→Evaluation cable path and selection semantics are correct, but endpoint-midpoint/fixed-offset label placement includes endpoint rectangles in its collision decision and can choose a known overlap with an unrelated worker-evidence Artifact. The owner is the cable-overlay renderer and its existing focused test, not research-world membership, the pre-R18 gate, Canvas layout, Kernel truth, or the oracle.

## Preserved existing dirty set

This authority task does not touch or stage the six already-authorized dirty paths. Their identities before this authority commit are:

| Unstaged path | Working blob | Per-file binary-diff hash |
| --- | --- | --- |
| `collab-electron/src/windows/shell/src/research-world.js` | `9202eabb03d9ace550627e5324505c90016435fe` | `652c5a9fb3306a55d0c358b456310e3eb8210f49` |
| `collab-electron/src/windows/shell/src/research-world.test.ts` | `170983b0ff71ce5de2adacb6489c69611e4b6979` | `d4a577375c81bc58b8099e69facf6effbf42b283` |
| `qa/gates/pre-r18-coherence.ts` | `42ca268bb96f4e198f0aa06f88b33f1594341b91` | `3ddfcbc8f431542057e0a6ad2ccb6fa36d9e2235` |
| `qf-atlas/ATLAS.md` | `4e05cc86392d7326b006632dc08bdc39b1014354` | `cd1eaa550216b0de257b738724bf0bd2ae9d3dcb` |
| `qf-atlas/atlas.html` | `f5f7c068de450a808fa2224a7691628608463e74` | `af1687c72eb71e7c5f82e2866428d630a9ea3af8` |
| `qf-atlas/atlas.json` | `285ec3c902b9fef4b9dc470428a906abcc30bc24` | `dad52614ea6a466026a492eb6c533f380a94c55d` |

The Builder may resume those paths under their existing authority and add only the two cable-overlay paths named above.

## Exact deterministic placement contract

For a selected cable label, sample the exact rendered SVG path by arc length using the fixed fraction order `[0.50, 0.375, 0.625, 0.25, 0.75]`. At each sampled point, try the existing label-offset candidates in their existing fixed order. Candidate enumeration is deterministic: fraction order first, existing offset order second.

Collision testing excludes only the two exact endpoint tile rectangles identified by the selected cable's `from_ref` and `to_ref`. Every other visible tile rectangle is unrelated for placement purposes, including the worker-evidence Artifact. Choose the first candidate whose label rectangle overlaps no unrelated visible tile.

If every candidate overlaps, use the first candidate in that same fixed enumeration as the deterministic fallback. The fallback may be visibly imperfect but must never depend on DOM iteration races, random choice, time, or a different cable/path computation.

The test must reproduce the selected hypothesis→Evaluation case whose old endpoint-midpoint/fixed-offset fallback overlaps the unrelated worker-evidence Artifact, prove it RED, then prove the deterministic candidate search selects the first non-overlapping candidate. It must also prove endpoint rectangles alone are excluded, an unrelated tile at an earlier candidate causes the next candidate to be chosen, and the all-candidates-blocked fallback is stable across repeated runs.

## Preserved C14 meaning and stops

Cable SVG path geometry, endpoints, object/link identities, direction, kind, selected state, hit target, Inspect behavior, overview/LOCAL/FULL semantics, exact current 17 objects, exact settled LOCAL 21 links, primary 12-cable overview, opacity semantics, and every existing C14 assertion remain unchanged. This is label placement only.

No product layout, tile geometry, research-world membership, gate assertion, oracle, Kernel, Canvas, Dock, IPC, lifecycle, fixture, timing, opacity, or topology change is authorized. No hiding/removing the label, shrinking its collision box to evade proof, excluding the worker-evidence Artifact, treating every world tile as an endpoint, or global cable reroute is permitted.

P14-A parser/selection mutation remains closed and measurement pending; P15 is not repaired or closed here. P18/candidate freeze, independent Verifier acceptance, Golden designation, `main`, every remote ref, and R18 remain closed. No candidate is permitted until P01-P17 are green. Any third new executable path or semantic expansion stops for new authority.
