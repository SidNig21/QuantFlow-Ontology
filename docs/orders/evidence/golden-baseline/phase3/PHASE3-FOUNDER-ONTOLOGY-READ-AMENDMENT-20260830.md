# Phase 3 founder Ontology read amendment — 2026-08-30

status: **FRESH READER YES / YES — ONE BOUNDED CURRENT-PRODUCT REPAIR**

- reviewed execution source: `bff1056843451b384765733e1ea065c2b2b910f4`
- reviewed execution tree: `4ee183e60cac7e19b4dfbe4d565f54f06e0c37ad`
- observed RED: [RED-01-TASK-QUERY-MISSING.md](founder-walkthrough/RED-01-TASK-QUERY-MISSING.md)
- active order: [WO-GOLDEN-PHASE3.md](../../WO-GOLDEN-PHASE3.md)

## Product defect

The exact packaged current product admitted a real Research Director and connected it to the real QuantFlow ontology gateway. The Director could query AgentDefinitions, but the orchestrator role filter omitted the existing generated reads required to inspect a Task that QuantFlow had just created through its own UI. A real Hermes / OpenCode Go / Kimi K3 turn therefore truthfully refused to fabricate Task state.

This is a Golden convergence defect in the current product. It is not R18 research acquisition, recall, autonomy, learning, schema expansion, or visual redesign.

## Exact authorized product surface

The Builder may edit only:

- `collab-electron/src/main/ontology-role-tools.ts`
- `collab-electron/src/main/ontology-gateway.test.ts`
- `qa/gates/windows-dock-ontology.ts`
- directly caused Phase-3 proof evidence
- generated Atlas outputs only when regeneration changes them

The orchestrator roster may add exactly these existing generated read tools:

- `qf_task_query`
- `qf_task_links`
- `qf_agent_session_links`

The already-exposed `qf_agent_definition_query` remains unchanged. Together they form the finite read chain:

```text
qf_task_query(title)
-> exact Task id, title, status
qf_task_links(Task id)
-> exact assigned_to AgentSession
qf_agent_session_links(AgentSession id)
-> exact spawned_from AgentDefinition
qf_agent_definition_query(...)
-> exact definition role and founder-facing identity
```

## Preserved boundaries

No schema, generated ontology definition, Kernel, `execute()`, SQLite helper, MCP bridge, Hermes prompt, capability group, dependency, package configuration, Dock, Canvas, Task creation, UI behavior, P14-B gate, provider contract, or R18 functionality may change.

No new truth store, direct SQL, write action, Task mutation, query architecture, synthetic response, fixture answer, label inference, expected-value substitution, or foreign-Kernel access is authorized.

## Required proof

Focused source and packaged proofs must establish:

1. The orchestrator receives exactly its original three tools plus the three named reads.
2. Critic and market-worker rosters retain their prior authority; a market worker cannot acquire the Director's desk reads.
3. Wrong session, role, live-seat capability, or Kernel path refuses before result or trajectory mutation.
4. One exact live Director and one Task assigned to it produce exactly one title match, exact independent Kernel-oracle title/status, exactly one `assigned_to` edge, exactly one `spawned_from` edge, and exact AgentDefinition role `orchestrator` / display identity `Research Director`.
5. Every successful read creates a distinct Kernel-owned trajectory Artifact linked by `produces` from the exact Director session and recording exact tool, arguments, result, session id, and role.
6. The packaged bridge lists and can invoke all three named additions.
7. `create_task` and every unrelated write remain unexposed.

Required independent RED cases include removing each required tool, zero/multiple title matches, missing/reversed/duplicate/misdirected assignment or spawn lineage, wrong definition role, wrong live-seat identity or capability, foreign Kernel, missing/misbound trajectory, and a packaged roster missing any named tool. Exact restoration must return GREEN.

After an immutable product candidate is independently accepted, Router repeats the exact real packaged founder journey with Computer Use. The final response must come from actual QuantFlow Task and lineage reads through the real Research Director and real Hermes / OpenCode Go / Kimi K3 path. A marker without those trajectory records remains RED.

No additional provider call is permitted before the provider-free product and packaged proofs are green.

