# Phase-3 P14 current-floor decision

In plain terms: Phase 3 must prove every research capability that exists today without pretending the still-missing live end-to-end composition already exists.

status: **ACCEPTED AUTHORITY AMENDMENT / BUILDER CLOSED PENDING READER YES / YES**
execution-floor: descendant of `9a0e6359` or exact later current source authorized by `WO-GOLDEN-PHASE3.md`
r18: **FROZEN / PENDING / NOT IMPLEMENTED BY PHASE 3**

P14 is four independent mandatory proofs: successful fresh production Bovada NFL capture plus a separate scheduled-within-24-hours capture cancelled by normal app shutdown with RPC receipt and zero market/source delta; fresh real packaged default-profile Hermes Director execution with Kernel AgentDefinition/AgentSession/lifecycle truth and a separately hashed external terminal transcript; fresh accepted R17 guided deterministic sample Technique→Dataset→Run→Artifact→independent Evaluation→one current Report plus reopen; and a read-only capability preflight that classifies `R18 seam unavailable` in Verifier evidence with exact zero downstream delta.

No accepted G12/R17 receipt substitutes for a fresh run. Malformed CLI/argument refusal is not the P14-A failure control. No sample is called current/live. No provider/model identity is accepted from model self-report; absent trusted Hermes runtime metadata it is exactly `provider-model-identity-unprovable`. The external transcript is execution evidence, not Kernel truth, and no durable turn object is claimed. No fixture is live evidence. No missing Technique identity or fabricated coverage passes. No credential is read or recorded, and no bet or trade is placed. P14-D is Verifier evidence, not a visible product refusal or shipped feature; it keeps R18 open and creates no Dataset, Run, result/report Artifact, Evaluation, or Report.

The four proofs are independently runnable and preservable, but not independently acceptable: a RED stops only that unit while the other three continue, and absolutely blocks P18/candidate/Golden until all four are green. For a P14-A admission RED, transport facts come only from the original production process/error receipt and reached selection error code: status `200`, approved final origin, and JSON media type. The no-refetch stored-Artifact analyzer binds body hash/bytes, available Artifact/Kernel timestamps, taxonomy counts, sequential predicate rejection/reach counts, timestamp range, eligible count, and the true source Artifact delta. It never claims to measure transport metadata and never emits raw payload, teams, competitors, odds, headers, or names. Source reachability alone is not admission and does not reclassify P14-A.
