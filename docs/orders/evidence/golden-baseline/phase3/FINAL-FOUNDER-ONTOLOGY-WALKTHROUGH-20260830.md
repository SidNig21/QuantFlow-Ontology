# Final founder Ontology walkthrough receipt — 2026-08-30

The real packaged QuantFlow app let Ryan's robot user create and inspect one governed Task, then the real Hermes Research Director used four existing Ontology tools through OpenCode Go / Kimi K3 to read that exact Task and answer visibly in the same tile.

- Tested packaged product: `a91b5deec9bb49538136c76a031648be2c08e05e` / tree `e7213a4420f109a8f71bd38970f570a7b4b6295e`.
- Full credential-safe machine receipt: [`FINAL-FOUNDER-ONTOLOGY-WALKTHROUGH-20260830.json`](FINAL-FOUNDER-ONTOLOGY-WALKTHROUGH-20260830.json).
- Visual authority: Router-owned Computer Use transcript for task `019ffb57-4d45-70a1-8cb7-30e99f79d348`. No screenshot file or hash is claimed because the current screenshots were not persisted.
- Execution authority: the isolated Hermes session and log hashes in the JSON receipt. Five provider calls are one user turn with four governed tool turns and one final response.
- Semantic authority: the isolated Kernel hash plus the exact Task, AgentSession, links, and immutable Ontology-call Artifact hashes in the JSON receipt.
- Persistence: normal close reached process/socket/PID zero; reopen showed the same Task and participant truthfully stopped/closed/restartable; the second normal close also reached zero.
- Honest visible limit: reopen rendered `Update failed — retry`; the log classified the `/releases/latest` response as HTTP 406. This is the unavailable release channel of the unsigned internal package, not a failure of Dock, Canvas, Kernel, Hermes, persistence, or shutdown.
- Scope: this proves one real governed standalone Task read. It does not claim the R18 Mission research workflow.

The earlier nonce receipt and its two screenshots remain immutable historical evidence. This stronger receipt adds the current-product Ontology read without rewriting those bytes.
