# Phase 3 current-Mission receipt argument authority — 2026-08-30

The preserved P13/P16 gate diff passes the superseded primary-ID list into the current-Mission receipt helper, so exactly one argument must be corrected before that already-authorized work resumes.

status: **MECHANICAL SAME-MEANING / PRESERVED ONE-FILE BUILDER MAY RESUME / NO NEW READER REQUIRED**

- Reader task confirming finite defect: `01a05212-f0e4-7101-a0d9-8e59df8a3f08`
- authority base commit: `b30e17626436bbde81c378338f452fed0bcdb06d`
- authority base tree: `9ec984dbc2b609584a430a62b013feb3f41b2023`
- preserved unstaged path: `qa/gates/pre-r18-coherence.ts`
- preserved working-file Git blob before this authority commit: `3ad250e4f109eb3ef8ef7791fb22216138a92ce8`
- preserved binary-diff Git hash before this authority commit: `2056e13c49f8ec98fbcfdbffb6178593822555fd`

This is a mechanical same-meaning proof-harness correction under standing Golden authority. The existing unstaged gate diff belongs to the stopped, already-accepted one-file view-state Builder and remains unstaged and byte-identical while this docs/evidence authority is committed. No semantic Reader is required for the single argument correction below.

## Exact additional edit

Inside the existing preserved diff in `qa/gates/pre-r18-coherence.ts`, change only the current-Mission receipt call from:

`defaultReceiptExpression(tileSpecs, expectedDefaultIds, expectedDefaultLinks, expectation.stages)`

to:

`defaultReceiptExpression(tileSpecs, expectedCurrentMissionIds, expectedDefaultLinks, expectation.stages)`

The Builder may then resume and complete the preserved one-file view-state diff under its accepted authority. No other new edit is authorized by this mechanical amendment.

## Literal proof and falsifiers

The current-Mission receipt must prove membership literally, not by count:

- painted object membership equals the exact 17 current-Mission IDs with no unknown extra and no missing ID;
- stage membership equals the exact 13 primary IDs;
- painted cable membership equals the exact 12 primary links;
- every historical-only ID/link is excluded;
- no `FULL_LINEAGE`-only object or link leaks into `CURRENT_MISSION`.

All five accepted navigation baits remain required: superseded `DEFAULT` check, skipped Mission navigation, Back treated as current Mission, wrong Mission, and full lineage without current Mission. Add fail-capable wrong-membership, wrong-cable, wrong-stage, and historical/full-lineage-leak baits. Each must RED on exact set equality; a count-only check, unknown extra that preserves count, filtered extra, or broadened membership is forbidden.

## Preserved authority and stops

No product, oracle, fixture data, identity, count, object/link membership, Canvas/Dock/Inspect behavior, timing, persistence, settlement, lifecycle, or navigation meaning changes. The only newly authorized byte change is the single argument above; all other bytes in the preserved diff remain governed by the accepted view-state amendment.

P14-A parser/selection mutation remains closed and measurement pending; P15 is not repaired or closed here. P18/candidate freeze, independent Verifier acceptance, Golden designation, `main`, every remote ref, and R18 remain closed. No candidate is permitted until P01-P17 are green. Any second path or different correction stops for new authority.
