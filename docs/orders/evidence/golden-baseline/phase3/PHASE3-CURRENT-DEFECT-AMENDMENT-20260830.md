# Phase 3 current-defect authority amendment — 2026-08-30

This amendment opens three finite repairs to the current Phase 3 reds while keeping the release candidate, Golden designation, and R18 closed.

status: **BUILDER OPEN FOR A/B/C ONLY / P14-A PARSER MUTATION CLOSED / NO CANDIDATE UNTIL P01-P17 GREEN**

reader-task: `01a05212-f0e4-7101-a0d9-8e59df8a3f08`
reader-verdict: **A / B / C authorized exactly as bounded below**

The accepted Phase-3 meaning, immutable G12 baseline, current execution evidence, and all existing acceptance assertions remain binding. `main`, every remote ref, accepted G1-G12 evidence, and R18 remain frozen. This authority does not designate a candidate and cannot be used to run P18 while any P01-P17 row is red.

## A — one isolated P14-A capture and safe tuple evidence

Because source Artifact `767b50d3ea7ecc588e94cb4bfa15e75457dcaf5bd45113d518ff126aa28255c4` was deleted by the already-recorded compliant cleanup, exactly one new isolated production P14-A capture is authorized. Before that new run is cleaned up, the no-refetch analyzer may emit only distinct path-node tuples for the coupon containing the exact NFL league. Each tuple contains only `type`, `id`, SHA-256 of `description` (never the description), cardinality, order/parent position, the bound execution-source SHA, and the fresh source Artifact content hash. It may emit no raw response, description text, names, teams/competitors, odds, headers, credentials, or independent transport claim.

The capture must retain the existing production process/error receipt, approved-origin/media-type boundary, exact source/object/link delta, separate scheduled normal-shutdown cancellation zero-delta control, isolated roots, and cleanup-zero proof. Tuple evidence is diagnostic only: it neither admits a market nor closes P14-A. Parser or selection mutation is **CLOSED pending tuple adjudication**. If the analyzer cannot satisfy the field allowlist or bind both identities, stop without emitting the unsafe evidence.

## B — mechanical same-meaning proof repairs

Only these files are editable for B:

- `qa/gates/technique-outcome-loop.ts`
- `qa/oracles/r17-technique-outcome.json`
- `qa/gates/pre-r18-coherence.ts`
- `qa/gates/founder-steering.ts`
- `qa/gates/founder-steering.test.ts`

The technique-outcome and pre-R18 consumer oracles must count the existing worker-evidence Artifact and executor-to-evidence `produces` link as distinct from the existing Run-result Artifact and Run-to-result link. Only honest expected totals and the corresponding same-meaning oracle entries may change. Conflating the two Artifacts, omitting either link, accepting an extra lineage object, or preserving the obsolete cardinality must RED.

The founder-steering gate must load the guided Dataset and select the exact admitted Technique through the existing UI before submit. It must preserve the ordinary form path, the coverage refusal, zero-delta wrong/missing-Technique cases, and the prohibition on a synthetic Task. B changes proof composition only; it does not change product behavior or accepted R17 semantics.

## C — bounded native-TUI lifecycle repair

Only these files are editable for C:

- `collab-electron/src/main/agent-host.ts`
- `collab-electron/src/main/host-native-tui.ts`, only if required by the same teardown path
- `collab-electron/src/main/agent-host-lifecycle.test.ts`

On successful normal native-TUI teardown, the host must use the existing Kernel terminal transition before `closeAppKernel`, so the durable session is terminal/closed. An undelivered-result refusal must not falsely close the session. Already-terminal, PTY-exit, cancel, and failure meanings remain unchanged. Reopening the same session ID must show terminal state with no live recipient, and final owned-process/root cleanup must be zero.

The focused lifecycle falsifier first makes normal teardown omit the terminal transition and must RED on reopen. A second bait makes an undelivered-result refusal close the session and must RED. Restore the repair and require the focused lifecycle test GREEN before packaged proof.

## Required execution order and stop

Run the focused B and C falsifiers before accepting their greens. Then run one fresh P14-C, P13/P16, and P15 from the final source, with their existing visible UI-to-Kernel, persistence, lineage, steering, refusal, and cleanup assertions unchanged. Run the single authorized P14-A capture/tuple diagnostic independently; P14-A remains RED/pending unless the already-required production admission succeeds, and no parser edit is authorized by this amendment.

Any required path outside the exact lists above, semantic reinterpretation, weakened/deleted assertion, synthetic Task, product Dataset-composition seam, credential handling, trade/bet behavior, or R18 work stops and returns to a fresh Reader. No P18 candidate freeze, independent Verifier acceptance, Golden descendant, `main`/remote mutation, or R18 activation is authorized until every P01-P17 row is green.
