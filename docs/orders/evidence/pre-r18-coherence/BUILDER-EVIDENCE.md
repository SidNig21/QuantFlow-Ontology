# Pre-R18 Coherence — Final Consumer Workflow Builder Evidence

## Candidate and authority

- Branch: `wo-pre-r18-coherence`
- Reader-approved authority/order SHA: `44f33995c42d2e94dbc22037cf9959c23b635f39`
- Product/proof candidate: `67f9ba42affcb30fa8bc0961808ed10d8541ea6f`
- Accepted R17 oracle remains `qa/oracles/r17-technique-outcome.json`: 16 objects and 20 exact links.
- No Kernel schema/action/link, ontology type, inventory, Canvas engine, truth store, R18 capability, release, installer, or packaging surface changed.

The candidate makes the existing current-Mission world readable without changing
its truth. DEFAULT is a five-stage Mission → Work → Evidence → Evaluation →
Current Report projection. LOCAL shows the exact bounded lineage and puts object
or relationship detail in Dock INSPECT. FULL deliberately exposes all 16 objects
and all 20 links. START and CATALOG share one launchable definition projection,
while all five Dock modes retain one-pane paint isolation.

## Changed files by defect

- Dock identity, START discovery, exact catalog projection, and pane isolation:
  `index.html`, `dock.js`, `dock.test.ts`, `shell.css`.
- Deterministic DEFAULT/LOCAL/FULL staging, human tile grammar, Dock INSPECT,
  participant refresh, current-report authority, and restoration:
  `research-world.js`, `research-world.test.ts`, `renderer.js`,
  `task-composition.js`, `task-composition.test.ts`.
- Subordinate, fit-aware cable routing and collision-aware labels:
  `cable-overlay.js`, `shell.css`.
- Real DOM/SVG C14 receipts and finite falsifier mutations:
  `qa/gates/pre-r18-coherence.ts`.
- Evidence-only files: the fourteen named WebP captures, this receipt, and
  `SCREENSHOT-MANIFEST.md`.

## Product and proof receipts

| Command/check | Exit and result |
|---|---|
| `bun install --frozen-lockfile` in `collab-electron` | `0` |
| Focused Dock/research-world/task-composition/cable-overlay tests | `0`; 26 pass, 0 fail, 186 expectations |
| `bunx tsc --noEmit` | `0` |
| Capture-disabled `bun qa/run.ts pre-r18-coherence` with capture/proof env unset | `0`; DEFAULT 13/13, LOCAL exact/dimmed, FULL 16/20, 16 object inspections, 20 link inspections, C01–C14 PASS, production boundaries PASS, clean shutdown |
| Capture-enabled `bun qa/run.ts pre-r18-coherence` | `0`; 14/14 frames at 1600×1000, 665,388 bytes total, FULL 16/20, clean shutdown |
| `repo-shape`, `lockfile-committed`, `kernel-sole-writer`, `no-canvas-domain-writes`, `kernel-sole-writer-app`, `doc-action-surface`, `one-skin`, `rung-ladder` | all final exits `0` |
| `bun qa/run.ts doc-links` | `0`; 73 live documents and every pointer resolves |
| `git diff --check` | `0` |

The live control emitted these production-boundary receipts:
`renderer_submission=PASS boundary=qf.research.submit_question`,
`preload_ipc=PASS boundary=qf:research-world:projection`,
`main_handler=PASS boundary=read-only projection handler`, and
`kernel_projection=PASS independent R17 literal oracle comparison`.
Its geometry receipt measured a 1200×1000 Canvas, 16 tiles, and 20 links; its
DEFAULT union ratios were width `0.8499922688802083` and height
`0.6387013549804688`.

## Falsification receipt

With `QF_PRE_R18_COHERENCE_FALSIFY` set independently to `C01` through
`C14`, every top-level invocation exited `1` with its own
`FALSIFY RED C##` marker.

The seven named C14 invocations used the same live journey and each exited `1`
after restoring every mutation:

- `C14/default-projection`: every applicable title, badge, status, and fact
  slot plus mission-action, text-size, stage-membership, stage-order,
  stage-bounds, tile-nonoverlap, primary-links, legacy-overlay,
  connection-overlay, and every painted selected-cable obstruction turned RED.
- `C14/local-lineage`: selected-subject, selected-tile, exact-local-set,
  local tile/cable opacity, unrelated tile/cable opacity,
  control-no-propagation, and inspect-match each turned RED.
- `C14/full-lineage`: objects-16, links-20, object-opacity,
  background-cable-opacity, object-select-inspect, link-select-inspect,
  endpoint-tolerance, unrelated-tile-obstruction, and painted-height each
  turned RED across the finite 16-object/20-link set.
- `C14/dock-isolation`: one-pane, inactive-zero-rects, mode-tabs, dock-title,
  director-role, mission-label, technique-label, team-count, team-roles,
  browse-catalog, empty-next-action, and no-start-inventory-rows each turned RED.
- `C14/model-complete`, `C14/back-to-world`, and
  `C14/history-authority` each printed its exact RED receipt and exited `1`.

No receipt is an unconditional string check: the matrices mutate the live
DOM/SVG receipt object, prove only the named key red, restore it, and continue.

## Superseded inherited gate

`bun qa/run.ts research-world-visible` remains byte-unchanged as ordered. Its
retry completed preflight timing and cleanup but exited `1`: it still requires
all 15 legacy cables in the initial view and then ten inline tile Inspect
controls. The Reader-approved amendment explicitly requires primary-only DEFAULT
(12 of those 15 links in that older fixture) and moves full identity/triples to
Dock INSPECT. Kernel/Main still returned exact 13/15 truth; no hidden,
test-only cable or inline-Inspect compatibility surface was added. This is
recorded as superseded acceptance, not a product or ontology failure.

## Package-wide environmental note

The authorized broad `bun test` completed with 498 pass, 43 fail, and 8 errors
across 541 tests. The failures remain unrelated package/environment noise:
Hermes/profile and packaged-resource fixtures, Windows/tmux/PTY/sidecar
dependencies, Electron and DOM stubs, Bun nested-describe behavior, and shared
image-extension fixtures. The 26 changed-surface tests, typecheck, production
PRE-R18 live control, and static/boundary checks are green. No unrelated failure
was repaired.

## Evidence, visual inspection, and cleanup

The exact capture hashes, sizes, counts, and state meanings are in
[SCREENSHOT-MANIFEST.md](./SCREENSHOT-MANIFEST.md). Frames 01, 04, 07, 09, 12,
13, and 14 were inspected at original resolution. They show a discoverable empty
START, readable staged DEFAULT, real catalog roles/actions, hierarchical Artifact
and Evaluation Dock inspection, and a subordinate-cable FULL overview with no
floating inspector or accidental selection.

Every live PRE-R18 run ended with `roots_remaining=0 leaked=[]` and
`cleanup=clean`. The exact interrupted verifier root named in
`VERIFICATION-FINAL.md`,
`C:\Users\rybow\AppData\Local\Temp\qf-pre-r18-coherence-2lZN1J`, is
already absent and has zero external owning processes. Pre-existing Atlas and
installed acceptance processes were not touched.

Atlas is generated only after this evidence ordering is committed. Its final
generate/check/ratchet/build-base-diff exits and separate Atlas commit SHA are
reported in the immutable handoff. The normal-app Computer Use check likewise
runs only after all candidate bytes are committed; its
`CONSUMER-READINESS` result is reported against that immutable final SHA.
