# WO-008c — Hermes host-bridged ACP (Outcome A outside the guest)
status: done — verified + merged 2026-07-20 (rework D1–D2)
assignee: builder
depends: WO-008b (probe held — in-guest native exec impossible on AgentOS 0.2.7)
created: 2026-07-20

## Objective

Hermes speaks ACP as a **real host process** (`hermes acp`), driven by QuantFlow’s Electron host. Dock **Spawn** still admits a session and opens a **session tile** — same founder UX — without asking AgentOS’s WASM-only guest to exec Python/ELF. Handshake only; no live turn (WO-008a).

## Why this order exists (measured)

WO-008b proved: authorized mounts make Hermes files **visible** in the guest; guest `spawn` of Hermes/Python fails with `ERR_NATIVE_BINARY_NOT_SUPPORTED` / WASM refuse. Bundle hits the same wall. See `species/hermes/D0-MOUNT-PROBE.md`. AgentOS remains correct for **Node/WASM ACP guests** (toolloop, critic-mock, future Pi-in-AgentOS). Hermes is a **host-bridged** species.

## Context pack

1. `species/hermes/D0-MOUNT-PROBE.md` + WO-008b verification record
2. `collab-electron/src/main/agent-host.ts` — `admitAndStartSession` / `runTurn` (WO-007b)
3. Hermes programmatic integration — **ACP** = JSON-RPC over stdio (`hermes acp`); same protocol AgentOS uses for in-VM guests
4. Legacy `acp-agent.ts` is debt #14 (auto-approve) — do **not** revive it; new bridge is deny-by-default ready for WO-008a (no permission handler required yet if you never prompt)

## Deliverables

**0. Host ACP client for Hermes (handshake).**
On the **Electron main / host** side (not inside AgentOS guest exec): spawn `hermes acp` (absolute `HERMES_BIN` from founder config / PATH resolve recorded in the report — paths only, never secrets) as a stdio child; complete ACP initialize + `session/new` (or equivalent); adopt the **ACP session id** into Kernel `create_agent_session` + `start_agent_session`. **Send no prompt.** Paste session id + proof of no chunks/artifacts.

Prefer a small module (e.g. `hermes-host-bridge.ts`) owned by `agent-host` routing — extract-first if `agent-host.ts` would grow past comfort; `renderer.js` / `tile-manager.js` still must not grow.

**1. Species launch routing (generic, not a one-off hack).**
`admitAndStartSession("hermes")` (and dock spawn) must take the **host-bridge** path for Hermes. In-VM AgentOS `createSession` remains for AgentOS packages (toolloop / critic-mock). Document the rule in one place (species flag, package manifest field, or host allowlist of `launch: "host_acp"` — pick the smallest data-driven shape; **stop and report** if you need a schema column). No `if (species === "hermes")` scattered across renderer.

**2. Dock → tile UX.**
Register Hermes → dock lists it → **Spawn** → session tile opens, Kernel `created`+`started`, chip/status live. **Do not Run turn** on Hermes. Paste screenshot + event types. Critic-mock / toolloop must still spawn via AgentOS (regression: one mock admit still green).

**3. d0-smoke Outcome A (host path).**
Update `species/hermes/d0-smoke.ts` (or a sibling host-smoke) so Outcome A means: host-bridged handshake succeeded. Keep typed failure modes honest (UNKNOWN vs old B). Exit 0 on A. No prompt.

**4. Lifecycle hygiene.**
Cancel / close from dock (legal edges) tears down the host child; no orphan `hermes` processes (count before/after). Cold reopen: no phantom `running` Hermes rows (Law D pattern).

## Contract

- No prompt / tool turn to Hermes (WO-008a).
- No credentials in repo, fixtures, or logs. Hermes `~/.hermes/` stays Hermes’.
- Do not weaken `agent-path` falsifications for the AgentOS mock path.
- Mounts plumbing from WO-008b may stay (useful for data dirs); it is **not** the Hermes exec path.
- Extract-first: `renderer.js` / `tile-manager.js` must not grow.
- Prefer generic `host_acp` launch over Hermes-only special cases in the UI.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).
2. Host handshake log — Outcome A / exit 0 (no prompt).
3. Dock spawn Hermes → tile + `created`/`started`; no Run turn.
4. Mock/toolloop still admits via AgentOS (one green paste).
5. Orphan check: cancel/close → zero leftover hermes children.
6. `cd collab-electron && bun run build` — exit 0.
7. Line counts: `renderer.js` / `tile-manager.js` before/after.

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Out of scope

Permission UI / allowlists (**WO-008a**) · A2A · putting Hermes back inside AgentOS guest exec · Docker-as-AgentOS substitute (Hermes-in-Docker is fine *as the host process* Hermes itself uses; QuantFlow still talks ACP to that process).

## Report back

1. Gates 1–7, unedited.
2. Where launch routing lives (one path).
3. Three decisions the order didn't specify, one line each.
4. Anything ambiguous or unbuildable — stop and say so.

---

## Verification — round 1 (2026-07-20) · REWORK (science stands; structure does not)

**Branch `wo-008c` @ `077ca4f`, cold `/tmp/verify-008c`: 12/12 gates, exit 0. Build exit 0. Line counts held (renderer 1746, tile-manager 969). `agent-host.ts` 527→654 (under 1k; dual admit extract OK).**

**Science accepted (Hermes ACP docs + measured):**
- Host `hermes acp` stdio JSON-RPC handshake → Outcome A, exit 0 (`d0-smoke` re-run: session minted, no prompt, orphan check OK). Matches [Hermes ACP](https://hermes-agent.nousresearch.com/docs/user-guide/features/acp) / ACP internals boot (`initialize` + `newSession`).
- `host-admit-kernel` after pack → Kernel `created`+`started`, zero artifacts (dock Spawn equivalent).
- AgentOS toolloop `agent-path` still green. `runTurn` forbidden on `host_acp`. Deny-by-default `requestPermission` (not legacy auto-approve). Gate allowlists `host-acp-bridge.ts` for ACP SDK.

**Defects (thermo + seam — fix on same branch; do not merge):**

- **D1 — `launch: "host_acp"` does not survive pack / is not the deploy source of truth.** Source `agent-package/agentos-package.json` has `"launch":"host_acp"`, but packed `hermes.tar` → `agentos-package.json` **drops it** (only `name` / `agent.acpEntrypoint` / `version`). `species-launch.ts` reads the **unpacked source tree sibling**, not the packed package. Repo checkout works by accident; a deploy that only ships `packed/*.aospkg` falls through to `agentos` and Hermes hits the WASM wall again. **Fix:** preserve `launch` through pack **and/or** resolve launch from the packed manifest (or a host config keyed by species) so the runtime path does not depend on leftover source files. Paste packed JSON + a falsify (delete/rename source agent-package → still host_acp).

- **D2 — ACP client triplicated.** Identical deny-by-default + `ClientSideConnection` + spawn/teardown live in `host-acp-bridge.ts`, `species/hermes/d0-smoke.ts`, and `species/hermes/host-admit-kernel.ts`. **Code judo:** one shared host-ACP client module; Electron and hermes smokes import it (e.g. move core under `species/hermes/` or a tiny package — Electron imports the species helper, not three copies). Behavior unchanged.

Fix D1–D2 only; re-run Outcome A + host-admit + static gates; push. **Do not merge.**

---

## Verification — round 2 (2026-07-20) · PASS

**Branch `wo-008c` @ `44e9318`, cold `/tmp/verify-008c-r2`: `bun qa/run.ts --all` → 12/12, exit 0. Build exit 0 after install. Line counts held (renderer 1746, tile-manager 969). `agent-host.ts` 654 (&lt;1k).**

**D1 closed (measured):** pack writes `packed/hermes.meta.json` `{"launch":"host_acp",…}`; packed root `agentos-package.json` still strips `launch` (toolchain). Committed `species/hermes/launch.json` is durable SoT. Falsify: rename `agent-package/` away → `host_acp` still from meta / `launch.json`. `species-launch.ts` precedence: meta → launch.json → host `speciesLaunch` → source last.

**D2 closed (measured):** sole `ClientSideConnection` / deny-by-default / spawn for host ACP in `species/hermes/host-acp-client.ts`. Electron `host-acp-bridge.ts` is a re-export; `d0-smoke` + `host-admit-kernel` import the shared module.

**Science re-run:** Outcome A exit 0 (`session=bfafebda-…`); host-admit Kernel `created`+`started`; agent-path green; orphans OK. Hermes ACP docs still match (stdio `hermes acp`, initialize + newSession, no prompt).

**Accepted / carry-forward (not blockers):**
- Default `agentos` when no launch doc remains correct for AgentOS species; Hermes is covered by committed `launch.json` + pack meta.
- `kernel-sole-writer-app` allowlists the thin bridge while the SDK import lives under `species/` (outside the Electron walk) — **WO-008a deliverable 4** must make that perimeter real.
- Optional: drop source-manifest fallback later if unused.

**Rotation:** merge · WO-008c → done · NEXT → **WO-008a** (order written this commit).
