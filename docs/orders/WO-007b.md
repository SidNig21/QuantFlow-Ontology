# WO-007b — Host seams: the spawn god-function dies
status: open
assignee: builder
depends: WO-007 (merged); `species/**` remains out of bounds (WO-008's admitting lane — now closed)
created: 2026-07-19

## Objective

Connecting and speaking become different acts: `spawnAgentSession` (admit + create + always-prompt + publish, one function) splits into `admitAndStartSession` and `runTurn`, session env gains a generic, data-sourced channel, and the shipped WO-007 post-merge findings land — so a real agent can plug in without being forced to talk.

## Context pack

1. `collab-electron/src/main/agent-host.ts` — the god-function (`spawnAgentSession`, line ~253, default prompt `"uppercase quantflow"`) and the `slowChunkMs`-only env special case (~269–273)
2. `docs/orders/WO-008.md` §measured facts — guest env is caller-supplied only (`agent-os.js:2693`); manifest-env defaults exist **only as a comment** (`agent-os.js:2689`), absent from the typed surface
3. `qa/gates/agent-path/` — imports the host's shared functions; its falsifications may not weaken
4. Reviewer post-merge findings (2026-07-19): session-tile Cancel not gated to legal edges (dock is); `closeSession` missing from the universal preload (shell has it); renderer `definitions[0]` spawn special-case (~line 994)

## Deliverables

**0. Probe first — verify-or-reject manifest env defaults.** Pack a throwaway test package whose manifest declares env defaults; measure whether `createSession` merges them into `launchEnv`. **If yes:** manifest env is THE per-species env channel (pure data — document it in the order report and use it in deliverable 2). **If no:** stop after deliverable 1 and report — the architect chooses between an `agent_definition` env column and a host resolver; do not invent one.

**1. The split.**
- `admitAndStartSession(species)`: resolve row → `linkSoftware` → `createSession` → `create_agent_session` (ACP id adopted) + `start_agent_session`. **Sends nothing to the agent.** Proof: after it returns, the event log holds exactly `created` + `started` for that session; zero chunks, zero artifacts.
- `runTurn(sessionId, promptText, opts)`: the prompt + streaming + optional publish half, callable any number of times on a running session.
- Dock spawn IPC → `admitAndStartSession` only. The mock-species demo turn stays reachable: a tile/dev affordance calls `runTurn` explicitly (say where you put it).
- `qa/gates/agent-path` updated to the new names, coverage **unchanged or stronger**: all four falsification pairs still red/green, plus one new assertion — spawn-only yields no chunks/artifact until `runTurn` (falsify: call `runTurn` → chunks appear; paste both directions).
- Delete the `slowChunkMs` special case in favor of the generic env/opts channel.

**2. Generic session env** — plumbed per deliverable 0's outcome; values sourced from species data (manifest or row), **never** from the renderer. Renderer-supplied env is rejected at the IPC boundary.

**3. Post-merge findings land:** tile Cancel gated to legal edges exactly like the dock · `closeSession` in the universal preload (symmetric with cancel) · `definitions[0]` special case removed.

**4. Extract-first guardrail (binding on this and every later host order):** `renderer.js` (~1767 lines) and `tile-manager.js` (~969) end this order **no larger than they started** — anything new gets extracted into modules first. Paste before/after line counts.

## Contract

- Do not touch `species/**` (WO-008's lane), `qf-kernel-schema/**`, `packages/qf-kernel/**`, or any schema surface.
- No behavior change to the mock demo path beyond the split (a turn still happens when explicitly asked).
- All standing gates green; `agent-path` falsifications may not weaken; seam laws hold (`agent-host.ts` sole AgentOS importer via `kernel.ts` reads).
- No permission handling, no allowlists, no Hermes-specific code — WO-008a's lane.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).
2. `bun qa/gates/agent-path.ts` — green, all four original falsification pairs **plus** the new spawn-only/turn pair.
3. Deliverable 0's probe result, with the measured evidence either way.
4. `cd collab-electron && bun run build` — exit 0.
5. Line counts: `renderer.js` and `tile-manager.js` before/after.

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Report back

1. Gates 1–5, unedited.
2. Where the explicit `runTurn` affordance lives, one line.
3. Three decisions the order didn't specify, one line each.
4. Anything ambiguous or unbuildable — stop and say so.
