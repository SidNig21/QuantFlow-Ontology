# WO-008e — A2A proof: 4 Hermes tiles (orchestrate → fan-out → review → talk-back)
status: open
assignee: builder
depends: WO-008d (merged — Hermes TUI term tile exists; multi-agent orchestration not proved)
created: 2026-07-20
amended: 2026-07-20 (founder: 4-tile fan-out/fan-in, not a 2-tile ping)

## Objective

Prove the collaboration foundation with **four Hermes term tiles** on one canvas:

1. **Orchestrator** — issues one task  
2. **Worker A** and **Worker B** — receive that task **simultaneously** and produce work  
3. **Reviewer** — receives **both** workers’ submissions, reviews, and **talks back to the orchestrator** with both results cleanly attributed  

All routing is **Kernel-mediated** (host adapter OK). No guest-to-guest side channel. Datasets stay parked.

## Why this order exists (founder)

One Hermes TUI (WO-008d) is a seat. The product needs Hermes as an **orchestrator of other agents on the canvas**. A two-tile ping is too thin. The bar is fan-out to two workers in parallel, fan-in to a reviewer, and a clean talk-back to the orchestrator — the smallest shape that looks like real multi-agent work.

## Context pack

1. `docs/orders/WO-008d.md` — native TUI term tile + Kernel `agent_session` + orphan hygiene  
2. `collab-electron/src/main/host-native-tui.ts` / `agent-host.ts` / species route  
3. Host ACP substrate (WO-008a) — may power machine delivery into a tile; desk UX stays TUI  
4. Schema intent: `task` + `assigned_to` / `delegates_to` (`docs/ONTOLOGY_SCHEMA.md`) — **measure** whether Kernel can create these today  

## Roles (fixed labels for the demo)

| Role | Tile | Job |
|---|---|---|
| Orchestrator | Hermes #1 | Emits one task payload (text OK for v0) |
| Worker A | Hermes #2 | Receives task; produces submission A |
| Worker B | Hermes #3 | Receives **same** task at the same dispatch; produces submission B |
| Reviewer | Hermes #4 | Receives A+B; reviews; sends talk-back to Orchestrator naming both |

Same `hermes` binary / species is fine — roles are **session roles**, not four dock species, unless the smallest path needs distinct `agent_definition` rows (prefer not).

## Deliverables

**0. Measure the gap (paste unedited).**
- Spawn Hermes ×4 today → four Kernel sessions + four term tiles? Paste session ids or the failure.  
- Does Kernel implement `task` / `delegates_to` / equivalent? Paste API or “missing.”  
- Smallest Kernel shape for: one task → two assignees (or two linked assignments) → two submissions → one review talk-back. Prefer existing schema. **Stop and report** if a schema order is required.

**1. Four live Hermes tiles.**
Four `native_tui` sessions; four term tiles; four Kernel rows `created`/`started` (or current legal pair). Cancel any one → that PTY dies; the other three stay; orphan check OK.

**2. Fan-out: one task → Workers A and B simultaneously.**
- Orchestrator (or host test harness acting as orchestrator) creates a **Kernel-backed** task/dispatch naming both workers and one payload.  
- Host delivers into **both** worker tiles.  
- **Simultaneous** means one dispatch action (one command or one atomic host call) that targets both — not “spawn A, wait, spawn B.” Paste timestamps or a single event id proving one fan-out.  
- Both workers’ tiles visibly react (task text / turn starts).

**3. Fan-in: both submit → Reviewer.**
- Each worker’s result is recorded as a Kernel-backed submission (artifact, task result, or measured equivalent — pick smallest; one sentence in report).  
- Reviewer receives **both** submissions (host delivery into Reviewer tile).  
- Reviewer tile visibly shows / reacts to both, attributed (A vs B distinguishable).

**4. Talk-back: Reviewer → Orchestrator.**
- Reviewer (or host completing the review step) sends a Kernel-backed talk-back to the Orchestrator that references **both** workers’ work.  
- Orchestrator tile visibly receives it.  
- Receipts: Kernel events/rows traversable Orchestrator → workers → reviewer → talk-back (paste ids).

**5. No guest-to-guest side channel.**
Handoffs go through Kernel (+ host adapter). Direct Hermes↔Hermes net/files as control plane = fail. Falsify by breaking the host delivery path: workers or reviewer go silent (red); restore → green. Paste both outputs.

**6. Hygiene.**
Extract-first on shell JS. `agent-host.ts` under 1k. Static gates green; build exit 0. Evidence under `docs/orders/evidence/wo-008e/`.

## Contract

- **4 tiles + one fan-out + both submissions + reviewer talk-back** is the bar.  
- Default Spawn UX stays Hermes TUI. Do not replace tiles with ACP “Run turn” chrome (host ACP OK under the hood).  
- No credentials in repo/logs. Paths only.  
- Do not weaken AgentOS `agent-path`.  
- Do **not** start datasets / Bovada / Python sidecar / WO-012 species packs in this order.  
- A scripted host harness that drives the Kernel path (without the founder typing four TUIs) is **allowed** if the four tiles still show the deliveries — founder visual still required once.

## Acceptance gates — builder-run (paste unedited)

1. Every static gate (PROTOCOL rule).  
2. Deliverable 0 measurement.  
3. Four Hermes term tiles + four Kernel session ids.  
4. Fan-out: one dispatch → A and B both react (proof of simultaneity).  
5. Fan-in: Reviewer receives both submissions, attributed.  
6. Talk-back: Orchestrator receives reviewer message covering both.  
7. Falsify delivery path → silent; restore → works (both outputs).  
8. Cancel one session → orphan OK; other three live.  
9. `cd collab-electron && bun run build` — exit 0.  
10. Line counts: `renderer.js` / `tile-manager.js` / `agent-host.ts` before/after.

**Never `bun qa/run.ts --all`, never delete `node_modules`.**

## Out of scope

Datasets (**WO-009**) · distinct Researcher/Backtester/Critic species packs (**WO-012**) · rewriting Hermes upstream · pretty canvas cables beyond proving the flow · betting / live markets.

## Report back

1. Gates 1–10, unedited.  
2. Delivery path (PTY inject vs host ACP vs mix) — one sentence.  
3. Kernel command/event names for fan-out, submit, talk-back — one path each.  
4. Three decisions the order didn't specify, one line each.
