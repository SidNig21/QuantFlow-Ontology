# WO-GOLDEN-G3 — Retire peer-bus and critic-mock package islands

status: **DRAFT — SEMANTIC READER ONLY**
order-type: Golden Baseline Phase 2 non-rung group
branch: `wo-golden-g2`
founder-approved-route: G3 — peer-bus and critic-mock islands
parent-group: G2 **CLOSED / ACCEPTED** at `1ae84771d043c77bebaece4f886096c8cae5b981`
r18-authority: **FROZEN**
main-authority: **NONE**
builder-authority: **NONE UNTIL READER YES/YES + NEXT.md ROTATION**

## Outcome

QuantFlow retains one current collaboration truth path and one current governed critic path. The standalone `tools/qf-peer-bus` package, its separate package contract, and the obsolete `species/critic-mock` package leave active main only after proof that no production, package, compatibility, QA, or named future-rung consumer needs them.

This group must not delete or relabel the app-owned peer-bus transport used by current Electron collaboration, nor the current Kernel governed-review path.

## Reader questions

One fresh Reader must answer exactly:

1. Can every acceptance gate actually fail on the defect it names?
2. Does every deliverable have exactly one meaning?

The Reader must additionally:

- prove the standalone package island is distinct from current app-owned `peer-bus.db`, `peer-delivery.ts`, Agent Host, Dock collaboration, and Windows/Hermes paths;
- prove `species/critic-mock` is not a current production profile, supported compatibility route, package input, or R18–R25 dependency;
- identify every exact test, Atlas falsifier, allowlist, document, package declaration, lockfile, and dependency that names either island;
- decide whether the lifecycle-command invariant remains current and, if so, specify the smallest dedicated QA fixture that preserves it without retaining an obsolete package;
- distinguish G3-local dependency removal from broader G4 AgentOS, G5 ACP, and G7 direct-dependency ownership.

Any unproven production/package/compatibility consumer is Reader `NO`.

## Frozen starting evidence

After Reader `YES/YES` and before mutation, record:

- clean `BUILD_BASE_SHA` and exact manifest of every file under both roots;
- import, launch, package-resource, build-output, installer, Dock-profile, migration/state-compatibility, QA, documentation, and R18–R25 consumer census;
- root and package-local manifest/lock dependency ownership;
- exact current app-owned collaboration files and gates that must remain;
- complete focused starting matrix, including every pre-existing red;
- Atlas clean-tree precondition and exact model/ratchet baseline.

No target may differ between Reader evidence and Builder start.

## Deliverable A — Prove two obsolete islands

Create `docs/orders/evidence/golden-baseline/g3/BEFORE.md` classifying every reference as production, package/build, supported compatibility, current QA, current authority, history, generated output, or filename/comment only.

Prove zero production or packaged consumer of the two standalone packages while separately proving current app-owned collaboration and governed review remain live. Reachability alone is not retention or removal evidence.

## Deliverable B — Remove exact package-owned residue

Delete only Reader-approved literal files beneath the two roots and package-local manifests, locks, and docs that exist solely for those islands.

Remove root manifest/lock entries only when a dependency becomes unreachable solely because of G3. Broader AgentOS, ACP, protocol, or dependency contraction remains G4, G5, or G7.

Do not delete user state, `.qf-peer-bus` compatibility data, app-owned transport tables, Electron collaboration code, or current critic/governed-review code.

## Deliverable C — Preserve proof meaning

Retarget or remove stale references only after naming the invariant:

- lifecycle-command falsification uses a minimal dedicated QA fixture if its generic package-script invariant remains current;
- `kernel-one-path` and `kernel-sole-writer` continue to fail on real unauthorized current paths without deleted subjects;
- product identity and current docs describe the current product, not critic-mock;
- Atlas falsifiers and explanations retain their meaning on current or purpose-built temporary subjects;
- historical evidence may remain history but may not claim active authority.

No assertion may be deleted, weakened, or made vacuous merely to green G3.

## Deliverable D — Prove the surviving path

Focused proof must establish:

- current app-owned peer-bus transport still delivers its collaboration contract;
- Kernel Task/Artifact/Evaluation/Report truth remains authoritative;
- governed-review and independent-critic paths pass;
- no production Dock inventory or runtime route references the mock;
- build and dependency graphs contain no deleted-island reference;
- shutdown and isolated temp-state cleanup reach zero.

## Focused acceptance matrix

The Reader may tighten commands but may not reduce these meanings:

1. literal manifest and production/package consumer census;
2. lifecycle-command falsifier on retained or minimal replacement fixture;
3. `kernel-one-path` and `kernel-sole-writer`;
4. governed-review and independent-critic focused proof;
5. app-owned Dock collaboration/peer-delivery proof;
6. Dock production inventory and product identity;
7. dependency graph, lock consistency, repo shape, Electron build;
8. Atlas `--check`, all falsifiers, ratchet HARD RED 0, tree neutrality;
9. literal deletion/build-output absence and `git diff --check`;
10. process and temp-root cleanup zero.

Full Windows installer/release traversal is unnecessary unless the Reader proves G3 changes an included package boundary. Phase 3 retains mandatory full qualification.

## Throughput and stops

- Freeze the starting matrix before mutation.
- Semantic changes require Reader adjudication.
- A starting-SHA red strictly limited to same-meaning classification, selector, allowlist, invocation, teardown, generated metadata, or receipt formatting may be repaired directly by the Router. It gets one focused old-red/new-green falsifier plus non-regression.
- Independent commands may run concurrently only without shared-state mutation. Exact-byte receipts may be reused when relevant bytes/configuration are equal.
- One immutable candidate and one independent Verifier remain mandatory.
- The same semantic assertion red twice after repair stops G3. A red owned by another Golden group is assigned there only if G3 non-regression remains provable.

G3 may not merge to `main`, open G4 Builder authority, begin R18, or combine groups.
